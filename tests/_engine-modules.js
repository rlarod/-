/* tests/_engine-modules.js
 * =========================================================================
 * 거래 엔진(js/trading.js)을 sandbox 에 태울 때 ★같이 태워야 하는 것★ — 단 하나의 목록
 * =========================================================================
 *
 * ── ⚠️⚠️ 2026-08-31 — ★이름을 바꿨습니다★ (_sandbox-modules.js → _engine-modules.js)
 *   처음 이름이 tests/_sandbox-modules.js 였습니다.
 *   tests/tests-dir-hygiene.test.js 의 ★약한금지★ 낱말에 "sandbox" 가 있습니다.
 *
 *   ⚠️ 기록팀이 그 함정을 ★자기 손으로 문서에 적어놓고 자기가 밟았습니다.★
 *      (같은 날 tests/temp-file-guard.test.js 가 자기 이름에 걸려 개명한 일을
 *       바로 그 파일 주석에 적어뒀는데도 그랬습니다)
 *
 *   ★단독 실행으로는 안 잡혔습니다.★ 그 검사는 "git 이 추적하는" 파일만 봅니다.
 *   파일이 git 밖일 때는 목록에 없어서 통과했고(그때 npm test 159/159),
 *   PM 이 git add 한 ★그 순간★ 빨강이 됐습니다. 재실행 없이 커밋돼 c6242d8 로
 *   배포까지 나갔습니다.
 *
 *   ── 왜 "예외" 가 아니라 "이름 바꾸기" 인가 ──
 *     1) 예외를 한 번 만들면 다음 사람이 자기 파일도 넣습니다. 예외 목록은 자랍니다.
 *     2) "sandbox 는 이 파일이 진짜로 하는 일의 이름 아니냐" 는 반론이 있었습니다.
 *        (js/jitter-probe.js 의 "probe" 가 실제 모듈 이름인 것처럼)
 *        ★아닙니다.★ 이 저장소에 js/sandbox.js 같은 모듈은 없습니다.
 *        "sandbox" 는 테스트 도구를 가리키는 일반 낱말이라 probe 와 다릅니다.
 *        게다가 이 목록이 진짜로 가리키는 것은 ★엔진이 필요로 하는 모듈★ 입니다.
 *        _engine-modules 가 하는 일을 더 정확히 말합니다.
 *     3) 이름을 바꾸면 ★원인이 사라집니다.★ 예외는 증상만 가립니다.
 *
 *   ⭐ 같은 사고가 또 안 나게 tests/tests-dir-hygiene.test.js 에 [5] 를 넣었습니다 —
 *      ★도우미목록·_order.txt 에 등록된 파일은 git 추적 여부와 상관없이★ 이름을 봅니다.
 *      등록했다는 것 자체가 "이건 임시 파일이 아니다" 라는 선언이기 때문입니다.
 *      그러면 git add 전에도 잡힙니다.
 *
 * ── 왜 이 파일이 생겼나 (2026-08-31) ────────────────────────────────────
 *   대표 결재로 유지증거금이 바이낸스 명목 구간별로 바뀌면서
 *   js/risk-brackets.js 가 생겼습니다. js/trading.js 는 그 표가 ★없으면★
 *   예전 고정값(MMR_FALLBACK 0.5%)으로 조용히 되돌아갑니다.
 *
 *   봉인 16개가 그 표를 안 태우고 있었습니다. 결과 —
 *       테스트는 초록  ↔  회원은 다른 값을 겪음
 *   화면도 안 깨지고 오류도 안 납니다. ★우리가 P1 로 부르는 조용한 고장★ 입니다.
 *
 *   실제로 갈렸던 값 — 청산가 59,700(테스트) vs 59,924.40(회원),
 *   99,550(테스트) vs 99,440(회원), 지갑 200,000 진입 성공(테스트) vs 거절(회원).
 *
 *   ⚠️ 그때 harness.js 를 안 쓰는 새 테스트가 또 생기면 ★같은 일이 반복됩니다.★
 *   그래서 "무엇을 태워야 하는가" 를 ★여기 한 곳★ 에만 적습니다.
 *
 * ── 쓰는 법 ─────────────────────────────────────────────────────────────
 *   tests/harness.js 를 쓰면 자동으로 태워집니다. 아무것도 안 해도 됩니다.
 *
 *   harness 를 못 쓰고 직접 sandbox 를 만드는 테스트는 이렇게 씁니다 —
 *
 *     const { 엔진필수, 엔진뒤 } = require("./_engine-modules.js");
 *     엔진필수.forEach((m) => vm.runInContext(read(m.경로), sandbox, { filename: m.경로 }));
 *     vm.runInContext(read("js/trading.js"), sandbox, { filename: "js/trading.js" });
 *     엔진뒤.forEach((m) => vm.runInContext(read(m.경로), sandbox, { filename: m.경로 }));
 *
 *   ⭐ 엔진필수 는 js/trading.js ★보다 먼저★, 엔진뒤 는 ★그 뒤에★ 태웁니다.
 *      (index.html 과 같은 순서입니다)
 *
 * ── ⚠️ 왜 목록이 둘인가 (2026-09-01) ────────────────────────────────────
 *   태우는 시점이 반대인 모듈이 생겼습니다.
 *
 *     엔진필수  엔진이 ★읽어가는★ 것        → 엔진보다 먼저 있어야 읽힙니다
 *     엔진뒤    엔진 함수를 ★감싸는★ 것      → 엔진이 먼저 있어야 감쌀 수 있습니다
 *
 *   순서가 뒤집히면 ★조용히 아무 일도 안 일어납니다.★ 감싸는 모듈은
 *   App.Trading 이 아직 없으면 그냥 되돌아가고(오류도 안 냅니다),
 *   테스트는 초록인데 회원은 감싸지지 않은 옛 동작을 겪습니다.
 *   그래서 목록을 하나로 합치지 않고 ★시점까지 같이 적습니다.★
 *
 * ── 늘릴 때 ─────────────────────────────────────────────────────────────
 *   엔진이 새로 의존하는 모듈이 생기면 여기에 한 줄 추가하세요.
 *   ★이유를 반드시 적으세요.★ "안 태우면 무엇이 조용히 달라지는가" 를 적습니다.
 *   tests/mmr-fallback-blindspot.test.js 가 이 목록을 그대로 읽어
 *   엔진을 태우는 모든 테스트를 검사합니다.
 * ========================================================================= */
"use strict";

const 엔진필수 = [
  {
    경로: "js/risk-brackets.js",
    이유:
      "바이낸스 명목 구간별 유지증거금표(2026-08-31 대표 결재). 안 태우면 js/trading.js 가 " +
      "MMR_FALLBACK = 0.005 로 되돌아가, 회원이 겪지 않는 옛 고정값 0.5% 를 재게 됩니다. " +
      "청산가가 실제로 달라집니다 (예: 59,924.40 → 59,700).",
    확인법: "App.RiskBrackets 가 있으면 태워진 것입니다",
  },
];

/* 엔진(js/trading.js) ★뒤★ 에 태워야 하는 것 — 엔진 함수를 감싸는 모듈들.
   먼저 태우면 App.Trading 이 아직 없어서 ★아무 일도 안 하고 되돌아갑니다.★
   오류가 안 나기 때문에 알아채기가 어렵습니다. */
const 엔진뒤 = [
  {
    경로: "js/max-margin-safe.js",
    이유:
      "App.Trading.getMaxAffordableMargin 을 감싸, 되돌려 계산한 최대 증거금이 " +
      "엔진 진입 검사(margin + margin×배율×taker > balance)에 소수 마지막 자리 때문에 " +
      "걸리던 것을 막습니다. 안 태우면 ★테스트는 옛 동작(거절)을 보고 회원은 " +
      "새 동작(통과)을 겪습니다★ — 실측: 지갑 130,000·50배에서 " +
      "130,000.00000000001 > 130,000 으로 거절, 대표님 잔고 98,986.53 에서는 " +
      "배율 조합 30개 중 15개가 거절됐습니다.",
    확인법: "App.MaxMarginSafe 가 있으면 태워진 것입니다",
  },
];

/* index.html 안에서 이 순서로 실려야 합니다
   (실측 2026-09-01 — 1268행 risk-brackets → 1269행 trading → 1272행 max-margin-safe). */
const 엔진 = "js/trading.js";

module.exports = { 엔진필수, 엔진뒤, 엔진 };
