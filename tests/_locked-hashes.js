/* tests/_locked-hashes.js
 * =========================================================================
 * 수정 금지 12개 파일의 md5 — ★단 하나의 출처★
 * =========================================================================
 *
 * ── 왜 이 파일이 생겼나 (2026-08-31) ────────────────────────────────────
 *   대표 결재로 js/trading.js 가 딱 한 번 열렸습니다.
 *
 *       대표 (2026-08-31)
 *         "ㅇㅇ 지금만 허용할테니까 바이낸스 시스템이랑 똑같이 따라해"
 *         "바이낸스 거래 시스템을 따라해 그것만 허용"
 *
 *   그 순간 옛 해시를 붙들고 있던 봉인 ★48개★ 가 한꺼번에 터졌습니다.
 *   ⚠️ 이건 사고가 아닙니다. 봉인이 제 일을 한 것입니다.
 *
 *   문제는 ★고치는 비용★ 이었습니다. 같은 32자리 문자열이 48개 파일에
 *   따로따로 박혀 있어서, 한 번 열릴 때마다 48곳을 손으로 고쳐야 했습니다.
 *   게다가 이번 결재는 A·B·C·D 네 건으로 나뉘어 있어 ★해시가 앞으로도
 *   서너 번 더 바뀝니다.★ 48 × 4 = 192번을 고치는 셈입니다.
 *
 *   손으로 192번 고치면 반드시 한 곳을 빠뜨립니다. 빠뜨린 그 하나는
 *   "실패한 테스트" 로 남는 게 아니라 ★남들 눈에 안 띄는 빨강 하나★ 로
 *   묻히고, 결국 누군가 그 봉인을 통째로 지워버립니다.
 *
 *   그래서 값은 ★여기 한 곳★ 에만 둡니다. 48개 파일은 이 파일을 읽습니다.
 *
 * ── 이러면 봉인이 약해지지 않나? ────────────────────────────────────────
 *   오히려 강해집니다. tests/locked-hashes-source.test.js 가 이 파일을 감시합니다.
 *     · 잠긴 11개는 ★CLAUDE.md 의 기준표★ 와 글자 단위로 같아야 합니다.
 *       (CLAUDE.md 는 대표 문서입니다. 여기 값만 몰래 고치면 그 자리에서 터집니다)
 *     · 열린 파일(js/trading.js)은 아래 결재기록 에 근거가 적혀 있어야 합니다.
 *   즉 "값을 바꾸는 것" 이 여전히 ★근거를 적어야만 되는 일★ 로 남습니다.
 *
 * ── ⭐ 다음에 js/trading.js 가 또 바뀌면 (B·C·D 건) ─────────────────────
 *   ① md5sum js/trading.js 를 찍는다
 *   ② 아래 결재기록 배열 맨 뒤에 한 줄 추가한다 (무엇을 바꿨는지 반드시 적는다)
 *   ③ 아래 열림 의 "js/trading.js" 값을 새 해시로 바꾼다
 *   그게 전부입니다. 다른 48개 파일은 손대지 않습니다.
 *
 * ── ⛔ 하지 말 것 ───────────────────────────────────────────────────────
 *   잠긴11 의 값을 고치는 것. 그 11개는 여전히 ★한 글자도 못 고치는★ 파일입니다.
 *   값이 안 맞는다면 파일이 바뀐 것이지, 여기가 틀린 게 아닙니다.
 * ========================================================================= */
"use strict";

/* 여전히 잠긴 11개 — CLAUDE.md 기준표(2026-08-20)와 같아야 합니다. */
const 잠긴11 = {
  "js/ui.js": "333fc427e75b47b306699c92aa4e7b50",
  "js/auth.js": "9cec9a7257eb54f379bf72e14e21e463",
  "js/supabase-sync.js": "faddcbbc34b5165177ff26cb978040f8",
  "js/chat.js": "a93dfaa7f82ce72a914b270acb3650bb",
  "js/leaderboard.js": "62e839f06e0565cca5d9216e484b6031",
  "js/admin.js": "424e4c63ec1cd24681c4f27f60aee2fa",
  "js/season.js": "9c5fbf13ced09ca2f348e48f87c78224",
  "js/board.js": "8b847bd8f5d8231b8dd329f8b15dbe37",
  "js/orderbook.js": "fa5f77dc5108133128f85ba5ab3f096e",
  "js/chart.js": "02ddcb000d577131f797143d08c09123",
  "js/websocket.js": "1a914631175760e0b0cb5144bc11b59e",
};

/* ⭐ 대표 결재로 열린 파일 — ★여기 한 줄만 고칩니다★ */
const 열림 = {
  "js/trading.js": "ff3fef520608d56d4c7eb6aef9b08dff",
};

/* 결재기록 — 열린 파일이 왜 바뀌었는지. 없으면 다음 사람이 "누가 몰래 고쳤나" 로 읽습니다.
   ⚠️ 새 해시를 넣을 때는 반드시 여기에도 한 줄 남기세요. */
const 결재기록 = [
  {
    날짜: "2026-08-20",
    파일: "js/trading.js",
    해시: "33250202c00b097ff8344ae2ee64cbe7",
    근거: "최초 봉인 기준값 (CLAUDE.md 기준표)",
    내용: "유지증거금률이 const MMR = 0.005 하나로 고정돼 있던 판",
  },
  {
    날짜: "2026-08-31",
    파일: "js/trading.js",
    해시: "7e26f9d581820f425b4402edd132d653",
    근거: '대표 결재 — "바이낸스 거래 시스템을 따라해 그것만 허용"',
    내용:
      "[A건] 유지증거금을 바이낸스 명목 구간별(js/risk-brackets.js)로 바꿨습니다. " +
      "const MMR = 0.005 → const MMR_FALLBACK = 0.005 (표를 못 읽을 때만 쓰는 값), " +
      "calcLiquidationPrice 에 notional 인자 추가, maintenanceMargin 공개. " +
      "유지증거금 ≥ 증거금 인 주문은 진입 즉시 청산이라 열리지 않게 막았습니다. " +
      "58줄 추가 / 9줄 삭제.",
  },
  {
    날짜: "2026-08-31",
    파일: "js/trading.js",
    해시: "ff3fef520608d56d4c7eb6aef9b08dff",
    근거: '대표 결재 — "바이낸스 거래 시스템을 따라해 그것만 허용" (A건과 같은 결재)',
    내용:
      "[B건] 명목 구간별 최대 배율(바이낸스 Leverage & Margin Bracket)을 적용했습니다. " +
      "bracketMaxLeverage(notional) 를 추가해 공개하고, 넘으면 bracketLeverageError 로 " +
      "왜 안 되는지 숫자로 알려주며 거부합니다. A건이 넣었던 '유지증거금 ≥ 증거금이면 거부' 를 " +
      "'구간 최대배율 초과면 거부' 로 ★대체★ 했습니다. 수리팀이 명목 1,000~20억을 훑어 " +
      "(개시증거금률 − 실효 유지증거금률) 최솟값 +0.4000% 로, 옛 검사가 새 검사에 완전히 " +
      "포함됨을 증명했습니다(tests/bracket-max-leverage-seal.test.js 가 그 증명을 봉인합니다). " +
      "호출 지점은 openPosition·placeLimitOrder 두 곳뿐이고 청산·부분청산·강제청산 경로에는 " +
      "없어서 ★이미 열려 있는 포지션은 강제로 줄지 않습니다.★ 37줄 추가 / 21줄 삭제. " +
      "같은 결재로 js/leverage-modal.js 도 바뀌었습니다(수정 금지 파일이 아닙니다) — " +
      "창이 따로 계산하지 않고 App.Trading.bracketMaxLeverage 를 그대로 부릅니다.",
  },
  /* C·D 건이 들어오면 여기에 이어 붙이세요 (날짜/파일/해시/근거/내용) */
];

/* CLAUDE.md 기준표와 이 파일이 같은 값을 들고 있는지 표시합니다.
   true  = CLAUDE.md 가 아직 옛 해시를 들고 있다 (PM 갱신 대기)
   false = CLAUDE.md 와 이 파일이 같다

   2026-08-31 12시대 — PM 이 CLAUDE.md 기준표를 7e26f9d5… 로 갱신했습니다.
   그래서 true → false 로 내립니다. 봉인이 ★양방향★ 이라 갱신 순간
   "CLAUDE.md 가 이미 갱신됐습니다 → false 로 바꾸세요" 로 터져서 알려줬습니다.
   B·C·D 건에서 해시를 바꿀 때 CLAUDE.md 를 아직 못 고쳤다면
   ★이 값을 잠깐 true 로 올려두세요.★ 그래야 잊혀지지 않습니다.

   2026-08-31 [B건] — 해시가 ff3fef52… 로 또 바뀌었습니다. CLAUDE.md 기준표는
   PM 이 직접 고칩니다(기록팀은 그 문서를 못 고칩니다). 그래서 false → true 로
   ★다시 올렸습니다.★ PM 이 문서를 고치는 순간 이 봉인이
   "CLAUDE.md 가 이미 갱신됐습니다 → false 로 바꾸세요" 로 터져서 알려줍니다.
   ⚠️ PM 께 — 기준표 줄 끝에 설명을 붙이지 마세요. 형식이 깨지면 md5sum -c 도
      이 봉인도 그 줄을 ★없는 줄★ 로 읽습니다. 설명은 표 위 제목 줄에 씁니다.
   tests/locked-hashes-source.test.js 가 ★양쪽 방향으로★ 확인합니다 —
     · 여기가 true 인데 CLAUDE.md 가 이미 갱신됐으면  → 실패 ("false 로 바꾸세요")
     · 여기가 false 인데 CLAUDE.md 가 아직 옛것이면   → 실패 ("문서를 갱신하세요")
   그래서 이 플래그는 ★잊혀지지 않습니다.★ */
const CLAUDE_MD_갱신대기 = false; // 2026-09-01 12:5x PM 이 CLAUDE.md 를 ff3fef52… 로 갱신함

const 전체 = Object.assign({}, 잠긴11, 열림);

/* 파일명만 쓰는 테스트("trading.js")와 경로까지 쓰는 테스트("js/trading.js")가
   섞여 있습니다. 둘 다 받도록 짧은 키도 같이 넣어 둡니다. */
const BY_FILE = Object.assign({}, 전체);
for (const k of Object.keys(전체)) BY_FILE[k.replace(/^js\//, "")] = 전체[k];

module.exports = {
  잠긴11,
  CLAUDE_MD_갱신대기,
  열림,
  결재기록,
  BY_FILE,
  /* 봉인 테스트들이 그대로 갖다 쓰는 이름 — 48개 파일이 이걸 읽습니다. */
  TRADING: 열림["js/trading.js"],
};
