# TL 정식 오픈 전 최종 점검 보고서

작성: 2026-08-19
대상: https://sigma-lovat-81.vercel.app/

---

## 1. 완료한 작업

### 오늘 잡은 버그 (22건)

**거래 계산**

| 문제 | 원인 |
|---|---|
| 실현손익이 실제보다 크게 표시 | 진입 수수료가 빠져 있었음 |
| 강제청산 손익 5원 오차 | 강제청산을 플래그로 찾음(실제는 `reason` 문자열) |
| 마이페이지·서버 저장값만 보정 안 됨 | 모듈 내부 호출은 감싼 함수를 안 탐 |
| 음수 SL이 실제 손절선이 됨 | "죽은 SL 보정" 규칙에 음수가 걸림 |

**데이터 유실 (가장 심각)**

| 문제 | 원인 |
|---|---|
| 청산 기록이 서버에 한 건도 저장 안 됨 | `return_rate` 컬럼이 테이블에 없음 |
| 새로고침마다 데이터 전체 삭제 | 시즌 번호 서버 8 vs 브라우저 없음 |
| 잔고가 서버에 저장 안 됨 | `BEFORE INSERT` 트리거가 upsert를 가로챔 |
| 포지션 보유 중 새로고침 시 잔고 증가 | 펀딩 정산 시각이 초기화돼 중복 지급 |
| 계정 전환 시 앞사람 데이터 노출 | 주인 표시가 없었음 |
| 창 두 개 사용 시 한쪽 기록 소멸 | 서로 덮어씀 |

**TL·랭킹**

| 문제 | 원인 |
|---|---|
| TL 잔액이 화면마다 다름 | 두 함수의 계산식이 달랐음 |
| 보유 TL 2,000 vs 36,560 | 계급 점수와 실제 잔액을 혼용 |
| 랭킹이 잔고 기준이라 순위 왜곡 | 포지션 보유만으로 순위 하락 |
| 랭킹 총자산·수익금 칸이 빔 | 서버가 그 칸을 안 줌 |
| 시즌 초기화 시 전 회원 TL 음수 | 사용 기록만 남고 획득이 0 |

**화면·기타**

| 문제 | 원인 |
|---|---|
| 모바일에서 메뉴 2개 접근 불가 | 470px 필요한데 화면 390px |
| TL 마켓 화면 가로 밀림 | 탭 4개가 482px |
| 실시간 채팅 계속 끊김 | Realtime 설정 |
| 청산 알림이 채팅에 수백 번 도배 | 제 보호장치가 목록을 비워서 |
| 청산 알림이 조용히 사라짐 | 도배 방지 트리거에 막힘 |
| 로그인해도 채팅 못 씀 | 제 코드가 포커스를 영구 차단 |
| 신고를 아무도 못 봄 | 관리자도 조회 불가 |
| 원화로 바꿔도 일부 화면 안 바뀜 | 통화 변경 신호를 안 들음 |

**제가 만든 버그 3건**(채팅 도배, 채팅 입력 차단, 잔고 저장 차단)도 포함돼 있습니다.

### 새로 만든 기능

- **닉네임 규칙** — 2~12자, 공백·특수문자·욕설·사칭 차단 (화면+서버)
- **개인정보 분리** — `customer_private_info`, 전화번호 마스킹, 본인·관리자만 조회
- **마이페이지 확장** — TL 내역, 핫딜·마켓 구매 내역, 로그아웃, 회원탈퇴

---

## 2. 수정한 파일

**새로 만든 모듈 (14개)**

```
js/price-guard.js            이상 시세 차단
js/tpsl-guard.js             TP/SL 입력 검증
js/realized-pnl-fix.js       실현손익 보정
js/funding-record-fix.js     펀딩 기록 정정
js/funding-restore-guard.js  펀딩 중복 지급 방지
js/sync-guard.js             서버 기록 보호
js/account-isolation.js      계정 전환 시 데이터 격리
js/multi-tab-guard.js        창 두 개 사용 경고
js/currency-refresh.js       통화 전환 반영
js/tl-balance-sync.js        보유 TL 일치
js/chat-status-calm.js       재연결 안내 정리
js/nickname-rules.js         닉네임 규칙
js/my-private-info.js        내 계정 정보
js/mypage-history.js         마이페이지 내역
```

**수정 금지 12개 파일은 하나도 건드리지 않았습니다.** 모든 수정은 별도 모듈로 우회했고, md5로 매 커밋 확인했습니다.

---

## 3. DB 변경사항

**실행 완료**

```
schema-realtime-fix.sql      실시간 채팅
schema-reset-season-fix.sql  시즌 초기화 안전장치
schema-report-admin.sql      신고 확인
schema-leaderboard-fix.sql   랭킹 기준
schema-tl-balance-fix.sql    TL 잔액
schema-chat-event-exempt.sql 청산 알림
거래저장-복구.sql             return_rate 컬럼
지갑초기화-해결.sql            잔고 트리거
시즌번호-해결.sql              시즌 번호 1로
schema-nickname-rules.sql    닉네임 규칙
schema-private-info.sql      개인정보 분리
schema-mypage.sql            마이페이지 함수
```

**새 테이블** — `customer_private_info` 하나뿐입니다. 기존 테이블은 컬럼 추가(`return_rate`)만 했고 삭제는 없습니다.

---

## 4. 필요한 환경변수

현재 추가로 필요한 것은 **없습니다.** Supabase URL과 anon key는 이미 설정돼 있습니다.

소셜 로그인을 붙이면 그때 필요합니다.

---

## 5. OAuth 설정에서 필요한 항목

**⛔ 이 부분은 사장님이 직접 하셔야 합니다.**

### 카카오 (권장 — 가장 쉬움)

1. https://developers.kakao.com/console/app → 애플리케이션 추가
2. **비즈 앱 전환** (카카오 로그인 사용 조건)
3. 플랫폼 → Web → 사이트 도메인: `https://sigma-lovat-81.vercel.app`
4. 카카오 로그인 **활성화 ON** + **OpenID Connect ON**
5. Redirect URI: `https://oxpjpotilcumjqixsdxw.supabase.co/auth/v1/callback`
6. 동의항목 → **닉네임 필수**
7. 앱 키 → **REST API 키** 복사
8. 보안 → **Client Secret** 발급
9. Supabase → Authentication → Providers → **Kakao** → 키 입력

### 네이버

Supabase가 기본 지원하지 않습니다. **Edge Function을 따로 만들어야** 합니다. 카카오보다 작업량이 큽니다.

### 전화번호

**SMS 발송 업체 계약이 필요하고 건당 과금**됩니다. 가입자 1,000명이면 최소 2만원, 재발송 포함 시 더 듭니다.

---

## 6. 테스트 결과

### 자동 테스트

```
테스트 파일 32개 / 통과 1,213개 / 실패 0
수정 금지 12개 파일 md5 변동 없음
```

### 비회원 흐름

| 항목 | 결과 |
|---|---|
| 홈이 바로 열림 | 통과 |
| 전체 화면 로그인창 없음 | 통과 |
| 내 정보에 로그인 폼 | 통과 |
| 거래 시도 시 차단 | 통과 |
| 6개 페이지 열람 | 통과 |

### 거래 흐름

| 항목 | 결과 |
|---|---|
| 포지션 생성 | 통과 |
| 미실현손익 | +166.67 |
| 실현손익 | +156.58 |
| 실제 번 돈 | +156.58 (일치) |
| 강제청산 | 통과 |
| 지정가 대기 → 체결 | 통과 |
| NaN 시세 방어 | 통과 |
| 음수 시세 방어 | 통과 |

### 모바일 (390px)

6개 페이지 모두 **가로 넘침 0**

### 다크모드

6개 페이지 모두 **안 보이는 글자 0건**, 로고 정상

---

## 7. 아직 해결하지 못한 문제

### ⛔ 소셜 로그인 (1·6순위)

사장님의 개발자 등록 없이는 진행 불가입니다.

### 펀딩비 미납분 처리

잔고를 0에 가깝게 두고 큰 포지션을 들면 펀딩비를 거의 안 내고 버틸 수 있습니다(실측 99.98% 면제). 실제 거래소는 증거금에서 차감 후 청산하는데, 그 계산이 `trading.js`(수정 금지) 안에 있어 손대지 않았습니다. **기록은 실제 낸 금액으로 맞춰뒀습니다.**

### 계급이 거래 횟수로 오름

손실 −21%인데 중장으로 표시됩니다. 200번 거래하면 손익과 무관하게 중장입니다. **경제 설계 결정 대기 중**입니다.

### TL 경제 설계 미결

논의만 하고 적용하지 않았습니다.

| 항목 | 현재 | 제안 |
|---|---|---|
| TL 지급 | 청산×10 + 수익률×20 | `1500 × log₂(1 + 수익/1천만)` |
| 계급 점수 | 거래 횟수로 오름 | 수익 위주 |
| 기본 레버리지 | 100배 무료 | 50배 + 이용권 |

지시서 12순위에 **"100배 이용권을 지금 강제 적용하지 않는다"**고 되어 있어 현재 100배 무료를 유지했습니다.

### 강제청산 수수료

바이낸스는 명목가의 1.25~2%를 별도 부과하는데, 우리는 증거금 전액 손실 방식입니다. **"아직 건들지마"** 지시에 따라 유지했습니다.

---

## 8. 월요일 오픈 전 반드시 확인할 사항

### 필수

1. **실제 계정으로 전 과정 한 번 돌려보기**
   가입 → 거래 → 청산 → 새로고침 → 잔고·기록 유지 확인
   제 환경은 Supabase·바이낸스가 차단돼 있어 실제 연결 상태를 확인할 수 없습니다.

2. **시즌 초기화 여부 결정**
   현재 데이터가 뒤엉켜 있습니다(잔고와 실현손익 불일치). 오픈 전에 한 번 초기화해 깨끗하게 시작하는 것을 권합니다. `schema-reset-season-fix.sql`이 적용돼 있어 안전합니다.

3. **효과 없는 마켓 아이템 판매 중지**
   수수료할인·청산보호·훔쳐보기 3종은 효과가 연결돼 있지 않습니다.
   ```sql
   update public.tl_market_products set status='paused'
    where item_type in ('fee_discount','liquidation_guard','position_peek');
   ```

4. **핫딜 상품 운영값 확정**
   재고·1인 구매 제한·특가 여부가 임시값입니다.

### 권장

5. **TL 경제 설계 확정** — 지금 커피 한 잔에 190회 거래가 필요합니다.

6. **관리자 계정 확인** — `admin_users`에 사장님 계정이 있는지.

7. **모바일 실기기 확인** — 브라우저 시뮬레이션은 통과했지만 실제 폰에서 한 번 보시는 게 좋습니다.

---

## 9. 오픈 후 제안 (지금은 하지 않음)

- 강제청산 수수료를 바이낸스 방식으로
- 펀딩비 미납분 증거금 차감
- 신고 처리 관리자 화면
- 쿠폰 자동 발송 연동

---

**정리하면, 소셜 로그인을 제외한 기본 기능은 모두 동작합니다.**
가장 큰 위험이었던 데이터 유실 문제 6건이 오늘 전부 해결됐습니다.
오픈 전 실제 계정으로 한 번만 전 과정을 돌려봐 주세요.
