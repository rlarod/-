-- =========================================================================
-- trades 테이블에 return_rate(레버리지 미포함 일반 수익률) 컬럼 추가
-- =========================================================================
-- 기존 roe 컬럼(레버리지 반영 ROE)은 그대로 두고, 컬럼 하나만 추가합니다.
-- ADD COLUMN IF NOT EXISTS만 사용 — 기존 데이터/테이블 전혀 안 건드림,
-- 재실행 안전.
-- =========================================================================

alter table public.trades add column if not exists return_rate numeric;
