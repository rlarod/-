diff --git a/style.css b/style.css
index 267c053..1bf19ce 100644
--- a/style.css
+++ b/style.css
@@ -136,7 +136,11 @@ a:hover{text-decoration:underline;}
      로고 블록 높이 37px 기준으로 헤더 63px, 좌측 8px, 간격 48px가 됩니다. */
   /* 로고를 키우면서(110px) 위아래 여백을 줄여 헤더가 과하게 두꺼워지지
      않게 했습니다. 실측: 여백 30px -> 14px, 헤더 170px -> 138px. */
-  max-width:var(--content-max);margin:0 auto;padding:14px 8px;
+  /* 2026-08-21 바이낸스 기준 정렬 — 바이낸스 선물 헤더 실측 64px.
+     로고 높이 110px은 대표 지시(2026-08-18 "68px는 너무 작아 안 보임")라
+     그대로 두고, 여백만 14px -> 8px 로 줄입니다. 헤더 138px -> 126px.
+     (테스트 하한 120px 유지) */
+  max-width:var(--content-max);margin:0 auto;padding:8px 8px;
   display:flex;align-items:center;gap:48px;flex-wrap:nowrap;min-width:0;
 }
 .top-banner-right{display:flex;align-items:center;gap:14px;flex-wrap:nowrap;margin-left:auto;flex:0 0 auto;}
@@ -161,7 +165,11 @@ a:hover{text-decoration:underline;}
      글자높이/메뉴높이 = 0.269 -> font-size 14px, item 좌우 패딩 = 콘텐츠 폭의 1.17% -> 22px */
   flex:0 0 auto;white-space:nowrap;
   background:transparent;border:none;color:rgba(255,255,255,0.86);
-  padding:20px 22px;border-radius:0;font-family:var(--sans);font-size:26px;font-weight:600;
+  /* 2026-08-21 바이낸스 기준 — 바이낸스는 헤더 하나(64px)에 메뉴가 들어 있어
+     메뉴 전용 줄이 없습니다. 우리는 메뉴 줄을 지우지 않는 대신 위아래 여백만
+     20px -> 5px 로 줄여 77px -> 47px 로 낮춥니다.
+     글자 26px·좌우 22px 은 테스트가 강제하는 값이라 건드리지 않습니다. */
+  padding:5px 22px;border-radius:0;font-family:var(--sans);font-size:26px;font-weight:600;
   cursor:pointer;transition:.12s;letter-spacing:-0.02em;line-height:1.4;
 }
 /* 1366px 이하(노트북 기본 해상도)에서도 9개가 한 줄에 다 들어가도록 좌우 여백만 축소 */
@@ -359,7 +367,7 @@ html[data-theme="dark"] .brand-logo.brand-logo-dark{display:block;}
 }
 .interval-btn{
   background:var(--surface);border:1px solid var(--border-soft);color:var(--text-dim);
-  padding:6px 12px;border-radius:3px;font-family:var(--mono);font-size:20.5px;font-weight:600;
+  padding:5px 10px;border-radius:3px;font-family:var(--mono);font-size:16px;font-weight:600;
   cursor:pointer;transition:.12s;
 }
 .interval-btn:hover{border-color:var(--text-faint);color:var(--text);}
@@ -659,7 +667,7 @@ tr.empty td{font-family:var(--sans);text-align:center;color:var(--text-faint);pa
    항목은 하나도 지우지 않았고 배치만 위아래로 바꿨습니다. */
 .ob-header{
   display:flex;flex-direction:column;align-items:stretch;gap:5px;
-  margin-bottom:8px;font-size:20.5px;color:var(--text-dim);min-width:0;
+  margin-bottom:8px;font-size:17px;color:var(--text-dim);min-width:0;
 }
 .ob-title{
   font-weight:600;color:var(--text);min-width:0;
@@ -704,7 +712,7 @@ tr.empty td{font-family:var(--sans);text-align:center;color:var(--text-faint);pa
 
 .ob-row{
   position:relative;display:grid;grid-template-columns:1fr 1fr 1fr;gap:0;
-  padding:5px 4px;font-family:var(--mono);font-size:18.5px;line-height:1.35;overflow:hidden;border-radius:3px;
+  padding:2px 4px;font-family:var(--mono);font-size:18.5px;line-height:1.35;overflow:hidden;border-radius:3px;
   cursor:pointer;
 }
 .ob-depth-bar{
@@ -741,7 +749,7 @@ tr.empty td{font-family:var(--sans);text-align:center;color:var(--text-faint);pa
 }
 @media (max-width:600px){
   .orderbook-panel{padding:10px 12px;}
-  .ob-row{font-size:20px;padding:3px 2px;}
+  .ob-row{font-size:20px;padding:1px 2px;}
   .ob-current-price{font-size:22px;}
 }
 
@@ -753,7 +761,7 @@ tr.empty td{font-family:var(--sans);text-align:center;color:var(--text-faint);pa
 .tabs{display:flex;gap:4px;border-bottom:1px solid var(--border);margin-bottom:16px;flex-wrap:wrap;}
 .tab-btn{
   background:transparent;border:none;color:var(--text-dim);font-family:var(--sans);
-  font-size:22px;font-weight:600;padding:10px 14px;cursor:pointer;border-bottom:2px solid transparent;
+  font-size:18px;font-weight:600;padding:7px 12px;cursor:pointer;border-bottom:2px solid transparent;
 }
 .tab-btn:hover{color:var(--text);}
 .tab-btn.active{color:var(--gold);border-bottom-color:var(--gold);}
@@ -1851,7 +1859,15 @@ tr.empty td{font-family:var(--sans);text-align:center;color:var(--text-faint);pa
 .page-left .app{padding-top:5px;padding-bottom:10px;}
 
 /* 공지/게시판은 이제 2박스 */
-.page-left .notice-board-wrap{grid-template-columns:minmax(0,1fr) minmax(0,1fr);}
+/* ⭐ 2026-08-21 바이낸스 기준 재배치 — 공지 박스를 거래 화면 "아래"로 내립니다.
+   근거(실측): 바이낸스 선물 1440 은 헤더(64px) 다음이 바로 거래 화면(y66)입니다.
+   우리는 공지 201px + 배너 134px = 335px 이 거래 화면 앞에 있어 y736 에서 시작했습니다.
+   index.html 은 수리팀이 쓰는 중이라 마크업을 못 건드리므로, .page-left 가
+   flex 칼럼인 점을 이용해 order 로만 자리를 옮깁니다(마크업·기능 그대로).
+   .page-left 의 자식은 [.notice-board-wrap, .top-ad-banner, .app] 셋뿐이고
+   .app 이 order 기본값 0 이라 거래 화면이 먼저 오고 공지·배너가 뒤로 갑니다.
+   되돌리려면 아래 order 두 줄만 지우면 됩니다. */
+.page-left .notice-board-wrap{grid-template-columns:minmax(0,1fr) minmax(0,1fr);order:2;}
 
 /* 우측 칼럼 — 내 정보 아래로 채팅이 화면 끝까지 */
 .page-right .user-panel-box{flex:0 0 auto;}
@@ -2305,10 +2321,10 @@ tr.empty td{font-family:var(--sans);text-align:center;color:var(--text-faint);pa
 /* 좌우 2단이 풀리는 1799px 이하에서는 표가 좁아져 값이 잘립니다.
    그 구간만 글자를 한 단계 줄입니다(1800px 이상은 위 clamp 값 그대로). */
 @media (max-width:1799px){
-  .position-table th{font-size:13px;}
-  .position-table td{font-size:13.5px;}
-  .position-table .position-symbol-name{font-size:14px;}
-  .position-table .position-symbol-sub{font-size:11.5px;}
+  .position-table th{font-size:15px;}
+  .position-table td{font-size:16px;}
+  .position-table .position-symbol-name{font-size:16.5px;}
+  .position-table .position-symbol-sub{font-size:13px;}
   /* 그래도 좁으면 칸을 줄이지 말고 표 안에서 가로로 스크롤합니다
      (값이 잘려 안 보이는 것보다 낫습니다). */
   .position-card > .table-scroll{overflow-x:auto;}
@@ -2320,7 +2336,7 @@ tr.empty td{font-family:var(--sans);text-align:center;color:var(--text-faint);pa
 .position-table th:last-child,
 .position-table td:last-child{min-width:150px;}
 @media (max-width:1799px){
-  .position-card .table-scroll > .position-table{min-width:1560px;}
+  .position-card .table-scroll > .position-table{min-width:1850px;}
 }
 
 /* 1800~2200px은 좌측 칼럼이 가장 좁은 구간 — 표 글자를 한 단계 줄여
