# 야간 진행상태 — 이어받기용

> **회차를 시작하면 이 파일부터 읽는다. 끝낼 때 갱신한다.**
> 사용량이 차서 멈추면 5시간 뒤 이 파일로 이어받는다. **1회차부터 다시 시작하지 않는다.**
> 규칙은 `docs/야간모드.md` 참조.

**마지막 갱신** 2026-08-24 18:12
**목표 종료** 2026-08-25 09:30 (대표에게 보고)

---

## 지금 어디까지 왔나

### ✅ 끝난 것 (배포까지 완료)

| 커밋 | 무엇 |
|---|---|
| `739b349` | 회원 전원 삭제 SQL 봉인 (P1) |
| `150a8a4` | 모바일 3단계 — 주문 패널 하단 시트 |
| `4bc7d2b` | TL 실시간 지급 (거래마다 즉시) |
| — | 옛 TL 계산식 되살아남 차단 |
| — | 점검 1회차 P2 3건 (폰 주문버튼 / 시세바 / 표 힌트) |
| — | 통화 버튼 칸 반반 (대표 지시) |
| — | 백로그·승인대기 현행화 |
| — | **커뮤니티 계획** `docs/커뮤니티-계획.md` |

### 🔄 돌고 있는 것

| 팀 | 무엇 | 브라우저 |
|---|---|---|
| 디자인팀 | P3 4건 + 1440 통화버튼 폭 상한 + cloud-history 힌트 | **사용 중** |
| 수리팀 | `schema-tl-balance-fix.sql` 봉인 + 대표용 README | 사용 안 함 |

**⚠️ agent-browser 는 한 세션이다. 브라우저 쓰는 팀은 한 번에 하나만.**

### ⏭ 다음에 할 것 (순서대로)

1. **돌고 있는 두 팀 게이트 2 → 배포** (해시 12/12 · npm test · 직접 열어보기 · 라이브 확인)
2. **커뮤니티 1단계** — 글 목록에 계급장
   `js/rank-badge-attach.js:140` targets 에 `"board-list-body"` 추가
   ※ **TL-012(댓글 배지 중복)를 먼저 고쳐야 한다.** 안 그러면 글 목록에도 두 개 붙는다
3. **커뮤니티 3단계** — 인기글 켜기 (`index.html:661` `display:none`)
   `js/board.js` 는 수정 금지 파일 → 별도 모듈로 우회
4. **점검 2회차** — 오늘 7건 배포했으니 회귀 확인 (여섯 폭)
5. **이더리움 실현 가능성 조사** — 조사만. 만들지 않는다
6. **운영기록 2026-08-24 마무리 + 2026-08-25 작성**

### ⏸ 대표 없이는 못 하는 것 (전부 패스)

| 항목 | 왜 |
|---|---|
| SQL 4개 실행 | 서버는 대표만 |
| 김갱님 손익 0원 복구 | **손익 계산식은 미리 확인받는 예외 항목** |
| 로그인 화면 확인 | 비밀번호 입력 금지 |
| 커뮤니티 2단계(거래→글 자동등록) | 손실 거래도 글로 낼지 대표 판단 필요 |
| 주식 시세 | 진짜 돈 가능성 |

---

## 회차 기록

### 1회차 — 2026-08-24 18:12 시작

- 야간모드 헌장 작성 (`docs/야간모드.md`)
- 국내 커뮤니티 직접 조사 → `docs/커뮤니티-계획.md`
  (디시 개념글 / 에펨 포텐 / 네이버 종토방 / 커뮤니티 활성화 자료)
- 5시간 자동 이어받기 설정
- 팀 2개 가동 중

---

## 매 회차 시작할 때 확인

```bash
# 1. 로컬 서버 살아있나 (08-24 하루에 7번 죽었다)
timeout 10 curl -s -o /dev/null -w "%{http_code}" http://localhost:3000

# 2. 수정 금지 파일 12개
md5sum -c /tmp/baseline.md5

# 3. 안 올라간 커밋 있나
git status --short && git log --oneline origin/main..HEAD
```
