# 운영기록 2026-08-21

## 발견
- 총 건수: 3
- 등급별: P1 1 / P2 1 / P3 1

내역
- P1 시세 바에서 마크가격·펀딩비가 화면 밖으로 밀려 안 보임 (1440/390) — 수정 완료, 게이트 2 반려
- P2 호가창 헤더가 통화를 따라가지 않음 (KRW 모드인데 `가격(USDT)`) — 미착수
- P3 3D 병사 모델 미표시 (three.js 로딩 방식 충돌) — 미착수, 경고로만 처리되어 기능 영향 없음

## 게이트 1 (발견 → 배정)
- 통과: 3
- 반려: 0
- 반려 사유: 없음

## 게이트 2 (완료 → 승인)
- 통과: 0
- 반려: 1
- 반려 사유:
  - 시세 바 수정 — 375px / 360px 에서 마크가격이 스크롤 힌트에 가려짐(각 11px / 26px).
    원인: 힌트 상쇄값을 `gap:16px` 전제로 계산했으나 실제 gap 은 구간별로 20px(≤700) / 10px(≤400).
    부수 문제: 테스트가 CSS 의 첫 gap 값(16px)만 읽어 잘못된 전제를 검증 중이라 이 결함을 못 잡음.
- 3회 반려로 대표 보고한 건: 없음 (1회차)

## 팀별 처리
- 점검팀: 0
- 수리팀: 0 (하루 3건 제한)
- 디자인팀: 0
- 비고: 조직 구성 이전에 본부장이 직접 수정한 건이라 팀 배정 이력 없음.
  게이트 2 반려분은 수리팀에 배정 예정.

## 대표 승인 요청
- 커밋 여부 — 게이트 2 반려로 **보류**. 수정 후 재검증 필요
- `.gitattributes` 추가 — 승인받아 적용 완료 (커밋 대기)
- 빈 폴더 `Desktop\모투` 삭제 — 승인받았으나 **옛 세션 프로세스가 점유 중이라 미완료**
- 폰에서 펀딩비 라벨 줄이기 (390 이하에서 여전히 안 보임) — 미결정

## 확인 못 한 것
- 로그인 후 화면 (보유 TL·로그아웃 버튼·채팅 금액 잘림) — 비로그인 상태라 확인 불가.
  대표 캡처 또는 대표가 직접 로그인한 창이 필요함
- 서버 랭킹 로직의 실제 버전 — `랭킹-버전확인.sql` 결과 미수령
- `docs/조직도.md` · `docs/점검팀.md` 원문 — 파일이 이 PC 에 없음
- 세로 방향 잘림 — 이번 점검은 가로만 봄

## 수정 금지 파일 해시
- 결과: 12/12 일치
- 불일치 시 파일명: 없음
