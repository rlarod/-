# 운영기록 2026-09-03 (목) — 야간 운영

> 대표 지시 (2026-09-02 퇴근 직전):
> **"나이제 퇴근할건데 지금부터 트레이딩뷰랑 똑같이 만들어놔 아침까지 쉬지말고
> 내가 어차피 돈 드는거 없으니까 돈드는 거 빼고는 PM이 알아서 결정해서 진행해"**
> **"밤새 쉬지마 절대 완벽하게 트레이딩뷰 처럼 만들어놔"**
> **"트레이딩뷰 직접 들어가서 하나하나 클릭해보면서 다른 점 다 찾고 지표들도 싹다 추가시키고"**
> **"너무 많이 켜두면 또 느려져서 꺼지고 그러더라 그거 방지해야돼"**
> **"과부하 걸리지 않게 항상 15분마다 같이 측정해서 불필요한거 바로바로 꺼"**

## 발견
- 총 건수: (진행 중)
- 등급별: P1 0 / P2 1 / P3 2

## 게이트 1 (발견 → 배정)
- 통과: 3
- 반려: 0

## 게이트 2 (완료 → 승인)
- 통과: 1 (`c2322ff`)
- 반려: 0
- 3회 반려로 대표 보고한 건: 없음

## 팀별 처리
- 점검팀: 0
- 조사팀: 1
- 수리팀: 진행 중
- 디자인팀: 0
- 기록팀: 1
- 차트팀: 2 (+2 진행 중)
- 감사팀: 0

## 대표 승인 요청
- 없음. **돈 드는 것이 하나도 없었습니다.**

## 확인 못 한 것
- **로그인 후 화면 전부** — 11일째. PM 은 로그인하지 않습니다
- **알람 소리·브라우저 알림 팝업** — 자동화 브라우저는 소리를 못 듣습니다. 코드 경로만 확인
- **트레이딩뷰 앱 안의 설정 창** — 지표 추가에 회원가입 벽. 로그인하지 않았습니다.
  트레이딩뷰 값은 **공식 Pine 참고서 원문**을 직접 열어 읽은 것입니다
- **탭 두 개에서 알람 동시 울림** — 로직은 넣었으나 탭을 실제로 두 개 열어보진 않음

## 수정 금지 파일 해시
- 결과: 12/12 일치
- 불일치 시 파일명: 없음

---

# 🖥 부하 관리 — 대표가 말한 "느려져서 꺼지는" 원인을 찾았습니다

**대표 지적: "너무 많이 켜두면 또 느려져서 꺼지고 그러더라"**

## 원인은 팀 수가 아니라 ★안 닫힌 세션★ 이었습니다

```
17:20 실측   크롬 35개 · 4.05GB · CPU 77%
             죽은 agent-browser 세션 ★6개★ (chart14 · chart8 · ct14b~e)
             전부 낮에 끝난 작업 것

하나씩 닫은 뒤  크롬 12개 · 1.72GB · CPU ★28%★
```

**팀을 많이 켜서가 아니라, 끝난 팀이 안 닫혀서 쌓인 것이었습니다.**

## 자동 청소기를 만들었습니다

`scratchpad/health.sh` — **15분마다 루프가 맨 먼저 돌립니다.**

```
1  지금 도는 팀 이름을 인자로 받는다
2  ★인자에 없는 세션은 전부 자동으로 닫는다★ (이름을 하나씩 지정)
3  CPU · 크롬 개수 · 메모리 · 로컬 서버를 잰다
4  GO(<65%) 팀 더 붙임 / SLOW(65~85%) 새로 안 붙임 / STOP(>=85%) 배정 금지
```

⚠️ **`close --all` 은 이 스크립트도 안 씁니다.** 그걸 쓰면 일하는 팀 세션까지 끊깁니다.

**실측 추이** — 17:20 CPU 77% → 17:31 44% → 17:50 23% → 18:01 16%.
**브라우저가 죽은 사고 0건.**

---

# 배포 1건 — `c2322ff`

## 그리기 도구 3개 완성 — 도구 막대에 "준비중" 이 0개가 됐습니다

```
파동   12345(충격) · ABC(조정) 두 묶음. 꼭짓점 이름표 자동 배치
표정   선으로 긋는 6종. 고르는 창의 작은 그림과 차트에 그리는 함수가 ★같은 함수★
알람   가격 도달 알림. 화면 알림줄 · 소리 · 브라우저 알림
```

### 알람에 그은 선 (PM 결정)

대표가 **"돈 드는 것 빼고는 PM이 알아서"** 라고 했고 알람은 돈이 안 듭니다. **제가 열었습니다.**

```
✅  가격 선 · 화면 알림 · 소리 · 브라우저 알림      전부 무료
❌  주문 · 자동매매 · 유료 발송 서비스(문자·이메일)  ★코드와 테스트로 막았습니다★
```

소리는 파일을 안 받고 브라우저가 만듭니다 — **용량 0 · 돈 0.**

**팀이 조용한 고장을 두 개 미리 막았습니다:**
- 알람 칩에 **"창을 닫으면 못 알립니다"** 상시 표시. 안 적으면 회원이
  "안 울렸다 = 가격이 안 닿았다" 로 읽습니다
- **"그림 전체 지우기" 가 알람은 안 지웁니다.** 걸어둔 알람이 소리 없이 사라지면 같은 오해

### 지표 3개 + 색 12 → 20

```
OBV   아래 칸        SAR   주 차트에 ★점★      VWAP  주 차트, UTC 하루마다 재시작
```

**착수 전 관문 통과** — 거래량이 0으로 올 수 있다는 앞 팀 경고 때문에 **봉 1000개를 먼저 셌습니다.
0인 봉 0개.** 앞으로 0이 되면 그리지 않고 콘솔에 한 번 알립니다.

**색은 사람 눈 기준(ΔE2000)으로 전수 계산해서 골랐습니다.**

```
기존 12색 중 제일 가까운 쌍 = #BA6EED / #E637E6 (ΔE2000 9.71)
→ ★다음에 터질 자리★ 였습니다. 12색 시절엔 이 계산이 아예 없었습니다
초록·빨강 구간은 계산보다 더 넓게 피했습니다(hue 24~62 · 195~320)
계산만 믿으면 형광 연두 98도 · 주홍 18도가 통과하는데 눈에는 초록·빨강입니다
```

## PM 이 직접 잰 것

| | 값 |
|---|---|
| 회원 경로로 9개 전부 얹음 | 인스턴스 11 · 선 14 · **색 겹침 0** |
| 칸 / 기준선 | 6칸 / 5줄 → 끄면 1칸 / 0줄 |
| 360 도구 막대 | 18개 · **준비중 0** · 가로 넘침 0 |
| 360 알람 칩 | 310px (화면 여유 50px) |
| 360 파동 칩 | 294px (여유 66px) |
| 콘솔 오류 | 0건 |

## ⚠️ PM 의 측정이 또 틀렸습니다 (오늘 두 번째)

"색 겹침 1개" 로 나와 팀 보고를 의심했는데, **회원이 안 쓰는 낮은 단계 함수(`addInstance`)를
부른 탓**이었습니다. 회원이 실제 누르는 길(`suggestColor` → `createInstance` → `updateInstance`)로
다시 재니 0이었습니다.

**같은 실수를 오늘 두 번 했습니다** (앞서 "칸이 안 생긴다" 도 같은 원인).
**앞으로는 처음부터 화면 버튼을 누르는 쪽으로 잽니다.**

## npm test 처리 — 커밋될 상태만 따로 떠서 확인

작업트리에 다른 팀 미완성 파일이 있어 `html-assets-tracked` 하나가 빨갰습니다.
**`git worktree` 로 커밋될 상태만 따로 떠서 그 검사를 돌렸습니다 — 14/0 통과.**
남의 파일을 잠깐 빼는 방식은 팀이 그 파일을 만지는 중이라 안 썼습니다.

---

# 🟠 [P2] 색 겹침이 아직 남아 있었습니다 — 기록팀 발견

낮에 고친 것은 **"새로 얹는 줄 안에서만"** 이었습니다.

```
제일 짧은 재현   ATR → SAR → CCI → KDJ   ★네 번 누르면 납니다★
실측             무작위 순서 600가지 중 ★같은 칸 겹침 131★ · 어디서든 겹침 214
원인             선이 여럿인 지표(KDJ 3선 · StochRSI 2선)를 ★나중에★ 얹으면
                 2·3번째 선이 정의 기본색 그대로라 이미 쓰인 색을 집습니다
```

**오류도 경고도 안 납니다.** 8월 31일 "시세선과 MA7 이 한 줄로 보였다" 와 같은 종류이고,
**오늘만 이 계열이 세 번째**입니다.

## ⭐ 기록팀의 봉인 방식이 좋았습니다

**"아직 겹친다" 를 ★기록으로★ 남겼습니다. 고치면 그 줄이 빨개지면서 알려줍니다.**

실제로 그렇게 됐습니다 — 지표팀이 고치자 그 절이 빨개지며
**"이제 어느 순서로 얹어도 안 겹칩니다. 이 절을 '겹치면 실패' 로 뒤집으세요"** 라고 떴습니다.

**아무도 안 물어봐도 고쳐진 것이 자동으로 드러납니다.**

---

# 조사팀 — 트레이딩뷰 격차를 실제로 셌습니다

**지금까지 PM 이 "다음엔 이거" 를 머릿속으로 짐작해 배정했습니다. 그게 게이트 1 위반입니다.**

```
             우리        트레이딩뷰 (앱 실측)
지표         11종        127개
봉 종류      4개         21개
시간 단위    9개         24개
그리기       18개        8그룹 + 낱개 7
```

## ⭐ 개수보다 큰 것이 하나 나왔습니다 — [A] 최우선

```
fx 목록 9줄 중 ★설정 버튼이 붙은 건 2줄★
MA(7) · MA(25) · MA(99) · 볼린저 · RSI · MACD 는
회원이 기간도 색도 ★하나도 못 바꿉니다★ — 코드에 박혀 있습니다
```

**지표를 100개 더 만드는 것보다 이게 먼저입니다.** 회원이 매일 보는 6개입니다.
다만 큰 공사이고 지금 두 팀이 잡은 파일과 얽혀 **밤새 배정하지 않았습니다.**

## 등급을 솔직하게 나눴습니다

```
[C] 로 뺀 것   스크리너 · Pine 스크립트 · 소셜 · 브로커 연동 · 캘린더
               Renko/Kagi/P&F (시간축이 아니라 우리 엔진 전제와 안 맞음)
               레이아웃 서버 저장 (계정별 저장이 전제)
```

**"트레이딩뷰에 있으니 다 만들어야 한다" 가 아닙니다. 우리는 모의 선물 거래 사이트입니다.**

## 조사팀이 확인 못 한 것

- 트레이딩뷰 시간 단위 개수 — 메뉴가 안 닫혀서 못 열었습니다 (2분 규칙으로 중단)
  → **PM 이 직접 들어가 24개를 실측해 채웠습니다**
- 그리기 그룹 8개 안의 개별 도구 개수 — 같은 이유

---

# 기록팀 — 봉인 3건

```
색 겹침 43검사   기준선 73검사   계산 166검사   (+ 도우미 _kit-harness.js)
```

**일부러 19가지를 틀리게 만든 사본으로 돌려 전부 잡히는지 확인**했습니다.
예 — 기준선 지우는 코드를 `removeSeries` 뒤로 한 줄 옮기면 19건이 빨개집니다.

⚠️ **원본 파일은 한 번도 안 건드렸습니다.** 차트팀이 동시에 만지는 중이라 **사본으로 실험**했습니다.

**기본 인스턴스 2개 봉인** — PM 판단(그대로 두는 게 맞다)과 같은 결론.
다만 **세 군데인 줄 알았던 게 실은 네 군데**였고, 한 곳(`기본인스턴스수 = 2`)으로 모았습니다.

---

# 밤새 배포 (이어서)

```
08d183d  봉 종류 4→10(하이킨아시) + 그림 끌어옮기기·되돌리기·그림별 색
40f9eaa  지표 4개(스토캐스틱·ADX/DMI·슈퍼트렌드·일목균형표) + 색 겹침 완전 해결
1de1d0b  차트 하단 3건 — 기간 탭(1D~ALL) · 날짜로 가기 · 시간대
```

## 트레이딩뷰 대비 (저녁 시작 → 지금)

| | 저녁 | 지금 | 트레이딩뷰 |
|---|---|---|---|
| 지표 정의 | 8 | **13** | 127 |
| 봉 종류 | 4 | **10** | 21 |
| 시간 단위 | 9 | **16** | 24 |
| 그리기 도구 | 15 (준비중 3) | **18 (준비중 0)** | 92 |
| 가격축 | 선형만 | **로그·퍼센트** | 4종 |
| 십자선 봉 값 | 없음 | **있음** | 있음 |
| 그림 끌어 옮기기 | 없음 | **있음** | 있음 |
| 되돌리기 Ctrl+Z | 없음 | **있음** | 있음 |
| 기간 탭 | 없음 | **9개** | 9개 |
| 시간대 | 없음 | **있음** | 있음 |

---

# 🔴 [P1] 다른 종목을 보는 동안 알람이 죽었습니다 — 점검팀 발견

**PM 이 직접 재현했습니다.**

```
BTC 에 알람을 걸고 나스닥으로 옮기면
  getAlertCount() = ★0★          저장칸에는 BTC 알람이 그대로
그 사이 BTC 가 알람가를 지나도 안 울리고, 돌아와도 ★영영★ 안 울립니다
```

**점검팀이 바이낸스 실제 봉으로 증명했습니다** — 10:22 에 76,649.40 알람을 걸고 나스닥으로
이동, 그 사이 실제 BTC 는 76,660 → 76,616 으로 내려가 **분명히 지났는데** 10:24 복귀 후에도
`done:false`. 같은 시각에 건 다른 알람은 정상 발화.

**왜 P1 인가** — 화면에 오류가 하나도 안 납니다. 회원은 **"안 울렸다 = 가격이 안 닿았다"** 로
읽습니다. 칩의 상시 문구는 "창을 닫으면 못 알립니다" 뿐이고 **다른 종목을 보는 중에도
못 알린다는 안내가 화면 어디에도 없었습니다.**

## 고침 — 놓친 교차 되찾기

종목을 옮겼다 돌아오면(창을 닫았다 열어도) **그 사이 봉의 고가·저가를 훑어** 알람가를
지났는지 봅니다. 지났으면 **늦은 알림**으로 구분해 알립니다.

```
알림줄    늦은 알림 — 비트코인 76,343.00 를 09-02 19:50 에 지났습니다 (자리를 비운 사이)
이름표    알람 울림(늦음)     ← 지금 울린 것은 "알람 울림"
소리      587Hz 세 번          ← 지금 울린 것은 880Hz 두 번
```

**일부러 놓치는 것** — 자리를 뜬 그 순간에 걸쳐 있던 봉은 안 봅니다. 세면 **알람을 걸기 전에
이미 지나간 값**을 지금 지난 것처럼 알립니다. **헛알람보다 봉 하나 못 잡는 쪽**을 골랐습니다.

**헛알람 없음도 쟀습니다** — 최근 30분 고가+3,000 / 저가−3,000 두 개로 같은 절차를 돌려
25개 봉을 훑고 **둘 다 안 울림, 알림줄 0건.**

## ⚠️ 트레이딩뷰는 서버에서 알람을 돌립니다

차트팀이 공개 문서에서 찾았습니다 — *"Alerts run on powerful servers"*.
**그래서 종목을 옮기든 창을 닫든 제때 울립니다.**

```
우리       서버·푸시 설비가 없습니다
가능한 것  "돌아올 때 되찾기" ← 설비 없이 갈 수 있는 가장 가까운 자리
완전히 하려면  ㉠ 종목 4개 시세 동시 수신 (회선·부하)
              ㉡ 서버 감시 + 푸시 ← ★돈이 듭니다★. 아무것도 알아보지 않았습니다
```

**㉡ 는 대표 결재 항목이라 차트팀이 가입·체험 포함 아무것도 안 했습니다.** 옳은 판단입니다.

---

# ⭐ 팀이 봉인을 스스로 더 세게 만든 사례 4건

**오늘 밤 팀들이 "내 변경이 옛 검사를 조용히 통과해 버린다" 를 스스로 찾아 고쳤습니다.**

```
① 바깥으로 나가는 길   fetch( 를 ★개수만★ 셌습니다
                       App.Api.fetchKlines( 안에는 fetch( 가 없어 ★안 걸리고 지나갔습니다★
                       → 이름까지 적어 대조 + POST·credentials·Authorization 도 금지

② 화면 안내           주석에만 있어도 통과했습니다
                       실제로 문구를 바꾸자 화면에서는 사라졌는데 검사는 초록이었습니다
                       → 진짜 코드에서 두 사실을 다 적는지 확인

③ container 이벤트     ★개수★ 를 셌습니다 → ★이름까지★ 대조
                       톡 처리하는 쪽에 입구가 생기면 그 자리에서 깨집니다

④ 그리기 색           지표 20색과 겹치는지 ★매번 다시 잽니다★
                       지표팀이 가까운 색을 넣으면 여기가 깨집니다 (두 팀이 서로 감시)
```

**기준을 낮춘 곳은 한 곳도 없습니다.**

---

# 🟠 [P2] fx 지표 패널이 화면 밖으로 잘림 — 조사팀이 원인 확정

**PM 과 점검팀의 측정이 달라 배정하지 않고 조사팀에 넘겼습니다.**

## 결론 — 둘 다 정확히 쟀고, ★스크롤 위치★ 가 달랐습니다

```
390x844   scrollY 0    패널 569px · 넘침 ★-267★ · 추가버튼 보임   ← PM 값
          scrollY 560  패널 943px · 넘침 ★+318★ · 추가버튼 안 보임 ← 점검팀 값
```

**1px 차이로 갈립니다** — y=560 에서 아래 여유 544, 열 때 높이 543.

## 원인 (확신도 확실)

`js/chart-indicator-menu.js:552 open()` 이 **기본 7줄만 있는 상태에서 자리를 확정**하고,
나머지 줄과 "+ 지표 추가" 는 **그 뒤 관측자를 통해 들어옵니다.** `place()` 를 다시 부르는
코드가 없어 패널이 **화면에 고정된 채 아래로만 자랍니다.**

**결정적 증명** — 그 상태에서 창 크기 신호 하나만 던져 다시 재게 하니
`943px / 넘침 +140 / 안쪽스크롤 0` → `631px / 넘침 −260 / 안쪽스크롤 253`.
**계산 로직은 멀쩡하고 한 번 더 안 불릴 뿐입니다.**

## PM 측정이 왜 달랐나

제가 잰 `407px · 추가버튼 DOM 에 없음` 은 **"열자마자 같은 순간에 잰 값"** 이었습니다.
**추가버튼이 없었다는 것 자체가 "아직 안 들어왔다" 는 증거**였습니다.

**오늘만 제 측정이 세 번 틀렸습니다** — ① 칸이 안 생긴다 ② 색 겹침 1개 ③ 이번.
**셋 다 회원이 안 쓰는 낮은 단계로 재거나 너무 이른 순간에 잰 것이었습니다.**

---

# 🟠 [P2] 360 에서 지표 칩 줄이 캔들을 덮음

```
                    칩 줄     주 차트   가림
기본 9칩            76px      330px    23.0%
14칩                129px     182px    ★70.6%★
14칩 ★전부 끔★      129px     530px    24.2%   ← ★안 줄어듭니다★
21칩(13종 다 추가)  235px     182px    ★128.8%★  칩 줄이 차트보다 큽니다
1440 · 14칩         49px      311px    15.8%
```

**차트 위에 겹쳐 그립니다. 차트를 밀어내지 않습니다.**

⚠️ **조사팀이 점검팀의 "182px · 84.2%" 를 재현 못 했고, 그걸 그대로 적었습니다** —
"줄 수 세기나 인스턴스 수 둘 중 하나가 어긋났습니다." **넘겨짚지 않았습니다.**

## 트레이딩뷰에 있고 우리에 없는 것

```
트레이딩뷰도 범례를 차트 위에 겹쳐 그립니다 (우리와 같음)
★다만 범례 아래에 접기 버튼(꺾쇠 ^)이 있습니다★
```

**그게 이 문제에 대한 트레이딩뷰의 답입니다.** 수리팀에 배정했습니다.

---

# PM 결정 (대표 지시 "돈 드는 거 빼고는 PM이 알아서")

| 무엇 | 결정 | 근거 |
|---|---|---|
| 알람 기능 열기 | **염** | 원래 대표 결재 대기였으나 돈이 안 듭니다. 주문·자동매매·유료 발송은 코드와 테스트로 막았습니다 |
| 알람 전용 색 신설 | **함** | 금색 네 겹(MA7·그린 선·미체결·알람)을 풀어야 했습니다 |
| 지표 색 12→20 | **함** | 2026-09-02 대표 승인 범위 |
| 색을 20에서 더 안 늘림 | **안 늘림** | 10개 더 뽑으면 가장 가까운 두 색이 9.71→6.61 로 내려앉아 눈에 거의 같아집니다 |
| CCI 기간·Source | **20 · hlc3** | 트레이딩뷰 기준 (바이낸스는 9) |
| ATR 넣기 | **넣음** | 바이낸스엔 없지만 차트는 트레이딩뷰 관할 |
| 하이킨아시 지표 계산 | **진짜 종가** | 트레이딩뷰는 HA 값으로 하지만 경계가 겹치면 거래 쪽 우선 |
| 시간대 기본값 | **내 컴퓨터 시간으로 변경 지시** | 차트 UTC / 거래내역 로컬로 9시간 어긋나 있었습니다. 체결·주문 시각은 거래라 거래 쪽 우선 |
| 봉 종류 봉인 수정 | **승인** | "바이낸스에 없어서 뺐다" 가 낡은 근거. 돈에 닿는 조항은 그대로 살아 있음을 확인 |

---

# ⚠️ 여기부터는 뒤늦게 채웠습니다 (조사팀 지적)

**`d1a5594` 이후 배포 8건이 기록에 한 줄도 없었습니다.** 조사팀이 커밋과 문서를 대조해
찾아냈습니다. **PM 이 배포에만 쫓겨 기록을 놓쳤습니다. 어제도 같은 지적을 받았고 두 번째입니다.**

## 밤새 배포 14건 (전부 라이브 반영 확인)

```
c2322ff  19:33  그리기 3개 완성(파동·표정·알람) + 지표 3개(OBV·SAR·VWAP) + 색 12→20
5f015e6  19:52  가격축 로그·퍼센트 + 시간 단위 9→16 + 십자선 봉 값(OHLC)
08d183d  20:02  봉 종류 4→10(하이킨아시) + 그림 끌어옮기기·되돌리기·그림별 색
40f9eaa  20:14  지표 4개(스토캐스틱·ADX/DMI·슈퍼트렌드·일목균형표) + 색 겹침 해결
1de1d0b  20:19  기간 탭(1D~ALL) · 날짜로 가기 · 시간대
d1a5594  20:21  🔴 P1 알람 되찾기
a4184c5  20:26  시간대 기본값 = 내 컴퓨터 시간 · 1440 차트 38px 회복 · 시간 단위 기억
b3099f4  20:47  🟠 P2 fx 패널 잘림 + 360 칩 줄이 캔들 87% 덮던 것
314babc  21:39  ★리플레이(봉 되감기)★ · 그린 것 목록 · 전체 잠금/숨김 · 계속 그리기
68d9149  22:22  봉인 5건 + ★아무것도 안 지키던 봉인★ 하나 적발
cab6db7  22:30  ★MA(7)·(25)·(99) 를 회원이 설정 가능하게★
c32e820  23:36  🟠 P2 기본값 버튼 금색 충돌 + ★볼린저★ 도 설정 가능하게
8eedf5b  00:08  리플레이 겹침 3건 (조작막대·안내줄·스크롤 복귀)
b9494d0  01:33  🟠 P2 현재가 선 오인식 + 그린 것 목록 화면 밖 넘침
```

---

# 조사팀 재측정 — 지금 어디까지 왔나 (라이브 `b9494d0` 기준)

| 항목 | 어젯밤 | 지금 | 트레이딩뷰 |
|---|---|---|---|
| 지표 종류 | 11 | **18** | 127 |
| ↳ **회원이 설정 가능** | **2 / 9줄** | **6 / 9줄** | 전부 |
| ↳ 아직 못 바꾸는 것 | 7줄 | **거래량 · RSI · MACD 3줄** | — |
| 봉 종류 | 4 | **10** | 21 |
| 시간 단위 | 9 | **16** | 24 |
| 그리기·도구 버튼 | 18 | **19** · **준비중 0개** | 8그룹 + 낱개 7 |
| 기간 탭 | 없음 | **9개** | 9개 |
| 날짜로 가기 · 시간대 | 없음 | **있음** | 있음 |
| 가격축 모드 | 1 | **3** | 3 |
| 십자선 봉 값 | 없음 | **있음** | 있음 |
| 리플레이 | 없음 | **있음** | 있음 |
| 그린 것 목록 · 잠금 · 숨김 | 없음 | **있음** | 있음 |
| 계속 그리기 | 없음 | **있음** | 있음 |
| 알람 | 없음 | **있음** (서버 없이) | 서버 기반 |

**차트 기능 11가지 중 10가지가 실제로 돌아갑니다** (알람만 로그인 벽 때문에 미확인).

## ⭐ 어젯밤 [A] 최우선이 3분의 2 풀렸습니다

```
"회원이 기간도 색도 하나도 못 바꾼다" 던 6줄 중 ★4줄★ 이 풀렸습니다
   MA(7) · MA(25) · MA(99) · 볼린저      ← 풀림
   RSI · MACD · 거래량                   ← 남음 (거래량은 일부러 안 옮김)
```

설정 창 구성이 **트레이딩뷰의 Inputs / Style 탭과 같습니다** —
계산 탭(기간 · 값 종류 7가지 · 앞뒤로 밀기) / 모양 탭(색 · 굵기 · 선 모양 3가지).

---

# 🟠 아직 라이브에 나가 있는 것 — ATR 이 원화 회원에게 USDT 숫자

조사팀이 팀의 작업 중 코드에서 찾았습니다.

```
ATR 은 ★가격 단위★ 인데 표시 통화를 안 따라갑니다
원화로 보는 회원 화면에 ★USDT 숫자★ 가 뜹니다
오류 0건 · 화면 멀쩡 · 회원은 그게 USDT 인 줄 모릅니다
```

**지금 차트팀이 고치는 중입니다** (틀에 "표시 통화 따라가기" 를 내는 작업 13.3).
MACD 도 가격 차이라 같은 문제가 날 자리였습니다 — **옮기기 전에 찾았습니다.**

---

# 남은 것 (대표 보고용)

## [A] 회원이 매일 쓸 것

```
· RSI · MACD · 거래량을 회원이 못 바꿈            중간
· ATR 이 원화 회원에게 USDT 숫자                  작음  ← 작업 중
· 바이낸스에 있는 8시간봉 · 3일봉이 없음          작음
· 롱/숏 포지션 그리기 도구                        중간  (모의 선물 사이트에 특히 맞음)
· 수직선 · 사각형 · 화살표                        중간  (가장 기본적인 그리기)
· 지표 더 늘리기 (Williams %R · MFI · 피봇 등)    하나당 작음
· ★그린 것이 그 브라우저에만 남음★               큼   (폰에서 그린 게 PC 에 없음)
```

**마지막 것만 서버 표가 필요해 대표 결재(SQL) 사항입니다. 나머지는 전부 수정 금지 파일을 안 건드립니다.**

## [C] 만들지 않는 것

```
스크리너 · Pine 스크립트 · 소셜 · 브로커 연동 · 경제 캘린더 · 뉴스
Renko · Kagi · P&F · Line Break   ← 시간축이 아니라 우리 거래 엔진 전제와 안 맞음
레이아웃 서버 저장
```

**"트레이딩뷰에 있으니 다 만든다" 가 아닙니다. 우리는 모의 선물 거래 사이트입니다.**

---

# 밤새 잡은 문제 18건

```
P1 1건   알람이 종목 옮기면 조용히 죽음
P2 6건   fx 패널 잘림 · 칩 줄 87% 가림 · 기본값 버튼 금색 충돌 ·
         현재가 선 오인식 · 목록 화면 밖 넘침 · 리플레이 조작막대가 버튼 막음
P3 3건   시간 단위 안 기억 · 안내줄이 칩 줄 덮음 · 스크롤 복귀 안 됨
그 외 8건 조용한 고장 계열
```

## ⭐ 그중 10건은 팀이 시키지 않았는데 찾았습니다

```
· 새 지표가 기존과 같은 색 (3종)          차트팀이 스스로
· 기준선이 조용히 버려짐                  차트팀이 스스로
· CCI 반올림 오차                         차트팀이 자기 코드를 의심해서
· 차트/거래내역 9시간 어긋남              수리팀이 배정 밖에서
· ★아무것도 안 지키던 봉인★              기록팀이
· ★리플레이 차단 테스트가 뚫려 있음★     기록팀이
· 리플레이 중 현재가 선                   수리팀이 배정 밖에서
· placeStyle 에도 같은 병                 수리팀이 배정 밖에서
· ATR 원화 문제                           조사팀이
· 운영기록 8건 누락                       조사팀이 (PM 잘못)
```

## ⭐ 팀이 PM 지시를 두 번 바로잡았습니다

```
① PM: "봉인에 굵기도 넣어라"
   팀: 8/31 사고는 금색 2px ↔ 금색 1px 였습니다. 굵기를 넣으면 ★그 사고가 통과합니다★
   → 팀 말이 맞았습니다. 색 + 선모양 두 가지로만 판정 (PM 지시보다 엄격)

② PM: "alert 쓰는 건 이 파일 하나뿐"
   실제: ★17개 파일 43번★ — PM 이 틀렸습니다
   → 리플레이팀은 오히려 우리 사이트의 기존 방식을 따른 것이었습니다
```

**시키는 대로만 하지 않고 확인해서 되돌려준 것이 맞았습니다.**

## ⚠️ PM 이 네 번 잘못 쟀습니다 — 전부 같은 종류

```
① "칸이 안 생긴다"     회원이 안 쓰는 낮은 단계 함수를 부름
② "색 겹침 1개"        같은 이유
③ "패널 407px"         ★열자마자 같은 순간★ 에 쟀음 (내용이 아직 안 들어옴)
④ "지표 9"             같은 이유
```

**앞으로는 처음부터 화면 버튼을 누르는 쪽으로 잽니다.**

---

# 🖥 부하 (대표 지시 — 15분마다 재고 바로 끄기)

```
17:20  CPU ★77%★  크롬 35개 · 4.05GB   ← 죽은 세션 6개가 쌓여 있었음
17:31  CPU 44%    (세션 정리 뒤)
19:00~ CPU 1~26%  (자동 청소기 가동 뒤 계속 낮음)
01:34  CPU ★1%★   크롬 10개 · 1.3GB
```

**대표가 말한 "느려져서 꺼지는" 원인은 팀 수가 아니라 ★안 닫힌 세션★ 이었습니다.**

`scratchpad/health.sh` 가 15분마다 **도는 팀 것이 아닌 세션을 자동으로 닫습니다.**
`close --all` 은 안 씁니다(일하는 팀까지 끊깁니다). 이름을 하나씩 지정합니다.

**브라우저가 죽은 사고 3번** — 전부 재시도로 복구, **작업 손실 0건.**

---

# 검사 수

```
어제 저녁  파일 168 · 검사 8,401
지금       파일 188 · 검사 ★9,827★   (+1,426)
```

---

# 밤 후반 배포 (aa7827b 이후 8건)

```
435026d  틀에 눈금 고정·칸 이름표·표시 통화 + ★ATR 이 원화 회원에게 USDT 숫자★
2012702  🟠 P2 화면 아래로 넘치던 창 3개 (색·굵기 / 표정 / 글자 입력) + 봉인 3건
d360eec  봉인 3건 (띄우는 창 전수 census · 가격/지수 판정 · 알람 보호) + ★헛검사 수정★
4182725  그리기 도구 3개 (수직선 · 사각형 · 화살표)
6f8d491  RSI(14) 를 지표 틀로
f622df4  MACD(12,26,9) 를 지표 틀로 + ★MACD 도 원화 회원에게 USDT 숫자★
bc5b291  🔴 P1 fx 지표 창이 폰에서 지표를 한 줄도 안 보여주던 것
3c9baec  짧은 화면에서 주문막대 밑에 깔리던 창 2개 (시간단위 더보기 · 시간대)
```

---

# 🔴 [P1] 표정 도구가 폰에서 처음부터 못 쓰는 상태였습니다

**어젯밤 PM 이 "준비중 0개" 라고 보고한 그 도구입니다.**

```
여섯 폭 ★전부★ 44px 넘침
360 에서 여섯 단추가 ★하나도 안 보임★
오류 없음 · 화면 멀쩡 → 회원은 "표정이 안 찍히네" 로만 느낍니다
```

**"버튼이 열렸다" 와 "실제로 쓸 수 있다" 는 다릅니다. PM 이 열린 것만 확인하고 넘어갔습니다.**

## 왜 여태 안 걸렸나

```
표정 창 · 글자 입력칸만 ★다른 방식★ 으로 화면에 붙습니다(position:absolute)
봉인은 ★한 가지 방식★(putFixed)을 쓰는 함수만 봤습니다

차트 칸은 560~989px 로 폰 화면보다 깁니다
→ "차트 칸 안에 있다" 와 "화면에 보인다" 가 ★전혀 다릅니다★
```

---

# 🔴 [P1] fx 지표 창이 폰에서 지표를 한 줄도 안 보여줬습니다

```
360 · 375 · 390 에서 특정 자리(y225~325)에 fx 를 열면
   창에 뜨는 것   "지표" · "주 차트" · "+ 지표 추가" · 안내줄
   지표 줄        ★0 / 9★
```

**등급을 PM 이 P2 → P1 으로 올렸습니다.** 회원은 스크롤을 옮겨 다시 열면 보인다는 걸
알 방법이 없습니다. 대표가 최근 체결 빈 화면을 P2→P1 으로 올리라 하신 선례를 따랐습니다.

## ⚠️ PM 진단은 방향만 맞았습니다

```
PM 이 짚은 것   "avail < 38 인데 실제 줄 높이가 48~50 이라 한 줄도 안 들어간다"
실제            ★38 은 발동조차 안 했습니다★ (avail 이 50 이라 38 보다 큼)

결정타          창이 ★버튼 한쪽 자리만★ 봐서 쓸 수 있는 711px 중 ★270px★ 만 썼습니다
                38 은 "최소 보장" 이 아니라 "넓힐지 말지" 를 가르는 값이었고 너무 작았습니다
```

**PM 진단대로 38 만 고쳤으면 안 고쳐졌을 것입니다.**

---

# 🟠 원화 회원에게 USDT 숫자가 뜨던 것 2건

```
ATR    41.85              →  ₩58,927
MACD   −11.90 −48.47 …    →  ₩−17,865 ₩−72,713 …
```

**둘 다 오류 없이 조용히 틀린 숫자를 보여주고 있었습니다.**
**옛 모듈은 가격축만 통화를 따르고 이름표는 안 따랐습니다** — 회원은 축과 이름표가
다른 단위인 줄 몰랐을 겁니다.

**MACD 는 옮기기 전에 기록팀이 "MACD 가 오는 순간 검사" 를 걸어둬서 놓칠 수 없었습니다.**

---

# 🟠 짧은 화면(360×640) — 시간대 창은 꺼낼 방법도 없었습니다

```
창이 주문막대를 +57px 넘어가는데 ★안쪽 스크롤이 아예 안 생겼습니다★

이유 — CSS 가 "화면 높이 − 20px" 로 최대 크기를 정해서
       창이 "다 들어갔다" 고 여겼습니다
       그런데 그 아래 72px 은 주문막대가 덮고 있었습니다
```

**배정은 "+10px" 이었는데 실제는 시간단위 더보기가 ★+70px · 7줄 중 3줄이 안 보임★,
800 에서 멀쩡하던 390 폭까지 깨져 있었습니다.**

**나머지 넷(날짜 창·봉 종류 창·지표 설정판·차트 스타일 창)은 "안 넘침" 이라 안 고쳤습니다.**

---

# ⭐ 오늘 밤 제일 중요한 것 — 헛검사 3개

**테스트가 초록인데 아무것도 안 지키고 있던 자리를 셋 찾았습니다.**

```
① 안내줄 글씨 검사   A || B 에서 A 가 항상 참 → 17px을 12px로 바꿔도 통과
② 리플레이 주문 차단  소스 글자만 봄 → 차단 코드를 ★지워도★ 통과
③ 저장분 불러오기    저장칸을 한 겹 감싸 넘겨 ★기본값★ 을 보고 있었음
                     회원이 기간 44 로 저장해도 검사는 99(기본값)를 봄
```

**"테스트 199개 통과" 를 믿고 있었는데 그 셋은 아무것도 안 지키고 있었습니다.**

## 팀이 자기 테스트가 헛것이 될 뻔한 것도 두 번 잡았습니다

```
기록팀   돌연변이를 undefined 로 만드니 계산이 NaN → ★NaN 비교는 늘 거짓★
         "안 넘친 것처럼" 보였습니다. "NaN 0번" 검사를 따로 넣었습니다
         또 "고장난 코드에 돌연변이를 넣으면 고장→고장이라 아무것도 증명 못 한다" 도 막음

수리팀   가짜 화면이 줄 높이를 0으로 줘서 새 함수가 되돌림값만 쓰고 있었습니다
         그대로 뒀으면 검사 하나가 아무것도 안 지켰습니다
```

---

# ⭐ 팀이 PM 지시를 세 번 바로잡았습니다

```
① "봉인에 굵기도 넣어라"
   → 8/31 사고는 금색 2px ↔ 금색 1px 였습니다. ★굵기를 넣으면 그 사고가 통과합니다★
   → 팀 말이 맞았습니다. 색 + 선모양 두 가지로만 판정(PM 지시보다 엄격)

② "alert 쓰는 건 이 파일뿐"
   → 실제로는 ★17개 파일 43번★. PM 이 틀렸습니다
   → 리플레이팀은 오히려 우리 사이트의 기존 방식을 따른 것이었습니다

③ "_order.txt 등록 줄 2개 빠짐"
   → PM 이 다른 팀 미완성 테스트를 빼면서 ★주석만 남기고 줄을 지웠습니다★ (197 vs 199)
```

# ⚠️ PM 이 네 번 잘못 쟀습니다 — 전부 같은 종류

```
① "칸이 안 생긴다"     회원이 안 쓰는 낮은 단계 함수를 부름
② "색 겹침 1개"        같은 이유
③ "패널 407px"         ★열자마자 같은 순간★ 에 쟀음 (내용이 아직 안 들어옴)
④ "지표 9"             같은 이유
```

**그리고 합성 클릭으로는 표정 창·시간대 창·리플레이가 안 열려 여러 번 확인을 못 했습니다.**
**팀들도 같은 벽에 부딪혔고, 점검팀만 CDP 실제 마우스로 뚫었습니다.**

---

# ⭐ 팀들이 "받은 대로 하고 끝" 을 한 번도 안 했습니다

```
수리팀   조사팀 제안대로 넣었는데 안 되자 → 원래 코드(placeToast)를 다시 읽어 CHIP_EDGE 를 찾음
수리팀   PM 진단(38)이 발동조차 안 하는 걸 확인 → 진짜 원인을 찾음
수리팀   배정 "+10px" → 재보니 "+70px · 390 도 깨짐" → 더 나쁜 걸 찾아냄
차트팀   봉인이 막자 → ★사본★ 으로 확인하고 다른 길을 고름 (봉인 안 고침)
차트팀   남의 테스트를 고쳐야 하자 → ★먼저 신고하고★ PM 승인 받음
기록팀   자기 검사가 헛것이 될 뻔한 걸 두 번 스스로 잡음
점검팀   자기 측정 오류 두 건을 먼저 정정 ("톱니 0개"·"겹침 10,013px²")
조사팀   "제가 처음 그렇게 재서 못 잡았습니다" 를 적어 다음 팀에 넘김
```

**"안 되니까 검사를 고치자" 를 한 번도 안 했습니다.**
