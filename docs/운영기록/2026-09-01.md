# 운영기록 2026-09-01 (화)

> ⚠️ **이 파일은 감사팀 지적으로 뒤늦게 만들었습니다(12:44).**
> 그 전까지 오늘 기록이 아예 없었고, 어제(08-31) 기록도 11:06 이후 8커밋이 빠져 있었습니다.
> **PM 이 커밋 메시지로만 남기고 운영기록을 안 썼습니다.**

## 발견
- 총 건수: 4
- 등급별: P1 2 / P2 1 / P3 1

## 게이트 1 (발견 → 배정)
- 통과: 4
- 반려: 0

## 게이트 2 (완료 → 승인)
- 통과: 2
- 반려: 1 (B건 — 최대 버튼이 항상 실패해서 배포 보류)
- 3회 반려로 대표 보고한 건: 없음

## 팀별 처리
- 점검팀: 0
- 조사팀: 0
- 수리팀: 4
- 디자인팀: 0
- 기록팀: 3
- 차트팀: 0
- 감사팀: 1

## 대표 승인 요청
- 없음 (승인대기 12건 중 오늘 3건 처리됨 — 아래)

## 확인 못 한 것
- **로그인 화면 전부** — 9일째
- **B건 거부 문구가 회원 화면에서 읽히는지** — 로그인 필요
- **차트가 안 보이는 것이 정말 고쳐졌는지** — 3시간 20분 무재발이지만
  원래 오늘 안 났을 수도 있어 구분 불가

## 수정 금지 파일 해시
- 결과: **11/12 일치 + js/trading.js 는 대표 결재로 변경 중**
- js/trading.js: `7e26f9d5…`(A건, 배포됨) → `ff3fef52…`(B건, 커밋 전)

---

# ✅ 대표가 서버 SQL 을 직접 돌리셨습니다 (승인대기 2·3·8 완료)

⚠️ **감사팀이 "2번은 미완" 이라고 보고했는데, 그건 문서만 봤기 때문입니다.**
**대표가 고친 파일을 다시 돌리셨고 PM 이 결과를 확인했습니다. 기록이 없었던 것이 문제입니다.**

```
① check-signup-insert-abuse.sql          읽기 전용
     회원수 3 · 기준자본 10만 3 · 기준자본 다름 0
     충전횟수 음수 0 · 충전누계 음수 0 · 사이클번호 이상 0
     → ★조작 흔적 0건. 구멍은 있었지만 들어온 사람이 없었습니다★

② fix-signup-insert-guard.sql            승인대기 8번
     "5. ✅ 가입할 때 계급 회계 칸이 고정됩니다"
     → 계급 0점 → 3,459점 조작 경로 차단

③ fix-trading-accounts-rls.sql [0]       승인대기 3번
     "✅ security invoker - 잠금이 실제로 돕니다" · 트리거 1개
     → 회원이 요청 한 번으로 자기 잔고를 바꾸던 것 차단

④ fix-seed-recharge-guard.sql            승인대기 2번
     1차 실행 ❌ ERROR 42601 syntax error at or near "미리보기" LINE 172
     → PM 이 171행 쉼표 누락을 고쳐 다시 드림 (d88fb05)
     2차 실행 ✅ "막혔습니다" (use_user_item 에 recharge_total 반영 확인)
     [0] 결과 — 재충전이용권 지난 사용건수 0 · 그 회원수 0
     → 소급 보정 대상 0명 · 계급 변동 0명
```

**기존 회원 기록은 한 줄도 안 바뀌었습니다.**

## ⚠️ PM 이 SQL 을 문법 검사 없이 드렸습니다

**대표가 Supabase 편집기에서 오류를 처음 보셨습니다.**
**앞으로 SQL 을 드리기 전에 최소한 CTE 연결과 괄호 짝은 확인합니다.**

---

# 🔴 [P1] 처음 열면 3.3초 동안 밝았습니다 — 고쳐서 배포 (`37842ed`)

**대표 보고** — "홈페이지를 처음 키면 렉?? 이라해야나 화이트모드가 나오고 얼마있다 다크모드로 바껴"

## ⚠️ PM 진단이 절반이었습니다

**수리팀이 캔버스 색을 8ms 간격 397회 찍어 갈랐습니다.**

```
0 ~ 1,632ms      하양       ← PM 이 찾은 것 (html·body 에 배경이 아예 없음)
1,632 ~ 3,275ms  밝은 회색   ← ★PM 이 못 본 것★
3,275ms          다크
```

**둘째 구간이 대표가 "화이트모드" 라고 하신 그 화면입니다.**
`style.css` 는 왔는데 `data-theme="dark"` 가 없어 `:root` 의 밝은 팔레트가 적용된 상태였습니다.

**⚠️ PM 지시대로 인라인 배경만 넣었으면 그 1.6초는 그대로 남았습니다.**

## 결과

```
                    전              후
하얀 구간            0~1,632ms   →  없음
밝은 회색 구간        1,632~3,275ms → 없음
다크가 되는 시각      3,275ms     →  ★10.9ms★
하양 표본            382/397     →  ★0/420★

여섯 폭 전부 흰 순간 0 — 360 0/363 · 375 0/318 · 390 0/318
                          768 0/297 · 1440 0/279 · 1920 0/286
```

---

# 🔴 [P1] TL 마켓 상품이 아무 효과가 없었습니다 — 대표 B 선택 (`37842ed`)

**대표 선택** — A 진짜 작동하게 / **B 일단 내려서 못 사게** / C 산 회원에게 TL 환불

```
원인  js/leverage-gate.js:35 의 DEFAULT_MAX 가 이미 100 이라
      50 TL 짜리 "레버리지 x100배 이용권" 을 사도 Math.max(100,100)=100
```

**`statusInfo()` 는 손대지 않고 `saleInfo()` 를 위에 얹었습니다.**
섞었으면 기존 `tl-market.test.js` 107개가 깨졌을 겁니다.

```
카드 6개 유지 · 구매 버튼 0개 · "판매 중지" 배지 · 왜 못 사는지 안내
이미 산 회원 기록 손 안 댐 (그건 C 안)
되돌리기 — SALE_PAUSED = true → false 한 글자
```

## ⚠️ 화면만 막았습니다. 서버는 아직입니다

**수리팀이 실서버를 읽어 확인했습니다.**

```
레버리지 x100배 이용권  50 TL   status = ★'active'★   ← 서버는 아직 팝니다
나머지 5개                      status = 'paused'
```

**개발자도구를 아는 회원은 `purchase_tl_market_item` 을 직접 부를 수 있습니다.**
**막으려면 SQL 한 줄이 필요합니다. 백로그 등록 대상입니다.**

---

# 🔴 [P1] 바이낸스 B건 — 명목이 커지면 배율 상한 축소 (배포 보류 중)

**대표 결재** — "바이낸스 거래 시스템을 따라해 그것만 허용"

## 되긴 됐습니다

```
지갑 10만 · 최대 증거금
  100배 → 거부   75배 → 거부   51배 → 거부   ★50배 → 체결★
검산  명목 9,524,029 USDT → 4구간 → 최대 50배 ✅
레버리지 창  프리셋이 1·10·20·30·40·50 까지만. 여섯 폭 전부
```

**⭐ 수리팀이 "대체 안전성" 을 숫자로 증명했습니다** —
명목 1,000 ~ 20억을 훑어 `(개시증거금률 − 실효 유지증거금률)` 최솟값 **+0.4000%**.
**"구간 상한은 통과했는데 진입 즉시 청산" 은 존재하지 않습니다.**

**⭐ 기존 포지션은 안 건드립니다** — B건 전에 열린 100배·명목 9,600,000 을 복원해
배율·수량·증거금·청산가 그대로, 절반 청산·전량 청산 정상 확인.

## 🔴 그런데 배포를 보류했습니다 — "최대" 버튼이 항상 실패합니다

```
배율   지갑 10만 최대버튼 명목    결과
100    9,523,810              거부
 75    7,228,916              거부
 51    4,973,184              거부
 50    4,878,049              체결
```

**51배 이상에서 100% 거부됩니다. 누를 때마다 실패하는 버튼은 고장으로 읽힙니다.**

## 🔴 그리고 대표가 라이브에서 실제로 막히셨습니다

**A 만 먼저 올린 PM 판단이 어정쩡한 상태를 만들었습니다.**
**유지증거금만 구간별로 올라가고 배율 상한은 안 올라가서, 100배를 고를 수는 있는데 주문이 거부됩니다.**

```
대표가 본 문구
  "이 금액 구간에서는 배율이 너무 높습니다 —
   유지증거금(229809.00 USDT)이 증거금보다 커서 진입 즉시 청산됩니다."

PM 역산 — 명목 약 271억 원 (18,090,450 USDT), 그 구간 바이낸스 최대 25배
  100배 → 증거금 180,905 < 유지증거금 229,809  → 넣는 순간 청산
  25배  → 증거금 723,618 > 유지증거금 229,809  → 정상
```

**막은 것 자체는 맞습니다. 바이낸스에서도 안 들어갑니다.**
**문제는 ① 못 쓸 배율을 고르게 해놓은 것 ② 문구가 어려운 것 ③ 최대 버튼이 실패하는 것.**

**대표 지시 — "나오지않게 거래 되게 만들어". 셋 다 고치는 중입니다.**

---

# 🔴 감사팀이 PM 사고 두 개를 더 찾았습니다

## ① 빨간 커밋이 하나가 아니라 **둘**이었습니다

```
c6242d8 (08-31 17:49)   tests-dir-hygiene 빨강
d88fb05 (09-01 09:53)   ★같은 빨강을 그대로 이어받아 배포★

빨간 상태 유지 시간 — 약 17시간 20분
37842ed 이 개명으로 해소
```

**회원에게 가는 코드에는 영향 없습니다**(파일 이름 위생 검사). 나머지 7,877건은 통과했습니다.
**다만 `d88fb05` 에서 다시 안 돌린 것은 같은 실수를 한 번 더 한 것입니다.**

## ② `c6242d8` 커밋 본문이 파일 하나를 안 적었습니다

```
js/position-table-extra.js (+50/−16) 가 본문에 한 번도 안 나옵니다
```

**내용은 A건에 필요한 정당한 변경입니다** — 포지션 표 "유지증거금" 칸이 옛 0.5%로
계산되던 것을 엔진 값을 쓰도록 고쳤습니다. **딸려 들어간 파일이 아닙니다.**
**문제는 기록입니다.** (가)·(나)와 같은 성격의 P1 세 번째 자리인데 본문은 둘만 적었습니다.

## ③ CLAUDE.md 두 곳이 틀린 채 남아 있었습니다 — 고쳤습니다

```
75-76행  "유지증거금률이 우리는 0.5% 고정" · "우리가 더 빨리 청산됩니다"
         → A건 배포로 더 이상 사실이 아닙니다
         → e731651 이 이미 지적했는데 그 뒤 CLAUDE.md 를 두 번 고치면서
           이 두 줄만 그대로 남았습니다

206행    "이번 허가는 이 건에 한합니다"
         → A 하나만인지 A~D 넷 다인지 애매했습니다
         → 감사팀이 B건 착수를 이 근거로 보류시켰습니다
```

**⭐ 감사팀 지적이 맞았습니다.** 다만 대표 원문이 **PM 이 A/B/C/D 를 나열해 물은 데 대한 답**
(`"바이낸스 거래 시스템을 따라해 그것만 허용"`)이라 **넷 다 같은 결재입니다.**
**문장을 고쳐 애매함을 없앴습니다. B건 보류를 풉니다.**

## ④ 감사팀이 커밋 12개를 격리 worktree 에서 전부 다시 돌렸습니다

```
메시지 ↔ diff 대조    허위 1건 (ccd9ddb, 이미 정정됨)
커밋 시점별 npm test   12개 실측 — 10개 초록, 2개 빨강(위 ①)
딸려 들어간 파일       74개 커밋 포함 ★0건★
                      -A 로 인한 오염 오늘 하루 0건
```

**⭐ `7075551` 의 "새 색 0개" 주장을 직접 세어 사실 확인까지 했습니다.**

---

# 다음

```
수리팀   B-2 — 오류가 뜰 상황 자체를 없애기 (대표 지시)
기록팀   B건 해시 + 동작 바뀐 봉인 4개
남은 것  C 청산 마크가격 · D Cross · TL 마켓 서버 잠금 · 구글 폰트 311ms
```
