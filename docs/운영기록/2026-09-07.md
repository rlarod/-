# 운영기록 2026-09-07

> **09-04 이후 3일이 비었습니다.** 오늘 다시 시작했습니다.

## 발견
- 총 건수: 1
- 등급별: P1 0 / P2 0 / P3 0 · **분류 밖 1건**(아래)

### 09-04 운영기록이 3일간 미커밋으로 남아 있었습니다

951줄짜리 기록(배포 9건 · 게이트 2 반려 2건 · PM 실수 5건)이 작업트리에만
있었습니다. **커밋을 안 했으면 다른 PC 에서 clone 했을 때 그날 기록이 통째로
없습니다.** `b04e911` 로 올렸습니다.

`tests/_order.txt` 는 **빈 줄 하나** 차이뿐이라 되돌렸습니다.

## 시작 상태

```
HEAD          d7bdc66 (09-04 13:57)
작업트리       tests/_order.txt(빈 줄 1) · 09-04 기록 미추적  → 둘 다 정리함
수정 금지 파일  12/12 일치
★로컬 서버★   000 (죽어 있음) → npm run dev 재기동 → 200
라이브        200
크롬 우리것    0개 / 전체 12 · CPU 2%
```

**로컬 서버가 또 죽어 있었습니다.** CLAUDE.md 의 EMFILE 건이고, 3일 방치라
당연합니다. **팀을 붙이기 전에 살린 것이 맞았습니다** — 안 그랬으면 세 팀이
전부 "요소를 못 찾는다" 로 헤맸을 자리입니다.

## PM 라이브 확인 (1440×900) — 09-04 배포분이 그대로 살아 있습니다

```
차트 머리       ★41px★   (09-04 d7bdc66 그대로)
지표 칩줄       ★32px★ · 보이는 칩 ★1개★
마크가격 겹침    -80 (안 겹침)
최근 체결       27줄     ← ⚠️ 30 이 정상. 로딩 중인지 조용한 고장인지 감사팀 확인 요청
가로 넘침       0
콘솔 오류       0
```

## 게이트 1 (발견 → 배정)
- 통과: 0
- 반려: 0
- 반려 사유:

## 게이트 2 (완료 → 승인)
- 통과: 0
- 반려: 0
- 반려 사유:
- 3회 반려로 대표 보고한 건: 없음

## 팀별 처리
- 점검팀: 0
- 조사팀: 0
- 수리팀: 0
- 디자인팀: 1 (진행 중 — 401~700 구간 글씨 역전)
- 기록팀: 1 (진행 중 — 봉인 바닥 15 + 굵기 구멍 ㉢)
- 차트팀: 1 (진행 중 — 3단계 시간단위를 도구막대로)
- 감사팀: 1 (진행 중 — 09-04 배포 9건 + 3일 공백 라이브)

## 대표 승인 요청
- **세로 도구막대 버튼 크기** — 트레이딩뷰 52×38 · 27개 / 우리 38×38 · 17개.
  2026-09-03 에 대표가 **"줄인다"** 로 정하신 자리(08-26 "더 키워" 를 뒤집으신 것)라
  PM 이 임의로 못 바꿉니다. **트레이딩뷰처럼 키울지 여쭀고 답을 기다립니다.**
- 돈 나가는 건: **0건**

## 확인 못 한 것
- **로그인 후 화면** — PM 은 로그인하지 않습니다
- **여섯 폭 라이브** — 오늘 1440 만 봤습니다. 감사팀에 배정했습니다
- **최근 체결 27줄** — 정상인지 조용한 고장인지 아직 못 가렸습니다

## 수정 금지 파일 해시
- 결과: 12/12 일치
- 불일치 시 파일명: 없음

---

## 오늘 배정 (09:50)

| 팀 | 하는 일 | 만지는 파일 |
|---|---|---|
| 차트팀 | **3단계 — 시간단위 줄을 도구막대로** (폰 95px 회수) | `css/chart-toolbar.css` · `js/chart-indicators.js` 등 |
| 디자인팀 | 401~700 구간 글씨 역전 (360 은 15px 인데 412 는 13.5px) | `style.css` |
| 기록팀 | 봉인 바닥 13 → 15 + 굵기 구멍 ㉢ | `tests/` |
| 감사팀 | 09-04 배포 9건 + 3일 공백 라이브 | 감사만 |

### 차트팀 3단계 — PM 이 방향을 정했습니다

팀이 미리 찾은 막는 것 셋:
> `css/chart-toolbar.css`(디자인팀)가 크기 변수 6개를 `.chart-panel` 에 선언하고,
> 전체화면 규칙도 `.chart-panel` 자손으로 걸려 있어, 막대를 페이지 폭으로 빼면
> 막대가 무너집니다.

**PM 판단 — `css/chart-toolbar.css` 를 차트팀이 직접 만지게 했습니다.**
디자인팀은 `style.css` 만 쓰므로 충돌 없습니다.
방향 — ① 크기 변수 6개를 위로 올리되 **값은 그대로, 선언 위치만** ②
전체화면 규칙을 새 위치에 맞춰 다시 쓰기(⚠️ `.chart-panel` 이 전체화면 요소라
막대가 밖으로 나가면 전체화면에서 안 보일 수 있음) ③ 도구막대를 시간단위 줄
자리로 옮기는 쪽.

### 남은 미배정 건

```
· 이름표 단추가 위로 35px 넘침 — 차트팀 3단계에 같이 넣으라고 지시함
· 범례 가로 잘림 (360 에서 MACD 세 번째 값)
· 주 차트가 161px 까지 얇아지는 것
· 봉인 구멍 ㉠(팔레트 6자리 hex) · ㉣(모서리 px) · ㉤(그림자 box-shadow)
· 세로 도구막대 버튼 크기 — ★대표 답변 대기★
```

---

# 배포 · `42cd474` 더 큰 폰이 더 작은 글씨였던 것

```
전   360 폰 15px  ·  412 폰 ★13.5px★
후   360~700 전부 ★15px★
```

## ★디자인팀이 자기 앞선 계산을 스스로 뒤집었습니다★

09-04 보고에 "402 에서 여유 0.2px 밖에 안 나와 여백만으로는 못 푼다" 고 적었는데
**조건부로만 맞았습니다** — 좌우 여백 7px 을 둔 채 글자만 올렸을 때의 값이고
**`gap` 을 안 봤습니다.** `.top-banner-nav{gap:2px}` 이고 메뉴가 6개라 **2×5 = 10px**.

```
style.css:491   13.5px → ★15px★
style.css:3512  새 블록 @media(401~520){ .top-banner-nav{gap:0px;} }
```
**여백·글자·`:first-child` 를 하나도 안 건드렸습니다.** 좌우 여백 7px 이 그대로라
**이웃 글자 사이가 14px** — 360 구간의 8px 보다 넓습니다.
**400 이하는 제외** — 거기는 여백 3px 이라 gap 을 없애면 6px 이 되어 바닥 8px 밑입니다.

가장 빠듯한 401px 도 여유 **9.3px**(360 의 2.3px 보다 네 배).

## ⚠️⚠️ PM 사고 — 검수 안 거친 파일 6개가 같이 올라갔습니다

```
git add style.css      ← PM 이 지정한 것은 이것뿐
실제 커밋된 것          style.css + 기록팀 파일 ★6개★
```

**색인에 기록팀 파일이 이미 스테이징돼 있었고, PM 이 `git diff --cached --name-only`
출력을 보고도 `&&` 체인으로 바로 커밋이 실행됐습니다.**

**어제도 같은 일이 있었습니다**(09-04, 차트팀이 임시 색인을 쓰다 진짜 색인에 `git add`).
그때는 커밋 전에 발견해 뺐는데 **이번엔 못 막았습니다.**

**결과적으로는 문제없었습니다** — 기록팀이 직후 완료 보고를 했고 그 6개가 완성분이었으며,
PM 이 **올린 상태 그대로 사후 검증해 218/218 · 검사 11,845 / 실패 0** 이었습니다.
회원 화면 영향은 `style.css` 하나뿐이고 나머지는 전부 `tests/` 입니다.

**그래도 절차는 틀렸습니다.** 확인하고 올려야 하는데 올리고 확인했습니다.
**팀 지침에 "작업이 끝나면 `git restore --staged .` 로 색인을 비울 것" 을 넣었습니다** —
커밋은 PM 이 파일을 골라서 합니다.

## ⚠️ PM 이 브라우저로 직접 못 쟀습니다

`os error 10060` 으로 **네 번** 막혔습니다(세션 이름을 세 번 바꿔가며 시도).
서버 200 · agent-browser 0.34.0 정상 확인했고, 그때 차트팀이 같은 브라우저를 쓰고 있었습니다.

대신 셋으로 확인했습니다 — ① **소스 값 직접 확인**(13.5 → 15px 한 줄, 새 블록은
`gap:0px` 하나뿐) ② **디자인팀 4배 확대 캡처를 눈으로 봄**(글자 사이가 넉넉히 벌어짐)
③ **worktree 217/217 · 11,785/0**.

---

# 배포 · `43f8166` 버그의 진짜 이름은 "13.5px" 이 아니었습니다

`42cd474` 로 401~700 이 15px 이 됐는데 봉인의 밴드 2 바닥이 **13** 이라
**누가 13.5px 로 되돌려도 초록**이었습니다.
**08e93b0 뒤 360 구간에서 사흘을 놓친 것과 한 글자도 다르지 않은 구조입니다.**

## ★기록팀이 바닥 숫자만 올리지 않았습니다★

> "이번 버그의 진짜 이름은 **더 넓은 폰이 더 작은 글씨** 입니다.
>  바닥 숫자만 올리면 같은 유형이 다른 구간에서 또 납니다."

```
1  밴드 2 바닥 13 → 15
2  ★역전 금지★ — 320~1920 을 훑어 폭이 넓어지는데 글자가 작아지면 실패
3  ★밴드 경계를 CSS 에서 세어 대조★ — 사흘을 놓친 진짜 원인은 13 이라는 숫자가
   아니라 ★손으로 적은 밴드 표가 CSS 와 조용히 어긋난 것★ 입니다
4  훑는 폭 4개 → ★360~700 을 1px 씩 341개★
```

## 돌연변이 — 1px 훑기와 역전 검사가 ★각각 혼자★ 잡는 것을 보였습니다

| 심은 것 | 결과 |
|---|---|
| 401~700 → 13.5px | 빨강 5 — 300개 폭 + **역전 검사가 따로** |
| `gap:0px` 삭제 | 빨강 3 — 넘치는 폭이 **401px 딱 하나**. **1px 훑기만** 잡음 |
| 801+ 만 20px | 빨강 — **바닥 검사 셋 다 초록인데 역전만 잡힘** |
| `max-width:700 → 640` | 빨강 — `경계 401,641 vs 밴드 표 401,701` |

**"안 고쳤으면 못 잡았다" 도 검사 안에 넣었습니다** —
`(대조) 표준 폭 6개도 이 되돌림을 하나도 못 잡는다`
(401~700 에 드는 표준 폭이 하나도 없습니다).

검사 91 → **110개**. worktree **218/218 · 검사 11,864 / 실패 0**.

## PM 지적 521~700 — 기록팀이 계산했고 안전했습니다

```
401~520  여백 7·7 · gap 0  → 401px 여유 9.2px · 이웃 14px
521~700  여백 10·10 · gap 2 → 521px 여유 ★78.2px★ · 이웃 22px
```
**521 에서 오히려 여유가 늘어납니다**(여백이 7 → 10 으로 넓어지는데 담는칸이 더 크게 늠).
위험 구간은 아니었지만 **봉인이 안 보고 있던 것**이 문제라 341개 폭으로 넓혔습니다.

---

# 감사팀 — 09-04 배포 9건 + 3일 공백

## 1건 — **PASS**. 허위 보고 0건

worktree 격리 재측정 **217/217 · 11,785/0** — `d7bdc66` 주장과 소수점까지 일치.
PM 이 적은 41/32/1 · 저장칸 불변 · 27색 · 명암비 4.52 전부 재현됐습니다.
감사팀이 **CIEDE2000 을 독립 계산**해 차트팀 ΔE 값도 전부 맞는 것을 확인했습니다
(최소 9.71 · 새 7색 10.38 · 버린 `#FF01AE` 는 하락색과 16.44 로 채택색보다 가까움).

`e92159b` 공용 파일을 **실제로 뚫어봤습니다** — 판정기 무력화·파서 눈가림 두 공격 다
소비자 9개와 원본지킴이가 전부 실패로 잡았습니다. **기본 금지도 실측**(`1lh`·`1rcap`·
`17Q`·`1.5rex` 4/4 잡음, `17PX`·`!important` 2/2 통과로 헛잡음 0).

## ⚠️ PM 이 `d7bdc66` 에 적은 근거가 틀렸습니다

PM 은 "위로 35px 넘치지만 **안 보이는 버튼**이라 통과" 라고 적었는데,
**42px 중 7px 은 화면 안(y 0~7)에 있고 `pointer-events:auto` · `z-index:7`** 입니다.

**결론(통과)은 맞습니다.** 다만 진짜 이유는 다릅니다 —
```
js/chart-indicators.js:1256-1257   resize · scroll 둘 다 hideActs 를 겁니다
js/chart-indicators.js:1106        chipBot <= visTop → placeActs 가 return false
```
감사팀이 실측했습니다 — 진짜로 스크롤한 뒤에는 `display:none` 이고, 화면 맨 위
히트테스트는 `DIV.panel chart-panel` 이 잡힙니다(단추 아님). -35 상태를 만들 수 있는
유일한 방법은 `scrollTo` 와 호버를 **같은 태스크 안에서** 실행하는 것이고
**사람의 스크롤로는 불가능합니다.**

**지금 근거대로면 다음 사람이 "7px 은 보이는데?" 로 뒤집습니다.** 여기 남깁니다.

## 2건 — 3일 공백 라이브 **NO ISSUE** (6폭)

### 최근 체결 27줄 — ✅ **정상. 조용한 고장 아님** (확신도 확실)

```
DOM      6폭 전부 ★30줄★ 다 있음
신호     최근 체결 초당 ★443.3회★  vs  호가창 초당 64.7회  (16초 측정)
         = 체결이 호가창보다 ★6.8배★ 더 자주 옵니다
원인     js/trades-fit.js 가 ★의도적으로★ 칸에 들어가는 만큼만 보입니다
         1440 실측: 칸 495px ÷ 줄 34px = 14줄 → 나머지 16줄에 hide
```
2026-08-21 의 진짜 조용한 고장(체결 10초에 **0회** / 호가 20회)과 **정반대**입니다.
PM 이 본 27줄은 창 높이가 더 컸기 때문입니다.

### ★감사팀이 자기 측정을 스스로 정정했습니다★

360/375/390 에서 마크가격 겹침이 `0` 으로 나왔는데, **폰 기본 탭이 차트라 호가창
패널이 `0×0` 으로 숨겨진 상태**를 잰 것이었습니다. 호가 탭으로 전환해 다시 재니
-104/-111/-119. **숨겨진 요소를 재고 "겹침 0, 통과" 로 올릴 뻔했습니다.**

## 3건 — 감사팀이 올린 후보

```
점검팀  ★원화(KRW) 6폭★ — 09-04 배포 9건 중 3건이 글자 길이에 민감한데
        KRW 로 한 번도 안 재봤습니다. 74aa5d2 는 원화 전용 P2 였습니다  → ★배정함★
조사팀  CDP dispatchTouchEvent 가 ★360 폭에서만★ 응답 없음 — 여섯 팀 누구도
        360 진짜 터치를 검증할 수단이 없습니다
수리팀  TL-015 — auth:changed 를 13곳이 구독하는데 ★발신 0건★.
        비회원으로 3초 넘게 보다 다른 계정으로 로그인하면 앞 계정 데이터가 로컬에 남음
        ⚠️ 발신 지점이 js/auth.js — ★수정 금지 파일★. 함수 감싸기로만 가야 함
```

## ⚠️ 대표 결재가 오래 밀려 있습니다 (감사팀 지적)

```
승인대기 5번   로그인 폰 캡처        08-24 부터 ★14일째★
승인대기 2·8   P1 두 건 (SQL 필요)   08-27~28 부터 ★10일째★
```
**캡처 하나가 팀들의 "확인 못 함" 대부분을 풀어줍니다.** 오늘 `d7bdc66` 도 또
올렸습니다. **대표께 다시 올렸습니다.**

---

# 기록팀이 PM 에게 올린 것 둘

**① `style.css` 되돌리기 안내가 두 곳 다 불완전합니다** → **디자인팀 배정함**
```
style.css:491   봉인 밴드 2 바닥·역전 검사 이야기가 없음 → 적힌 대로 하면 npm test 빨강
style.css:4903  `바닥_임시` 는 오늘 오전에 `바닥` 으로 이름이 바뀌어 ★없는 이름★
```
**오늘 CLAUDE.md 에 넣은 `git rm` 규칙과 같은 종류입니다** — 되돌리는 방법이 틀리면
**사고 나서 급할 때 발이 묶입니다.**

**② 320~357 에서 메뉴가 넘칩니다** (320px 여유 −37.8 · 357px −0.8 · **358부터 들어감**)
표준 최소가 360 이라 검사로 안 만들었습니다(만들면 `style.css` 를 고쳐야만 초록).
`overflow-x:auto` 라 잘려 사라지지는 않습니다. **디자인팀에 "판단만" 요청했습니다.**

## 배정 (10:40)

| 팀 | 하는 일 |
|---|---|
| 차트팀 | 3단계 — 시간단위를 도구막대로 (계속) |
| 디자인팀 | `style.css` 되돌리기 안내 정정 + 320 판단 |
| 점검팀 | **원화(KRW) 여섯 폭** |
| 감사팀 | 완료 |

---

# 오후 (12:00~) — 배포 3건 · 게이트 2 반려 1건

## 게이트 2 (완료 → 승인) — 오후분

| 건 | 팀 | 결과 | PM 이 직접 한 검증 |
|---|---|---|---|
| `style.css` 되돌리기 안내 정정 | 디자인팀 | **통과 → `537f7af`** | 주석만 바뀐 것을 바이트로 확인 (주석 걷은 CSS 121,964 == 121,964) |
| P1 십자선 OHLC 가 가격축 덮음 | 수리팀 | **통과 → `8efb985`** | 격리 worktree 221→219/219 · 브라우저 직접 실측 · 캡처 눈으로 확인 |
| 봉인을 명단→패턴 훑기로 | 기록팀 | **통과 → `7285334`** | 격리 worktree 220/220 · **PM 이 직접 돌연변이 주입** |
| 맨 윗줄 한 줄로 합치기(3단계) | 차트팀 | **반려 1회** | 되돌리기를 실제로 눌러보니 터짐 (아래) |

**누계 — 게이트 2 통과 3 / 반려 1.**

## ★색인에 남의 팀 파일이 섞여 있어 `git commit --only` 로 갈랐습니다★

오늘 오전 `42cd474` 에서 **검수 안 한 기록팀 파일 6개가 같이 커밋되는 사고**가
있었습니다(PM 이 `git add style.css` 했는데 색인에 이미 남의 것이 있었음).
오후엔 같은 자리에서 **제 가드가 먼저 울렸습니다** —
```
★위에 style.css 말고 차트팀 것이 있으면 멈춥니다★
  css/chart-toprow.css  js/chart-toprow.js  tests/_order.txt  tests/chart-toprow-seal.test.js
```
그래서 `git add` 를 아예 안 쓰고 **`git commit --only <파일>`** 로 올렸습니다.
색인은 건드리지 않으므로 차트팀 것이 그대로 남습니다.

⚠️ **`git restore --staged .` 로 색인을 비우면 안 됩니다.** 차트팀 새 파일 3개가
`git ls-files` 에서 사라져 `html-assets-tracked` · `tests-dir-hygiene` 가 그 자리에서
빨개집니다. **이름을 하나씩 지정해서만** 다뤘습니다.

## ★격리 worktree 로 검증했습니다★ — 작업트리에 세 팀이 섞여 있어서

`npm test` 는 **git 이 아니라 디스크를 읽습니다.** 작업트리가 지저분하면 남의
미완성이 같이 돌아갑니다. 그래서 건마다 `HEAD` 에 **그 팀 것만** 얹은 worktree 를
떼어 `git add -A` 후 실행했습니다.
```
수리팀  219개 파일 / 219개 통과 / 실패 0 · 검사 11,889
기록팀  220개 파일 / 220개 통과 / 실패 0 · 검사 11,921
차트팀  221개 파일 / 221개 통과 / 실패 0 · 검사 11,975
```
커밋도 그 worktree 안에서 하고 `git update-ref` 로 `main` 을 앞당겼습니다
(부모가 현재 `main` 인지 매번 대조 — 진짜 fast-forward 인지 확인).

## P1 — 제가 직접 잰 숫자

침범 = OHLC 줄 오른끝 − 그림 영역 오른끝. **양수면 가격축을 덮는 것.**
```
폭     가격축     전       후
360     94px    +96  →  ★-12★
375     97px    +97  →  ★-24★
768    170px   +170  →  ★ -5★    ← 가격축을 통째로 덮고 있었습니다
1440   170px   +145  →  ★ -5★
1920   170px   +129  →  ★ -5★
```
**넓은 화면이 더 나빴습니다.** 원화는 자릿수가 많아 가격축이 170px 까지 넓어지는데
상자가 그만큼 더 늘어났기 때문입니다. 여섯 폭 전부 **칸넘침 0**(값이 잘린 곳 없음).

768 캡처를 눈으로 확인 — **`₩120,900,000` 이 온전히 읽힙니다.** 대표 캡처에서
`,900,000` 으로 잘려 있던 바로 그 숫자입니다.

## 봉인이 진짜 잡는지 ★PM 이 직접 돌연변이를 넣었습니다★ (사본에서만)

기록팀 봉인을 그대로 믿지 않고, 사본인 worktree 에서 수리팀 고침을 `HEAD~1` 로
되돌려 봤습니다.
```
tests/chart-overlay-right-edge.test.js  통과 31 / 실패 1
  오른쪽 끝 없는 절대배치 1건 -> 걸린 것: js/chart-ohlc-legend.js  .tl-ohlc
tests/chart-ohlc-axis-clear.test.js     8건 빨강
```
**파일과 클래스를 정확히 지목했습니다.** 원본은 한 글자도 안 건드렸습니다.

## 게이트 2 반려 1건 — 차트팀 3단계

**되돌리기를 적기만 하고 눌러보지 않은 병입니다. 오늘만 세 번째입니다.**

`js/chart-toprow.js:43~52` 안내를 **글자 그대로** 사본에서 실행했더니:
```
tests/chart-toprow-seal.test.js  →  ★종료코드 1★
  Error: ENOENT: no such file or directory ... js/chart-toprow.js
```
깔끔한 실패도 아니고 **예외가 나서 그 자리에서 터집니다.**
(`test-registry` · `tests-dir-hygiene` · `html-assets-tracked` 는 셋 다 초록 —
이 셋은 이 병을 못 봅니다.)

빠진 것:
```
✗ tests/chart-toprow-seal.test.js 를 지우라는 말이 없음
✗ tests/_order.txt 등록 블록을 지우라는 말이 없음
✗ js/chart-indicators.js · js/interval-more.js · js/stream-loading-hint.js 가 안내에 없음
```
`tests/_order.txt` 블록 주석에는 제대로 적혀 있는데 **모듈 머리글에는 없었습니다.**
되돌리는 사람은 보통 고친 파일의 머리글을 먼저 봅니다.

**나머지는 전부 통과했습니다** — 아래 실측은 PM 이 직접 잰 것이라 재작업 안 합니다.

## 차트팀 3단계 — PM 실측 (수정 전 포트 3007 / 수정 후 포트 3008)

```
폭 390   머리 263 → ★122px★     두 줄(95+89=184) → 한 줄 ★44px★
         첫 화면 캔들 띠 136 → ★277px (2배)★     페이지 가로 밀림 0
폭 1440  머리 107 → ★61px★      첫 화면 캔들 띠 107 → ★153px★
```
**옆으로 밀기 — 마지막 단추까지 닿습니다**
```
360   막대 344 / 내용 981 / 끝까지 민 뒤 마지막 단추 오른끝 350 <= 막대 352  OK
390   374 / 981 / 380 <= 382  OK      768  752 / 1201 / 758 <= 760  OK
1440  넘침 없음, > 표시 none  OK  (넘칠 때만 뜹니다)
```
**전체화면 — 막대가 안 사라집니다**
```
전   부모 tlc-toprow        차트판 안 false   보임
중   부모 ★panel chart-panel★   차트판 안 ★true★    높이 44
후   부모 tlc-toprow        차트판 안 false   높이 44   ← 제자리
```
⚠️ 처음엔 "전체화면이 안 된다" 로 잘못 봤습니다. **`reassert` 가 다음 프레임에
도는데 제가 같은 호출 안에서 재서** 못 본 것이었습니다. **적용과 측정을 따로
불러야 합니다** — 이 환경의 알려진 함정이고 제가 또 걸렸습니다.

## 팀별 처리 (누계)
- 점검팀: 1 (진행 중 — P1 배포 후 라이브 여섯 폭 회귀)
- 조사팀: 1 (진행 중 — `.tl-ind-bar` 138px 이 원화 가격축 169px 을 덮는지 실측)
- 수리팀: **1 완료** (P1 → `8efb985`)
- 디자인팀: **1 완료** (되돌리기 안내 → `537f7af`)
- 기록팀: **1 완료** (명단→패턴 훑기 → `7285334`)
- 차트팀: 1 반려 → 재작업 진행 중
- 감사팀: 1 (진행 중 — 되돌리기 안내 전수 감사)

## 배포 (전부 라이브 확인까지 마침)
```
537f7af  style.css 되돌리기 안내      라이브 200 · 315,860바이트 · 새 절 4자리 확인
8efb985  P1 십자선 OHLC              라이브 200 · applyWidth 3곳 확인
7285334  봉인 명단→패턴 훑기          (검사 파일 — 화면 영향 없음)
```

## 수정 금지 파일 해시
- 결과: **12/12 일치** (건마다 매번 확인)
- 불일치 시 파일명: 없음

## 확인 못 한 것
- **로그인 화면.** PM 은 로그인하지 않습니다. 포지션 표·주문 패널·내 자산·레버리지
  창은 이번에도 못 봤습니다. **승인대기 5번(로그인 폰 캡처)이 14일째입니다**
- **십자선 "살아있는" 상태(`tl-ohlc-live`).** 합성 마우스로는 안 켜져, 캔들을 덮는
  최대 높이 98px 은 **수리팀이 실제 마우스로 잰 값을 인용**했습니다. 제가 잰 것은
  그보다 좁은 상태입니다. 점검팀에 진짜 마우스 실측을 배정했습니다
- **`ResizeObserver` 없는 브라우저의 실기기 확인.** jsdom 으로만 봤습니다
- **진짜 전체화면.** `fullscreenElement` 를 흉내내서 쟀습니다. 실제 단추를 누른
  확인은 차트팀 2건으로 배정했습니다
- **`.tl-ind-bar` 138px** 이 실제로 숫자를 덮는지 — 조사팀 진행 중

---

# 점검팀 라이브 회귀 — P1 해결 확인 (12/12) · ★PM 정정 1건★

## P1 은 라이브에서 고쳐졌습니다

점검팀이 **실제 마우스**로 십자선을 켜고(12개 조합 전부 `tl-ohlc-live` 확인) 쟀습니다.
PM 은 합성 마우스라 이 상태를 못 쟀습니다 — **이 숫자가 더 정확합니다.**

```
침범 (양수 = 가격축 덮음)     12개 조합 ★전부 음수★, 최대 -5.5px
값 잘림                        ★0건★ (… 로 자르는 CSS 자체가 없음)
십자선 떼면 복귀               12개 전부 확인
OHLC ↔ 지표 칩 겹침            12개 전부 ★0px★ (제일 빠듯한 360 KRW 도 틈 0.3px)
통화·창크기 바꿔도 따라감      새로고침 없이 확인 (max-width 555 → 593 → 509)
라이브 파일 md5                8efb985 의 파일과 동일
```
768 KRW 캡처 — **`₩120,900,000` 완전히 읽힙니다.**

## 회귀 3종 · 콘솔 — 여섯 폭 전부 정상

```
마크가격     여섯 폭 전부 화면 안, 가려짐 0px
매수·매도    12초에 21~24회 변동
최근 체결    여섯 폭 전부 ★30줄★ · 12초에 20~27회 갱신
             (같은 시간 호가창 21~24회 — ★신호 횟수가 같은 수준이라 정상★)
콘솔 오류    여섯 폭 전부 ★0건★ (경고 2건은 기존 것)
가로 밀림    여섯 폭 전부 0px · 내용 숨기는 컨테이너 0개
```
⚠️ **폰에서 호가창 마크가격 좌표가 0 인 것은 조용한 고장이 아닙니다** — 폰은 탭
구조라 기본이 `차트` 탭이고 `#orderbook-panel` 이 `display:none` 입니다. 점검팀이
**탭을 눌러** 360 에서 `₩119,477,700` 이 정상 표시되는 것을 확인했습니다.

## ★캔들 덮는 높이가 천장에 붙었습니다★ — 여유 0.2px

```
360/375/390 KRW · 십자선 짚는 동안   ★97.8px★   (그림칸 600.5px 의 16.3%)
PM 이 정한 상한                       ★98px★
                                      여유 ★0.2px★
```
**글씨가 1px 커지거나 원화 자릿수가 하나 늘면 그 자리에서 넘습니다.**
십자선을 떼면 22.9px 로 돌아옵니다. 디자인팀에 배정했습니다.

---

## ★PM 정정 — `.tl-ind-bar` 는 "칩이 글자를 덮는다" 가 재현되지 않았습니다★

조사팀이 **확신도 확실**로 *"768 KRW 는 기본 조작만으로 +31.4px 침범, `₩96,000,000`
이 `,000,000` 으로 읽힌다"* 고 보고했고, PM 이 그 대목을 대표께 옮겼습니다.
**그 뒤 두 사람이 따로 재봤는데 둘 다 칩 침범이 0 이었습니다.**

```
PM (포트 3007, 768x900 KRW)          보이는 칩 1개(거래량) · 넘은 칩 ★0개★
                                      나머지 9개와 접기 꺾쇠까지 전부 width=0
점검팀 (라이브, 768 KRW)              지표를 4개·7개·9개까지 켜 봄
                                        4개  칩 오른끝 침범 -146.6
                                        7개  2줄로 접힘, 침범 -52.8
                                        9개  "▾ 지표 9" 한 개로 접힘, 침범 -432.8
                                      ★어느 경우에도 넘은 칩 0개★
                                      pointer-events:none 이라 축 드래그도 안 가로챕니다
```

**세 사람이 일치하는 것 — 상자(container)는 실제로 축을 침범합니다.**
```
768 KRW   조사팀 +87.5 · PM +87.5 · 점검팀 +87.5      (셋이 0.1px 안에서 같음)
1440 KRW  조사팀 +32.0 · PM +32.0 · 점검팀 +32.0
```

**갈리는 것 — 그 상자 안의 칩이 실제로 글자를 덮는가.** 조사팀만 "덮는다" 이고
PM·점검팀은 "안 덮는다" 입니다. 조사팀은 캔버스 픽셀까지 세어 캡처를 붙였으므로
**거짓이라고 단정하지 않습니다** — 셋이 서로 다른 지표 조합·화면 높이를 썼습니다.

**PM 판단 — 등급을 P3 으로 두고, 고치는 것은 그대로 진행합니다.**
지금 눈에 보이는 피해는 0px 이라 급하지 않습니다. 다만 **상자가 축 안으로 87.5px
들어가 있는 것 자체가 잠긴 고장**이라, 칩이 보이는 조합이 나오는 순간 바로 덮입니다.
수리팀 배정문에 **"조사팀 숫자를 옮겨 적지 말고 이 차이부터 가르라"** 고 이미
적어 두었습니다.

⚠️ **PM 이 대표께 "지표 칩 줄도 가격축을 덮습니다" 라고 보고했습니다.**
**정확히는 "상자가 덮고, 지금 조합에서는 글자가 안 덮인다" 입니다.** 다음 보고에서
정정합니다.

## 새로 나온 것 — [P3] 폰에서 십자선 ★변동 금액★ 이 안 보입니다

360/375/390 에서 `(-0.07%)` 는 나오는데 **`-₩84,150` 이 화면 어디에도 없습니다.**
잘린 게 아니라 `display:none` 입니다 (`js/chart-ohlc-legend.js:173` 의
`@media (max-width:767px){ .tl-ohlc .abs{display:none;} }`).

**`8efb985` 이 만든 게 아닙니다 — `d7bdc66`(09-04) 에서 들어왔고 주석에 일부러
감춘다고 적혀 있습니다.** 다만 오늘 상자에 `max-width` 가 생겨 사정이 달라졌으므로
**자리를 만들 수 있는지 디자인팀에 배정**했습니다. ⚠️ 높이 여유가 0.2px 뿐이라
그냥 켜면 천장을 넘습니다.

## 확인 못 한 것 (점검팀)

- **로그인 화면 전부.** 다만 1440/1920 비회원 화면에도 주문 패널이 열려 차트가
  좁아진 상태였고 그때도 침범 -5.5px 였습니다
- `ResizeObserver` 없는 브라우저 — 크롬으로만
- **터치 기기의 실제 십자선** — 폰에서 손가락으로 길게 눌렀을 때 켜지는지 못 봤습니다
- 점검팀이 자기 세션만 닫았는데 그 뒤 목록이 비었습니다.
  `--all` 은 안 썼고 다른 팀이 스스로 닫은 것으로 보이나 **100% 배제는 못 한다**고 적었습니다

---

# 감사팀 전수 감사 — 되돌리기 안내가 ★지뢰 85개★ 였습니다

커밋 503개 · `js/*.js` · `css/*.css` · `style.css` · `tests/_order.txt` 전수.
**의심 5건은 별도 worktree 에서 실제로 눌러봤습니다.**

| 위험도 | 대상 | 무엇이 틀렸나 | 눌러본 결과 |
|---|---|---|---|
| 🔴1 | `tests/_order.txt` **52곳**(224~786행) | `git rm` 이 아니라 그냥 "삭제" | `tests-dir-hygiene` **통과 35 / 실패 1** |
| 🔴2 | 커밋 **32건** | `git checkout HEAD~1 --` | 종료 0 · status 깨끗 · **아무 일도 안 일어남** |
| 🔴3 | `tests/_order.txt:218` | 봉인 파일을 지우라는 말이 **아예 없음** | `rm` 을 안 써도 무조건 터짐 |
| 🟠4 | `42cd474` 커밋 메시지 | 봉인을 같이 내리라는 말 없음 | `narrow-360-fit-seal` **통과 103 / 실패 7** |
| 🟠5 | `style.css` 꼬리 절 | **커밋 해시 오기** | `revert 43f8166` 은 그 두 파일을 안 지움 |
| 🟡 | `js/*.js` 머리글 7곳 | `rm` (js/ 는 위생 봉인이 안 봄) | 안 눌러봄 — 감사팀이 그렇게 적었습니다 |

## ★PM 실수 — 오늘 오전에 제가 통과시킨 커밋 안의 오류입니다★

`537f7af` 는 **"되돌리기 안내가 틀렸다" 를 고치려고 쓴 커밋인데 그 안에 또 틀린
해시가 두 개** 있었습니다. PM 이 직접 확인했습니다:

```
style.css:5423  "43f8166 에는 tests/_font-weight.js · font-weight-value-source
                 가 같이 들어 있어..."
                 → git show --name-only 43f8166 = tests/narrow-360-fit-seal.test.js ★하나뿐★
                 → git log --diff-filter=A = 두 파일 다 ★42cd474★ 소속
style.css:5414  "43f8166 에서 이름과 값이 같이 바뀌었습니다" (바닥_임시 → 바닥)
                 → 75343f1 에 바닥_임시 8개 · 42cd474 에 0개
                 → 이름이 바뀐 것도 ★42cd474★
```

**두 곳 다 `42cd474` 여야 합니다.**

**PM 이 게이트 2 에서 무엇을 했나** — 주석만 바뀐 것을 바이트로 증명했고
(`121,964 == 121,964`), `npm test` 218/218 을 봤습니다.
**그런데 그 주석에 적힌 사실이 맞는지는 안 봤습니다.**
확인법은 명령 두 줄이면 됩니다 — `git show --name-only <해시>` ·
`git log --diff-filter=A -- <파일>`. **안 한 것입니다.**

## CLAUDE.md 에 규칙 셋을 넣었습니다 (`3c8a5e6`)

```
① git checkout HEAD~1 -- <파일>  →  <해시>~1 을 쓴다
   HEAD~1 은 "지금 HEAD 의 하나 앞" 이라 뒤에 커밋이 쌓이면 엉뚱한 곳입니다.
   ★오류가 안 납니다.★ 종료 0 · status 깨끗 · 파일 그대로 — 조용한 고장
② 바뀐 파일을 git show --name-only 로 뽑아 하나씩 대조한다
   제일 자주 빠지는 것 — 봉인 파일 · tests/_order.txt 등록 줄
   test-registry · tests-dir-hygiene · html-assets-tracked ★셋 다 초록★ 이라 못 잡습니다
③ 안내 안에 적은 커밋 해시가 맞는지 확인한다   ← PM 실수
```
지난 커밋 메시지는 못 고치므로 여기에 남깁니다.

## 대표 결재 — ★숫자로 나왔습니다★

```
승인대기 5번 (로그인한 폰 캡처)   08-24 부터 ★14일째★
  ⚠️ 파일에는 "(4일째)" 로 적혀 있습니다 — ★10일 낡은 숫자★
  08-24 이후 「확인 못 한 것」에 "로그인 화면" 이 적힌 커밋   ★33건★
  → 캡처 4장이면 33건이 한 번에 닫힙니다
  → PM 이 규정상 영영 못 보는 화면 4개 — 포지션 표 · 주문 패널 · 내 자산 · 레버리지 창
승인대기 12번   A·B 는 이미 두 번 답이 나왔습니다 (c6242d8 · 1918380 실행,
                09-01 4eed5d6 로 껐음). 실제로 남은 건 ★C·D 두 개★
승인대기 4번    SALE_PAUSED=true 가 이미 들어가 있습니다 (37842ed)
                ⚠️ 그 커밋의 "(대표 B)" 가 이 항목의 B 인지 감사팀이 못 가렸습니다
승인대기 항목 수  12개 — 헤딩 12 = 표 12행. ★유실 없음★
파일 최종 수정    09-02 13:54 — 5일째 갱신 안 됨
```

---

# 디자인팀 실측 — 범례 3건 (재기만, 파일 변경 0) · ★PM 이 전부 결정했습니다★

## ★트레이딩뷰를 직접 열어 재니 우리가 정반대였습니다★ (390 실측)

```
트레이딩뷰 390   O · H · L  display:none        ← 버림
                 C          남김
                 변동        금액 + 퍼센트를 ★한 덩어리로★ 남김   한 줄 21px
우리 폰          O H L C    남김
                 금액        display:none        ← ★버림★
```
**정확히 뒤집혀 있었습니다.**

## 자리 계산 (글씨 17px · 줄높이 22.95 · gap 14)

```
₩120,512,100  109.2px  → 라벨 포함 칸 125.2px
360 KRW 상자 max-width 223px  →  125.2×2 + 14 = 264.4 > 223
                                 ★원화는 한 줄에 하나뿐★
4번째 줄 남는 자리 86px       →  금액(74.3) ★또는★ 퍼센트(73.4) 하나만
                                 둘 다는 153.7 이라 불가능
gap 14→8 로 줄여도 252.4 > 223  →  ★간격 조정으로는 절대 안 풀립니다★
```

## PM 결정

| 건 | 결정 | 왜 |
|---|---|---|
| (가) USDT 폰 금액 켜기 | **승인** | 높이 증가 **0px**, 잃는 것 없음 |
| (나) 원화 폰 | **★안2 (트레이딩뷰와 동일)★** | 대표 지시 "트레이딩뷰 시스템을 따라간다 이상." |
| (다) 원화 이름표 ₩ 떼기 | **승인** | 지표 계산값이지 가격이 아님. 잘림 117 → 88px |
| (라) 폰에서 `(12,26,9)` 감추기 | **승인** | 잘림 **67 → 1px**. 값이 아니라 설정 표시 |
| (마) `…` 로 자르는 방식 | **그대로** | 이름표는 겹쳐 쌓이는 자리라 아래로 밀 데가 없음 |

**안2 가 3건(주 차트 얇아짐)까지 같이 풉니다** — 따로 고칠 게 없어집니다:
```
360 KRW · 아래 칸 8개 · 십자선 짚는 중
  지금   주 칸 166px 중 십자선 줄이 ★57.1%★ 를 덮음 · 남는 캔들 자리 39.5px
  안2    97.8 → 47.9px   →   덮는 비율 ★28.9%★ (절반)
```

⚠️ **"기능 축소" 로 보이는 대목이라 디자인팀이 PM 결정을 요청했고, PM 이 정했습니다.**
파일 주석의 *"우리가 트레이딩뷰보다 한 가지 더 한다"* 는 문구도 같이 고치게 했습니다
— 더 하는 게 아니라 **기준과 달랐던 것**입니다.

## 범례 잘림 실측 (MACD 하나만 켠 상태)

```
폭    통화    잘림    1번값  2번값  3번값
360  USDT     67     OK    +1 ⚠   +67 ❌
360  KRW     117     OK   +31 ❌  +117 ❌
375  KRW     113     OK   +27 ❌  +113 ❌
390  KRW     117     OK   +31 ❌  +117 ❌
768 · 1440 · 1920      ★전부 0★
```
**폰 6경우 전부에서 MACD 3번째 값을 못 읽습니다. 원화는 2번째까지 못 읽습니다.**
지표 8개를 켠 360 KRW 에서는 이름표 8개 중 3개가 잘렸습니다.

## 주 차트 바닥값 (디자인팀 실측)

```
360×900 KRW   아래 칸 0→8 : 600 → 454 → 365 → 305 → 261 → 229 → 203 → 183 → ★166★
              9번째는 setOn 이 ★false★ 를 돌려주고 안 켜집니다 (알림줄도 뜸)
1440×900 KRW  1→7 : 789 → 634 → 530 → 455 → 399 → 355 → ★319★
```
보고돼 있던 "161px" 은 **360 · 아래 칸 8개** 값과 사실상 같습니다(실측 166).
`js/chart-indicator-room.js` 의 문턱은 **제대로 동작합니다** — 조용히 무시하지 않고
`false` 를 돌려줍니다. ⚠️ PM 이 예전에 제안했던 "칸 높이 N px 이면 배지 N 개" 같은
고정 규칙을 **안 만든 것이 맞았습니다.**

## 별건 — 표기 어긋남 (안 고쳤습니다)

```
.tl-ohlc         −₩19,500     (부호가 ₩ 앞)
.tl-kit-plabel    ₩-43,289     (₩ 가 부호 앞)
```
같은 화면에 두 가지가 있습니다. `js/chart-indicator-kit.js` 의 숫자 표기 함수라
**이번 건과 섞으면 무엇 때문에 깨졌는지 못 가려서** 별건으로 뒀습니다.

---

# 지금 상태 (13:4x)

```
도는 팀   수리팀(.tl-ind-bar · js/chart-indbar-room.js 새로 만드는 중)
          차트팀(3단계 반려분 + 전체화면 실측 + 트레이딩뷰 차이표)
          기록팀(_order.txt 53곳 + 새 봉인)
          디자인팀(범례 결정 1~4 실행)
색인      css/chart-toprow.css · js/chart-toprow.js · tests/chart-toprow-seal.test.js
          (차트팀 것. PM 이 git commit --only 로 갈라 커밋 중)
미추적    js/chart-indbar-room.js (수리팀)
          tests/chart-indbar-axis-clear.test.js · order-revert-comment-seal.test.js
          · palette-radius-shadow-seal.test.js
```

## ⚠️ ★커밋 사고 방지 — index.html 을 지금 올리면 라이브 404 입니다★

`index.html` 이 **아직 git 에 없는 파일 둘**을 부르고 있습니다:
```
:55    css/chart-toprow.css        (차트팀 — 색인에는 있음, 커밋 전)
:1497  js/chart-toprow.js          (차트팀 — 색인에는 있음, 커밋 전)
:1507  js/chart-indbar-room.js     (수리팀 — ★색인에도 없음★)
```
**`index.html` 만 먼저 커밋하면 clone 한 PC 와 라이브에서 빈 링크가 됩니다.**
전형적인 조용한 고장이고, 이 프로젝트는 집·회사 PC 이동이 실제로 있습니다.
**세 파일이 다 게이트 2 를 통과하기 전에는 `index.html` 을 안 올립니다.**

## 부하 — SLOW 판정, 새 팀은 안 붙입니다

```
우리 크롬 47개 / 3.92GB   (전체 57 — 나머지는 대표님 개인 크롬, 손대지 않음)
CPU 41% · 여유 메모리 51.3GB · 로컬 서버 200
```
세션 목록은 `chart-fs` · `chart-tv` · `chart-tvw` · `repair-indbar` **넷 다
도는 팀 것**입니다. 47개는 닫힌 세션이 남긴 것으로 보이나, **측정 중인 세션을
끊을 수 없어 지금 청소하지 않습니다.** 규칙대로 **새 팀만 안 붙입니다.**

---

# 대표 지시 「싹 배포해」 — ★남김없이 올렸습니다★

작업트리가 **비었습니다.** 검수 끝난 것도, 팀이 만지던 것도 전부 커밋됐습니다.

```
80875ed  폰 십자선이 트레이딩뷰와 정반대였던 것 (디자인팀)
ae13080  차트 맨 윗줄을 한 줄로 — 3단계 (차트팀)
45457d7  되돌리기 안내 50곳 + 새 봉인 2개 (기록팀)
c3dfca3  지표 칩 줄이 원화 가격축을 덮던 것 (수리팀)
3c8a5e6  CLAUDE.md 되돌리기 규칙 셋
8efb985  P1 십자선 OHLC 가 가격축을 덮던 것 (수리팀)
7285334  봉인을 명단 → 패턴 훑기로 (기록팀)
537f7af  style.css 되돌리기 안내 정정 (디자인팀)
42cd474 · 43f8166  더 큰 폰이 더 작은 글씨였던 것
운영기록 4건
```

## ⚠️ 중간에 ★사용량 한도★ 로 두 팀이 끊겼습니다

차트팀·디자인팀이 작업 도중 끊겼습니다(PM 쪽 한도, 팀 잘못 아님).
**두 팀 다 문법이 안 깨진 채로 멈춰 있었고**(`node --check` 10개 전부 통과),
**작업트리에 남은 것을 그대로 이어받게** 다시 붙였습니다. 처음부터 다시 하지
않게 **"여러분이 마지막에 남긴 말" 과 "그 사이에 배포된 3건"** 을 배정문에 적었습니다.

## 차트팀 3단계 — 게이트 2 를 ★두 번 반려★ 했고 두 번 다 눌러서 잡았습니다

**1차 — 되돌리는 방법이 틀렸습니다.** 안내대로 누르니 `ENOENT` 예외.
**2차 — 이번 변경이 만든 회귀.** 폰 전체화면에서 나가는 단추가 화면 밖 434px,
`›` 표시도 꺼져 있었습니다. **폰엔 Esc 가 없습니다.**

**차트팀이 고치면서 스스로 함정 셋을 더 찾았습니다** — 전부 눌러보다 나온 것입니다.
```
① git rm 은 삭제가 저절로 색인에 오르는데 ★손으로 고친 index.html · _order.txt 는 안 오릅니다★
② git rm 이 ★색인에만 올라간 파일★ 을 거절합니다 (지금 저장소가 정확히 그 상태였음)
③ "주석 8줄 + 등록 1줄 = 9줄" 인데 ★주석 마지막 줄에도 파일 이름이 있어★
   이름이 처음 보이는 줄에서 멈추면 등록 줄이 남습니다 → ★10줄★
```
**셋 다 봉인에 못 박았습니다.**

### PM 이 진짜 전체화면을 눌러 직접 확인 (390)
```
전체화면 요소   ★panel chart-panel★   (흉내 아님)
data-more       ★1★                   › 표시가 켜졌습니다
› 힌트          display:flex  x=349,y=7   ★화면 안★
끝까지 밀면     data-more ★사라짐★    (더 없는데 있다고 안 함)
                나가기 단추 ★x=205 · 보임 true★
나온 뒤         부모 tlc-toprow · offsetTop ★401★ · 374x44   제자리
```

## 디자인팀 — ★우리가 트레이딩뷰와 정반대★ 였습니다

```
트레이딩뷰 390   O·H·L 버리고  C + 변동(금액+퍼센트) 남김
우리 (수정 전)   O·H·L 남기고  ★금액을 버림★
```
360×640 원화에서 그 줄이 **98px** 로 부풀어 주 캔들 칸(103px)의 **95.2%** 를
덮었습니다 — **캔들이 사실상 안 보였습니다.**

### PM 실측 (라이브 배포 후)
```
폭 390  합친 줄 ★44px★ · 머리 263 → ★122px★
        USDT  OHLC 23px · 침범 -48.6      KRW  OHLC ★48px★ · 침범 -5.5
        칩 침범 -206.6 / -184.6 · 칩 겹침 -0.1 · 가로 밀림 0
폭 1440 합친 줄 40px · 머리 107 → ★61px★
        OHLC 48px · 침범 -5.5 · 칩 침범 -486.6 · 칩 겹침 -0.1 · 가로 밀림 0
콘솔 오류 ★0건★
```
격리 검증 — 차트팀 **224/224 · 12,073** · 디자인팀 **224/224 · 12,073**.
수정 금지 12개 해시 **12/12** (건마다).

⚠️ **최근 체결이 22 · 18 줄로 읽힌 순간이 있었습니다.** 페이지를 연 직후이고
몇 초 뒤 30줄이 찼습니다(같은 측정에서 30 · 30 도 나왔습니다). 점검팀이 앞서
여섯 폭 전부 30줄을 확인했습니다. **조용한 고장이 아니라 로딩 중입니다.**

## PM 결정 2건 (디자인팀이 올린 것)

**① 360 USDT 에서 0.7px 차이로 두 줄이 됩니다 — ★따로 배정★**
상자 241.0px, 한 줄로 펴면 241.7~241.8px. 변동 금액이 한 자리면 23px,
두 자리면 48px 로 오갑니다. **디자인팀이 스스로 정정했습니다** —
결정 (가)의 근거였던 "높이 증가 0px" 은 **금액이 짧을 때만** 맞았습니다.
**다만 두 줄이어도 48px 이고 수정 전(73px)보다 낫습니다. 배포를 늦출 이유가
없어 별건으로 뺐습니다.**

**② `₩` 감추기를 폰으로 좁힐지 — ★전 폭 유지★**
근거("지표 계산값이지 가격이 아님")가 폭과 무관하고 768 이상에서도 해가 없습니다.
**규칙을 두 벌로 만들지 않습니다.**

## 확인 못 한 것

  · **로그인 화면** — 오늘 배포 전부에 해당합니다. **승인대기 5번이 14일째**
  · **실기기, 특히 iOS** — 사파리는 `div` 전체화면이 안 돼 대체 방식으로 갑니다.
    **iOS 는 Esc 도 없어 `›` 표시가 더 중요한데 진짜 아이폰에서 못 눌러 봤습니다**
  · **PM 은 지표 이름표를 화면에 못 띄웠습니다.** 잘림 숫자는 디자인팀 실측 인용
  · **손가락 스와이프** — 밀기는 `scrollLeft` 를 코드로 넣어 쟀습니다
