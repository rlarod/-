# 운영기록 2026-08-24

## 발견
- 총 건수: 3
- 등급별: P1 1 / P2 1 / P3 1

| 등급 | 내용 | 처리 |
|---|---|---|
| P1 | `supabase/cleanup-test-data.sql:21` — 파일을 열어 Run 만 눌러도 회원 전원 삭제 | 봉인 완료 (739b349) |
| P2 | 폰에서 주문 패널이 화면을 1,148px 차지해 문서가 4,234px | 하단 시트로 전환 (150a8a4) |
| P3 | `tests/guest-leaderboard.test.js` 만 `전체 통과 ✅` 를 안 찍음 | 미처리 (실패는 아님) |

## 게이트 1 (발견 → 배정)
- 통과: 2
- 반려: 0
- 반려 사유: 없음

## 게이트 2 (완료 → 승인)
- 통과: 2
- 반려: 0
- 반려 사유: 없음
- 3회 반려로 대표 보고한 건: 없음

### 통과 근거 (본부장 직접 확인)

**739b349 — 전원삭제 SQL 봉인**
- `supabase/cleanup-test-data.sql` 에서 주석 아닌 줄을 전부 뽑아낸 결과 **SELECT 1개뿐**.
  DELETE·UPDATE·TRUNCATE·DROP **0건**. 파일을 통째로 Run 해도 숫자만 나옴
- `supabase/*.sql` 43개를 본부장이 따로 훑음 (함수 본문 `$$…$$` 제외한 최상위만)
  → 위험 문장 0건. `schema-trades-dedupe.sql` 의 DELETE 는 `rn > 1` 중복제거라 정상

**150a8a4 — 모바일 3단계 하단 시트**

| 폭 | 문서높이 | 가로밀림 | 하단바 | 주문칼럼 |
|---|---|---|---|---|
| 360 | **3,165** (전 4,234) | 0 | 73px | none |
| 375 | 3,281 | 0 | 73px | none |
| 390 | 3,281 | 0 | 73px | none |
| 768 | 4,512 | 0 | 없음 | flex (그대로) |
| 1440 | 3,263 | 0 | 없음 | flex (그대로) |
| 1920 | 2,325 | 0 | 없음 | flex (그대로) |

- 시트 열기 실제로 눌러 확인 — `position:fixed`, 화면 56~800 안, 내부 스크롤 1,233/744
- 캡처: `shots/gate2-360-sheet.png`

## 팀별 처리
- 점검팀: 0
- 수리팀: 1 (하루 3건 제한 — 여유 2건)
- 디자인팀: 1

## 대표 승인 요청
- 계급 SQL `supabase/schema-rank-1000.sql` 실행 (대기 중)
- 회원 삭제 SQL `supabase/회원삭제-2026-08-24.sql` 실행 (대기 중)
- TL 계산식 확정 — 계산식 변경은 대표 확인 항목

## 확인 못 한 것
- **로그인 상태의 폰 화면.** 비밀번호 입력 금지 규칙이라 비회원으로만 봄.
  로그인하면 시트 안에 미체결 지정가 카드·무료충전이 추가돼 내용이 1,233px 보다 길어짐.
  시트는 내부 스크롤이라 잘리지는 않지만 실제로는 못 봄
- **실기기(아이폰) 홈바 영역.** 에뮬레이터로만 확인
- **SQL 을 실제로 실행해 보지 않음.** 정적 분석으로만 봉인 확인 (실행 금지 규칙)
- **주말 사이 계좌가 초기화된 원인.** Mang9 510,669 → 100,000. 두 번 보고했으나 답 없음

## 수정 금지 파일 해시
- 결과: 12/12 일치 (작업 전·후 모두)
- 불일치 시 파일명: 없음

## 배포
- `91248cd..150a8a4` → main 푸시 완료. Vercel 자동 반영
