# 운영기록 2026-09-08

## 시작 상태 (아침 점검)

```
HEAD            b75dfab (09-07 16:3x)
안 올라간 커밋    ★0건★ — 어제 검수분은 전부 배포돼 있습니다
수정 금지 파일    ★12/12 일치★
라이브           200
★로컬 서버★     ★000 (죽어 있었음)★ → npm run dev 재기동 → 200
크롬 우리것       0개 · CPU 1% · 여유 메모리 56.4GB · 세션 0개 · VERDICT=GO
```

**로컬 서버가 또 죽어 있었습니다.** CLAUDE.md 의 EMFILE 건이고 하룻밤 방치라
당연합니다. **팀을 붙이기 전에 살렸습니다** — 안 그랬으면 세 팀이 전부
"요소를 못 찾는다" 로 헤맸을 자리입니다. (09-07 에도 같은 일이 있었습니다.)

## ⚠️ 작업트리가 빨간 채로 시작했습니다 — 기록팀이 도중에 끊겼습니다

어제 마지막 배정(기록팀)이 **완료 보고 없이 세션이 끝났습니다.** 해둔 작업은
작업트리에 그대로 남아 있었습니다.

```
 M tests/_order.txt                          되돌리기 주석 손보는 중
 M tests/narrow-360-fit-seal.test.js         HEAD → 43f8166~1 로 정정  ← 잘한 것
 M tests/palette-radius-shadow-seal.test.js  var() 풀기 작업 중
?? tests/revert-instruction-head-seal.test.js  새 봉인(HEAD~ 금지)
```

**PM 이 먼저 가른 것 — ★커밋된 HEAD 는 초록입니다★.**
```
작업트리        225개 실행 / 224개 통과 / ★1개 실패★ (검사 12,096 / 실패 6)
                실패: tests/order-revert-comment-seal.test.js
별도 worktree 로 뜬 HEAD 단독   ★통과 21 / 실패 0★
```
**빨간 것은 미완성분뿐이고 라이브는 멀쩡합니다.**
`js/` `css/` `style.css` 는 한 글자도 안 바뀐 상태였습니다.

⚠️ **완료 보고가 없으므로 커밋하지 않았습니다** — CLAUDE.md 의
"보고 없이 mtime 만 보고 커밋하지 않습니다" 그대로입니다.
**같은 팀을 새로 붙여 이어서 마치게 했습니다** (해둔 것·실패 6건의 실제 내용·
196행은 손대지 말 것을 배정문에 그대로 적었습니다).

## 발견
- 총 건수: 1
- 등급별: P1 0 / P2 0 / **P3 1**

### [P3] 차트 스타일 창이 사실과 다른 상태를 보여줍니다 — ★PM 이 재현★

차트팀이 어제 보고한 건입니다. **게이트 1 대로 PM 이 직접 재현했습니다.**

```
차트 스타일 창(#tl-cs-modal) 의 「보이기」 체크박스   ★checked = false★
실제 차트                                            격자 가로·세로 ★둘 다 true★ · #1D273B
```

**⚠️ 다만 "저장하면 격자선이 꺼진다" 는 ★PM 이 재현 못 했습니다★.**
창을 열고 「저장」을 실제로 눌렀는데:
```
저장 전   가로 true · 세로 true
저장 후   가로 ★true★ · 세로 ★true★ · localStorage['chart-style'] = ★null★
```
격자선이 안 꺼졌고 저장도 안 됐습니다.

**게이트 1 규칙대로 본 것만 사실로 채택합니다** —
✅ 창이 사실과 다르게 보이는 것은 사실 / ❓ 저장 시 피해는 미확인.
**배정문에 "고치기 전에 먼저 가르고, 피해 경로가 없으면 없다고 보고하라"** 고 적었습니다.

같이 발견 — `App.ChartStyle.getStateForTest()` 가 **부르면 그 자리에서 오류**를
냅니다(창을 연 뒤에도). 이름이 `ForTest` 인데 못 씁니다. 같이 배정했습니다.

## 게이트 1 (발견 → 배정)
- 통과: 1  (스타일 창 불일치 — 재현됨)
- 반려: 0
- 반려 사유: 없음
- ⚠️ 재현은 됐으나 **원인 설명 일부는 채택하지 않았습니다** (저장 시 피해 미확인)

## 게이트 2 (완료 → 승인)
- 통과: 0
- 반려: 0
- 반려 사유: 없음
- 3회 반려로 대표 보고한 건: 없음

## 팀별 처리
- 점검팀: 1 (진행 중 — 어제 13건 배포분 라이브 회귀 · 여섯 폭 × 두 통화)
- 조사팀: 0
- 수리팀: 0
- 디자인팀: 0
- 기록팀: 1 (진행 중 — 어제 중단분 이어서 · 작업트리 빨강 해소)
- 차트팀: 1 (진행 중 — 스타일 창 불일치 + 차트 전체 높이)
- 감사팀: 0

## 대표 승인 요청
- **로그인한 폰 캡처 — 08-24 부터 ★15일째★**
  포지션 표 · 주문 패널 · 내 자산 · 레버리지 창 4개.
  PM 은 규정상 로그인하지 않아 **영영 못 보는 화면**입니다.
  08-24 이후 「확인 못 한 것」에 "로그인 화면" 이 적힌 커밋이 **33건**(감사팀 실측).
  ⚠️ **어제 캔들 간격을 1.2 → 6px 로 바꿨습니다.** 진입가·청산가·미체결·TP/SL
  가로선이 새 배율에서 어떻게 보이는지 **아무도 못 봤습니다.**
  가로선이라 배율과 무관할 것으로 보지만 **확인 안 한 추정입니다.**
- 세로 도구막대 52×38·27개 vs 38×38·17개 (09-03 "줄인다" 결정 뒤 재확인 요청)

## 확인 못 한 것
- **로그인 화면 전부** — 위 승인 요청 참조
- **실기기(특히 iOS)** — 어제 배포한 전체화면 동작은 데스크톱 크롬을 폰 폭으로
  줄여서만 확인했습니다. iOS 사파리는 `div` 전체화면이 안 돼 대체 방식으로 갑니다
- **`getStateForTest()` 오류 원인** — 차트팀 배정 중

## 수정 금지 파일 해시
- 결과: **12/12 일치**
- 불일치 시 파일명: 없음

---

# 점검팀 라이브 회귀 — ★어제 13건이 12개 조합 전부 그대로★ · 새 발견 2건

십자선은 **진짜 마우스**로 켜고 `tl-ohlc-live` 를 확인한 뒤 잰 값입니다.

```
barSpacing 6.0 · rightOffset 10 · 격자선 #1D273B/보임 · 가로 밀림 0    ★12/12 일치★
.tlc-toprow  폰 44px / 768↑ 40px          차트 머리  폰 122px / 768↑ 61px
column-gap   폰 10px / 768↑ 14px
가격축 침범  .tl-ohlc · .tl-ind-bar ★12/12 전부 음수★ (제일 빠듯한 곳 -5.5)
칩 겹침      12/12 전부 ≤ 0
폰 십자선    O·H·L 감춤 · ★변동 금액 보임★ (360/375/390 × 두 통화 전부)
             예: C79,288.00 | +18.50(+0.02%)   /   C₩118,956,900 | +₩24,750(+0.02%)
```

⚠️ **점검팀이 PM 값 하나를 정정했습니다** — 어제 PM 이 "768 이상 48px" 이라 적었는데
**1440·1920 USDT 는 22.9px(한 줄)** 입니다. 자리가 넉넉해 한 줄에 다 들어갑니다.
글자가 안 잘리고 침범·겹침도 음수라 **이상 없음**. **PM 표기가 부정확했던 것입니다.**

## 어제 못 보고 넘어간 둘 — 둘 다 확인됐습니다

**① 폰 전체화면** (360/375/390 각 2회) — `›` 보임 · 끝까지 밀면 사라짐 ·
나가기 단추가 화면 안(x 205~249)에 들어옴 · 나온 뒤 제자리 · **전체화면 안에서
시간 단위 바뀜**(1분 → 5분 확인, barSpacing 6 유지).

**② 회원이 맞춰 둔 화면** — 시간축 드래그로 확대(6.6 → 9.286) 후 **15초 방치에
5회 전부 9.286 유지**. 봉 간격 바꾸면 6.0 복귀.
**★종목 전환은 아무도 안 재봤던 항목입니다★** — 비트코인 → 나스닥 → 삼성전자 →
비트코인 전부 **6.0 · rightOffset 10 · 격자 정상**.

## 회귀 3종 · 콘솔 — 여섯 폭 전부 정상

```
마크가격    여섯 폭 전부 화면 안 (제일 빠듯한 390 도 여유 27.6px)
매수·매도   11초에 10~11회 변동
최근 체결   여섯 폭 전부 ★30줄★
콘솔 오류   여섯 폭 전부 ★0건★
```
⚠️ 1440 에서 첫 측정이 25줄이었으나 **차는 중**이었고 이후 11회 전부 30줄.
호가창과 신호 횟수를 대조해 **조용한 고장 아님**으로 결론냈습니다.

## 바이낸스 대조 (점검팀이 같이 했습니다)

```
마크가격 $0.005 차이 · 현재가 1틱 · 24H 고가/저가 ★완전 일치★
펀딩비 · 다음 정산 시각 · 수수료(0.02/0.05) ★일치★
스프레드 ₩150 고정, 32회 중 28회가 최우선 호가와 정확히 일치
```
로드 직후 ₩9,900 벌어진 순간을 봤으나 재측정 결과 정상 범위 —
**발견으로 올리지 않았습니다.** 부풀리지 않은 판단이 맞습니다.

---

## 발견 [P3] 거래량 마지막값 배지가 원화 가격축 눈금을 덮습니다 — ★PM 재현★

**가격축이 가려지는 건이 ★세 번째★ 입니다.**
```
09-07  .tl-ohlc     십자선 OHLC 줄        ★대표가 캡처로 잡으심★   8efb985
09-07  .tl-ind-bar  지표 칩 줄                                    c3dfca3
09-08  ★이번★      거래량 마지막값 배지 (★캔버스에 그려짐★)
```

**PM 이 768×1024 · KRW 로 재현하고 캡처를 눈으로 확인했습니다** —
초록 배지 `63.55` 가 가격축 안, `₩118,620,000` 눈금 줄 위에 올라앉아 있습니다.

점검팀 실측 (라이브 768, DPR 1):
```
1회차  배지 9.25  x 585~655 / 눈금 ₩118,560,000 x 597~750  → ★겹침 58px★
2회차  배지 13.72 가 ₩118,620,000 을 덮어 ★`20,000` 만 읽힘★
360(13.25) · 390(15.62) 은 배지가 마지막 눈금보다 아래라 안 겹침
1440 · 1920 은 캡처 하단 밖이라 ★확인 못 함★
```

**PM 이 같이 뽑은 값** — 시리즈 `candle · volume · lines`,
오른쪽 가격축 폭 166px · `scaleMargins {top 0.06, bottom 0.24}`.
**거래량이 캔들과 같은 오른쪽 축을 쓰고 있습니다.** 다만 이건 PM 추정이고
**원인 확정은 조사팀에 배정했습니다** — 앞 두 건과 달리 **캔버스라 DOM 좌표가
없어** 픽셀로 재야 합니다.

⚠️ **눈금은 시세따라 움직입니다.** "지금 안 겹친다" 가 "안전하다" 는 뜻이 아닙니다.

## 발견 [P3] 전체화면에 들어가면 도구막대가 맨 왼쪽으로 되돌아갑니다

```
390  진입 직전 scrollLeft ★607(끝)★ → 진입 직후 ★0★ → 나가기 단추 427px 밖
375  442px 밖        360  457px 밖
```
폰에서 전체화면 단추를 누르려면 막대를 끝까지 밀어야 하는데, **들어가는 순간
막대가 처음으로 돌아가 나가려면 또 밀어야 합니다.** 폰엔 Esc 가 없습니다.
**다만 `›` 힌트가 보이고 밀면 단추가 들어옵니다 — 나가는 길은 있습니다.**
어제 합격 기준("밀면 들어오는가")은 충족하므로 **설계대로인지 아쉬운 점인지는
PM 판단** 이라고 점검팀이 적었습니다. **조사팀에 원인 확인만 배정했습니다.**

## 점검팀이 확인 못 한 것

- **휠(마우스 스크롤) 확대** — `agent-browser mouse wheel` 이 보내는 휠 이벤트가
  **`clientX:0, clientY:0`** 으로 떨어져 차트가 휠을 못 받습니다.
  드래그 이동·시간축 드래그 확대는 진짜 마우스로 정상 동작했습니다.
  **휠 확대가 되는지 안 되는지는 결론 못 냈습니다** — 실제 마우스로 굴려봐야 합니다
- **폰 터치 스와이프 · 핀치 확대** — 터치 에뮬레이션을 안 켠 상태
- **로그인 화면 전부**
- **1440 · 1920 의 거래량 배지 겹침 여부**
- **나스닥 · 삼성전자 가격이 실제와 맞는지** — 이번 범위 밖
