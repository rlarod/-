# 운영기록 2026-09-08

## 시작 상태 (아침 점검)

```
HEAD            b75dfab (09-07 16:3x)
안 올라간 커밋    ★0건★ — 어제 검수분은 전부 배포돼 있습니다
수정 금지 파일    ★12/12 일치★
라이브           200
★로컬 서버★     ★000 (죽어 있었음)★ → npm run dev 재기동 → 200
크롬 우리것       0개 · CPU 1% · 여유 메모리 56.4GB · 세션 0개 · VERDICT=GO
```

**로컬 서버가 또 죽어 있었습니다.** CLAUDE.md 의 EMFILE 건이고 하룻밤 방치라
당연합니다. **팀을 붙이기 전에 살렸습니다** — 안 그랬으면 세 팀이 전부
"요소를 못 찾는다" 로 헤맸을 자리입니다. (09-07 에도 같은 일이 있었습니다.)

## ⚠️ 작업트리가 빨간 채로 시작했습니다 — 기록팀이 도중에 끊겼습니다

어제 마지막 배정(기록팀)이 **완료 보고 없이 세션이 끝났습니다.** 해둔 작업은
작업트리에 그대로 남아 있었습니다.

```
 M tests/_order.txt                          되돌리기 주석 손보는 중
 M tests/narrow-360-fit-seal.test.js         HEAD → 43f8166~1 로 정정  ← 잘한 것
 M tests/palette-radius-shadow-seal.test.js  var() 풀기 작업 중
?? tests/revert-instruction-head-seal.test.js  새 봉인(HEAD~ 금지)
```

**PM 이 먼저 가른 것 — ★커밋된 HEAD 는 초록입니다★.**
```
작업트리        225개 실행 / 224개 통과 / ★1개 실패★ (검사 12,096 / 실패 6)
                실패: tests/order-revert-comment-seal.test.js
별도 worktree 로 뜬 HEAD 단독   ★통과 21 / 실패 0★
```
**빨간 것은 미완성분뿐이고 라이브는 멀쩡합니다.**
`js/` `css/` `style.css` 는 한 글자도 안 바뀐 상태였습니다.

⚠️ **완료 보고가 없으므로 커밋하지 않았습니다** — CLAUDE.md 의
"보고 없이 mtime 만 보고 커밋하지 않습니다" 그대로입니다.
**같은 팀을 새로 붙여 이어서 마치게 했습니다** (해둔 것·실패 6건의 실제 내용·
196행은 손대지 말 것을 배정문에 그대로 적었습니다).

## 발견
- 총 건수: 1
- 등급별: P1 0 / P2 0 / **P3 1**

### [P3] 차트 스타일 창이 사실과 다른 상태를 보여줍니다 — ★PM 이 재현★

차트팀이 어제 보고한 건입니다. **게이트 1 대로 PM 이 직접 재현했습니다.**

```
차트 스타일 창(#tl-cs-modal) 의 「보이기」 체크박스   ★checked = false★
실제 차트                                            격자 가로·세로 ★둘 다 true★ · #1D273B
```

**⚠️ 다만 "저장하면 격자선이 꺼진다" 는 ★PM 이 재현 못 했습니다★.**
창을 열고 「저장」을 실제로 눌렀는데:
```
저장 전   가로 true · 세로 true
저장 후   가로 ★true★ · 세로 ★true★ · localStorage['chart-style'] = ★null★
```
격자선이 안 꺼졌고 저장도 안 됐습니다.

**게이트 1 규칙대로 본 것만 사실로 채택합니다** —
✅ 창이 사실과 다르게 보이는 것은 사실 / ❓ 저장 시 피해는 미확인.
**배정문에 "고치기 전에 먼저 가르고, 피해 경로가 없으면 없다고 보고하라"** 고 적었습니다.

같이 발견 — `App.ChartStyle.getStateForTest()` 가 **부르면 그 자리에서 오류**를
냅니다(창을 연 뒤에도). 이름이 `ForTest` 인데 못 씁니다. 같이 배정했습니다.

## 게이트 1 (발견 → 배정)
- 통과: 1  (스타일 창 불일치 — 재현됨)
- 반려: 0
- 반려 사유: 없음
- ⚠️ 재현은 됐으나 **원인 설명 일부는 채택하지 않았습니다** (저장 시 피해 미확인)

## 게이트 2 (완료 → 승인)
- 통과: 0
- 반려: 0
- 반려 사유: 없음
- 3회 반려로 대표 보고한 건: 없음

## 팀별 처리
- 점검팀: 1 (진행 중 — 어제 13건 배포분 라이브 회귀 · 여섯 폭 × 두 통화)
- 조사팀: 0
- 수리팀: 0
- 디자인팀: 0
- 기록팀: 1 (진행 중 — 어제 중단분 이어서 · 작업트리 빨강 해소)
- 차트팀: 1 (진행 중 — 스타일 창 불일치 + 차트 전체 높이)
- 감사팀: 0

## 대표 승인 요청
- **로그인한 폰 캡처 — 08-24 부터 ★15일째★**
  포지션 표 · 주문 패널 · 내 자산 · 레버리지 창 4개.
  PM 은 규정상 로그인하지 않아 **영영 못 보는 화면**입니다.
  08-24 이후 「확인 못 한 것」에 "로그인 화면" 이 적힌 커밋이 **33건**(감사팀 실측).
  ⚠️ **어제 캔들 간격을 1.2 → 6px 로 바꿨습니다.** 진입가·청산가·미체결·TP/SL
  가로선이 새 배율에서 어떻게 보이는지 **아무도 못 봤습니다.**
  가로선이라 배율과 무관할 것으로 보지만 **확인 안 한 추정입니다.**
- 세로 도구막대 52×38·27개 vs 38×38·17개 (09-03 "줄인다" 결정 뒤 재확인 요청)

## 확인 못 한 것
- **로그인 화면 전부** — 위 승인 요청 참조
- **실기기(특히 iOS)** — 어제 배포한 전체화면 동작은 데스크톱 크롬을 폰 폭으로
  줄여서만 확인했습니다. iOS 사파리는 `div` 전체화면이 안 돼 대체 방식으로 갑니다
- **`getStateForTest()` 오류 원인** — 차트팀 배정 중

## 수정 금지 파일 해시
- 결과: **12/12 일치**
- 불일치 시 파일명: 없음

---

# 점검팀 라이브 회귀 — ★어제 13건이 12개 조합 전부 그대로★ · 새 발견 2건

십자선은 **진짜 마우스**로 켜고 `tl-ohlc-live` 를 확인한 뒤 잰 값입니다.

```
barSpacing 6.0 · rightOffset 10 · 격자선 #1D273B/보임 · 가로 밀림 0    ★12/12 일치★
.tlc-toprow  폰 44px / 768↑ 40px          차트 머리  폰 122px / 768↑ 61px
column-gap   폰 10px / 768↑ 14px
가격축 침범  .tl-ohlc · .tl-ind-bar ★12/12 전부 음수★ (제일 빠듯한 곳 -5.5)
칩 겹침      12/12 전부 ≤ 0
폰 십자선    O·H·L 감춤 · ★변동 금액 보임★ (360/375/390 × 두 통화 전부)
             예: C79,288.00 | +18.50(+0.02%)   /   C₩118,956,900 | +₩24,750(+0.02%)
```

⚠️ **점검팀이 PM 값 하나를 정정했습니다** — 어제 PM 이 "768 이상 48px" 이라 적었는데
**1440·1920 USDT 는 22.9px(한 줄)** 입니다. 자리가 넉넉해 한 줄에 다 들어갑니다.
글자가 안 잘리고 침범·겹침도 음수라 **이상 없음**. **PM 표기가 부정확했던 것입니다.**

## 어제 못 보고 넘어간 둘 — 둘 다 확인됐습니다

**① 폰 전체화면** (360/375/390 각 2회) — `›` 보임 · 끝까지 밀면 사라짐 ·
나가기 단추가 화면 안(x 205~249)에 들어옴 · 나온 뒤 제자리 · **전체화면 안에서
시간 단위 바뀜**(1분 → 5분 확인, barSpacing 6 유지).

**② 회원이 맞춰 둔 화면** — 시간축 드래그로 확대(6.6 → 9.286) 후 **15초 방치에
5회 전부 9.286 유지**. 봉 간격 바꾸면 6.0 복귀.
**★종목 전환은 아무도 안 재봤던 항목입니다★** — 비트코인 → 나스닥 → 삼성전자 →
비트코인 전부 **6.0 · rightOffset 10 · 격자 정상**.

## 회귀 3종 · 콘솔 — 여섯 폭 전부 정상

```
마크가격    여섯 폭 전부 화면 안 (제일 빠듯한 390 도 여유 27.6px)
매수·매도   11초에 10~11회 변동
최근 체결   여섯 폭 전부 ★30줄★
콘솔 오류   여섯 폭 전부 ★0건★
```
⚠️ 1440 에서 첫 측정이 25줄이었으나 **차는 중**이었고 이후 11회 전부 30줄.
호가창과 신호 횟수를 대조해 **조용한 고장 아님**으로 결론냈습니다.

## 바이낸스 대조 (점검팀이 같이 했습니다)

```
마크가격 $0.005 차이 · 현재가 1틱 · 24H 고가/저가 ★완전 일치★
펀딩비 · 다음 정산 시각 · 수수료(0.02/0.05) ★일치★
스프레드 ₩150 고정, 32회 중 28회가 최우선 호가와 정확히 일치
```
로드 직후 ₩9,900 벌어진 순간을 봤으나 재측정 결과 정상 범위 —
**발견으로 올리지 않았습니다.** 부풀리지 않은 판단이 맞습니다.

---

## 발견 [P3] 거래량 마지막값 배지가 원화 가격축 눈금을 덮습니다 — ★PM 재현★

**가격축이 가려지는 건이 ★세 번째★ 입니다.**
```
09-07  .tl-ohlc     십자선 OHLC 줄        ★대표가 캡처로 잡으심★   8efb985
09-07  .tl-ind-bar  지표 칩 줄                                    c3dfca3
09-08  ★이번★      거래량 마지막값 배지 (★캔버스에 그려짐★)
```

**PM 이 768×1024 · KRW 로 재현하고 캡처를 눈으로 확인했습니다** —
초록 배지 `63.55` 가 가격축 안, `₩118,620,000` 눈금 줄 위에 올라앉아 있습니다.

점검팀 실측 (라이브 768, DPR 1):
```
1회차  배지 9.25  x 585~655 / 눈금 ₩118,560,000 x 597~750  → ★겹침 58px★
2회차  배지 13.72 가 ₩118,620,000 을 덮어 ★`20,000` 만 읽힘★
360(13.25) · 390(15.62) 은 배지가 마지막 눈금보다 아래라 안 겹침
1440 · 1920 은 캡처 하단 밖이라 ★확인 못 함★
```

**PM 이 같이 뽑은 값** — 시리즈 `candle · volume · lines`,
오른쪽 가격축 폭 166px · `scaleMargins {top 0.06, bottom 0.24}`.
**거래량이 캔들과 같은 오른쪽 축을 쓰고 있습니다.** 다만 이건 PM 추정이고
**원인 확정은 조사팀에 배정했습니다** — 앞 두 건과 달리 **캔버스라 DOM 좌표가
없어** 픽셀로 재야 합니다.

⚠️ **눈금은 시세따라 움직입니다.** "지금 안 겹친다" 가 "안전하다" 는 뜻이 아닙니다.

## 발견 [P3] 전체화면에 들어가면 도구막대가 맨 왼쪽으로 되돌아갑니다

```
390  진입 직전 scrollLeft ★607(끝)★ → 진입 직후 ★0★ → 나가기 단추 427px 밖
375  442px 밖        360  457px 밖
```
폰에서 전체화면 단추를 누르려면 막대를 끝까지 밀어야 하는데, **들어가는 순간
막대가 처음으로 돌아가 나가려면 또 밀어야 합니다.** 폰엔 Esc 가 없습니다.
**다만 `›` 힌트가 보이고 밀면 단추가 들어옵니다 — 나가는 길은 있습니다.**
어제 합격 기준("밀면 들어오는가")은 충족하므로 **설계대로인지 아쉬운 점인지는
PM 판단** 이라고 점검팀이 적었습니다. **조사팀에 원인 확인만 배정했습니다.**

## 점검팀이 확인 못 한 것

- **휠(마우스 스크롤) 확대** — `agent-browser mouse wheel` 이 보내는 휠 이벤트가
  **`clientX:0, clientY:0`** 으로 떨어져 차트가 휠을 못 받습니다.
  드래그 이동·시간축 드래그 확대는 진짜 마우스로 정상 동작했습니다.
  **휠 확대가 되는지 안 되는지는 결론 못 냈습니다** — 실제 마우스로 굴려봐야 합니다
- **폰 터치 스와이프 · 핀치 확대** — 터치 에뮬레이션을 안 켠 상태
- **로그인 화면 전부**
- **1440 · 1920 의 거래량 배지 겹침 여부**
- **나스닥 · 삼성전자 가격이 실제와 맞는지** — 이번 범위 밖

---

# 배포 2건 — 조용한 고장 하나를 잡았습니다

```
5154536  봉 색만 바꿨는데 격자선이 꺼지던 것 (차트팀, P2)
0b21eb8  되돌리기 안내를 못 박는 봉인 셋 (기록팀)
```

## [P2] 봉 색만 바꿨는데 격자선이 꺼졌습니다 — ★PM 이 두 판을 띄워 재현★

차트 스타일 창이 **차트의 지금 값을 안 읽고 `gridV:false` 를 글자로 박아** 두고
있었습니다. 색 스와치를 만지면 `applyAll()` 이 **그 거짓말을 차트에 그대로 씁니다.**

```
                   바꾸기 전              봉 색만 #00aaff 로       결과
고치기 전(3020)    세로 true 가로 true  →  세로 ★false★ 가로 ★false★
고친 뒤  (3000)    세로 true 가로 true  →  세로 true  가로 true
```
**격자선은 손도 안 댔습니다.** 새로고침해도 안 돌아옵니다 — 저장값에 `gridV` 키가
생기면 `chart-view-default.js` 가 "회원 뜻" 인 줄 알고 비켜서기 때문입니다.

⚠️ **어제 배포(`b75dfab`)가 이 고장을 눈에 보이게 만들었습니다.** 하드코딩은
전부터 있었는데 격자선 색이 `rgba(0,0,0,0.06)` 이라 **없어져도 아무도 몰랐습니다.**

### ★PM 이 틀렸던 것 두 가지 — 차트팀이 정정★
```
① PM 이 읽은 체크박스는 ★격자선이 아니라 「봉」 탭의 테두리★ 였고 그건 사실과 맞습니다
   진짜 격자선 칸은 「배경」 탭이고, 거기에 불일치가 있었습니다
   → 결론은 맞았지만 ★PM 이 본 자리가 틀렸습니다★
② PM 이 "저장을 눌렀는데 안 꺼졌다" 고 한 것 — saveNow() 는 바뀐 게 없으면
   저장소를 지우고 그냥 빠져나가고 applyAll() 을 안 부릅니다.
   → ★PM 실측이 틀린 게 아니라, 피해 경로가 "저장" 이 아니라 "색 변경" 이었습니다★
```
**게이트 1 에서 "본 것만 사실로 채택" 하고 추정을 배정문에 사실로 안 적은 것이
맞았습니다** — "피해 경로가 없으면 없다고 보고하라" 고 적었고 차트팀이 다른
경로를 찾아왔습니다.

⚠️ **남는 것** — 어제 이후 이 창에서 봉 색을 바꿔 저장한 회원은 `gridV:false` 가
이미 박혀 있습니다. **창에서 「켜기」를 켜면 돌아옵니다.** 브라우저에만 있는
값이라 **몇 명인지 셀 수 없습니다.**

## 봉인 셋 — ★봉인 자신이 거짓말을 하고 있었습니다★

PM 이 **"225행·280행을 `git rm` 으로 고치세요"** 라고 배정했는데,
**기록팀이 열어보니 그 두 곳은 이미 옳았습니다.** 봉인 정규식이
`git rm -f js/max-margin-safe.js` 안의 `rm js/…` 를 **맨 rm 명령으로 오해**한 것입니다.

**지시대로 고쳤으면 맞는 글자를 틀리게 바꿀 뻔했습니다.**
어제 "53곳 → 50곳" 정정에 이어 **PM 숫자를 받아쓰지 않은 것이 두 번째로 옳았습니다.**

눌러보다 기록도 하나 고쳤습니다 — `tl-market SALE_PAUSED=false` 되돌리기가
"1개 빨강" 이라 적혀 있었는데 실측 **2개**였습니다(`market-sale-stop-server` 가
"서버도 같이 잠그라" 고 일부러 켜 둔 알림).

### PM 이 직접 넣은 돌연변이 2종 (사본에서만)
```
① 고정 해시 → HEAD~1        → 새 봉인이 tests/_order.txt:244 지목 + 고치는 법까지 찍음
② 되돌리기 줄 git rm -f → rm → 280행 정확히 지목
```

### ⚠️ PM 이 찾은 ★좁은 구멍★ — 통과시키고 다음 건으로 돌렸습니다
```
235행 "③ 이 줄과 tests/max-margin-safe-seal.test.js 를 git rm -f 로 삭제"
   → "… 를 rm 로 삭제" 로 바꿈       결과 ★통과 24 / 실패 0★  안 잡힘
```
이유 — [1]은 "문단에 `git rm` 이 있으면 통과" 인데 같은 문단 ②가 충족시키고,
[4]는 한국어 어순(`<파일> 를 rm 로`)이라 `rm <경로>` 정규식에 안 걸립니다.
**검사 쪽 구멍이고 회원 화면과 무관하며 봉인은 큰 폭의 개선이라, 재현 절차를
그대로 붙여 다음 건으로 배정했습니다.**

---

# ★PM 실측 정정 — 차트 높이는 차트팀이 맞았습니다★

PM 이 배정문에 **"768 은 화면 안(715px)"** 이라고 적었는데 차트팀 실측은
**423px 넘침** 이었습니다. **PM 이 라이브에서 다시 쟀습니다:**
```
768×1024   시간축 아래끝 1455 / 뷰포트 1024  →  ★431px 넘침★
1440×900   시간축 아래끝 1737 / 뷰포트  900  →  ★837px 넘침★
```
**차트팀이 맞고 PM 이 틀렸습니다. 16/16 전부 시간축이 첫 화면 밖입니다.**

⚠️ **PM 이 틀린 전제가 하나 더 있었습니다** — "지표를 켜면 아래 칸이 늘어 더
나빠진다" 고 했는데 **반대**입니다. 바깥 높이 1090px 은 고정이고
**지표를 켜면 캔들 칸이 쪼그라듭니다**(6개 켜면 1042 → 355px, 원래의 34%).
넘침은 그대로입니다. **지표는 다른 병이지 이 병을 악화시키지 않습니다.**

## 진짜 차이는 차트 높이가 아니라 ★머리★ 입니다
```
차트 위에 쌓인 것    트레이딩뷰 ★42px★   우리 ★663px★  (15.8배)
  로고 띠 139 · 메뉴 줄 77 · ★공지 게시판 232★ · 종목탭+시세바+여백 215
```
트레이딩뷰는 문서 높이 = 뷰포트 높이이고 **페이지가 아예 안 굴러갑니다.**

**공지 게시판이 머리의 35%** 입니다. 다만 **대표가 회원에게 알리는 자리라
지우는 안은 금지**하고, 접기·옮기기·줄 수 줄이기·**「지금 그대로」** 넷을 각각
재서 캡처와 함께 올리라고 디자인팀에 배정했습니다.

⚠️ **차트팀 안 2(`.main-grid` 높이 바닥 낮추기)는 재지도 말라고 했습니다** —
그 1150px 은 차트가 아니라 **주문창이 1088~1116px 필요**해서 올린 값이고,
낮추면 **주문창에 안쪽 스크롤이 생깁니다.** 주문창은 로그인 화면이라 저희가
못 봅니다. **대표 판단 사항으로 남깁니다.**

---

# 오후 — 배포 4건 더 (오늘 누계 ★9건★). 작업트리 비었습니다

```
053bfea  거래량 배지가 가격축을 덮고 ★가짜 가격선★ 을 그리던 것 (수리팀, ★P1★)
f095961  전체화면에 들어가면 나가기 단추가 화면 밖이던 것 (수리팀, P2)
590c5a5  공지 게시판을 차트 아래로 — 768 넘침 431 → 87px (디자인팀)
cc05799  그림자 13 → 11 곳 (디자인팀)
72a3f9d  봉인의 다단계·한국어 어순 구멍 (기록팀)
e47a61b  승인대기 5번 라벨 정정 + 13·14 추가
```

## ★PM 이 등급을 P3 → P1 로 올렸습니다★ — 거래량 배지

처음에 점검팀도 PM 도 P3 으로 봤습니다. **수리팀 실측을 보고 올렸습니다.**
```
· 차트를 ★가로지르는 가로 점선★ 이 여섯 폭 전부, ★폭의 50.0~50.4%★
  거래량 값 자리에 그려지는데 ★회원은 이걸 가격선으로 읽습니다★
· 가격축 눈금 최대 ★632px★ 가려짐. 12개 조합 중 ★7개가 겹침 100%★
```
**"회원이 잘못된 정보로 판단하게 만드는 것" 이 P1** 이고, 가격이 아닌 자리에
가격선처럼 보이는 선이 그어지는 것은 그 정의에 그대로 해당합니다.

**원인** — `js/chart.js:247` 이 거래량 시리즈에만 `lastValueVisible`·`priceLineVisible`
을 안 껐습니다. **바로 위 캔들에는 껐고 주석에 이유까지 적혀 있습니다** —
*"켜져 있으면 현재가 선과 겹쳐서 화면에 선이 2개로 보였음"*.
**같은 이유로 꺼야 할 것을 거래량에서만 빠뜨려 그 상황이 되살아나 있었습니다.**

**고친 방법은 트레이딩뷰가 답을 줬습니다** — 트레이딩뷰도 축에 배지를 그리지만
**범례에 같은 숫자가 또 있어** 값을 안 잃습니다. 우리는 없었습니다.
이제 칩에 `● 거래량 18.54` 로 붙습니다. **가격축을 한 픽셀도 안 건드립니다.**

PM 실측 — `거래량 visible ★true★`(막대 안 지움) · 배지 false · 점선 false ·
칩 침범 -102.8/-388.8 · 칩 높이 32px(한 줄).

## 전체화면 나가는 길 — ★코드에 `scrollLeft = 0` 이 없었습니다★

조사팀이 **같은 자리에 도로 끼워넣기만 해도 0 이 되는 것**을 보여 확정했습니다.
**DOM 이동 그 자체**가 원인이고, 옮기는 자리 5곳 전부에 처방을 넣었습니다.

PM 이 **진짜 전체화면을 눌러** 확인 —
```
누르기 직전  민 자리 607 · 단추 x=204
전체화면 안  전체화면요소 ★panel chart-panel★ · 민 자리 ★605 유지★(전 0)
             나가기 단추 ★x=205 화면 안★
나온 뒤      605 유지 · 제자리
```

## 공지 게시판을 차트 아래로 — PM 이 경계 네 폭을 콕 집어 쟀습니다

```
폭     넘침 전 → 후    회수    헤더 [공지]   헤더 높이
767     421 → 421      ★0★    0 (그대로)    113 → 113   ← 승인 범위 밖, 안 건드림
768     431 → ★87★    ★344★  0 → ★1★      113 → 113
1100    345 → 113      232     0 → ★1★      139 → 139
1101    345 → 113      232     1 (원래)      139 → 139
1440    713 → 480      233     1             139 → 139
```
**규칙 변경은 4줄뿐**(경계 1100→767, `order` 3개)이고 51줄은 주석입니다.
`.page-left` 가 원래부터 세로 flex 라 **마크업을 한 글자도 안 옮겼습니다.**
**폰(≤700)에서 이미 돌던 순서를 데스크톱으로 넓힌 것**이라 위험이 검증돼 있습니다.

**라이브 확인** — 차트가 첫 화면 위로 올라왔고 헤더에 `[공지] …` 한 줄이 살아
있습니다. 콘솔 오류 0 · 가로 밀림 0.

---

# ★PM 이 틀렸던 것 — 오늘 팀이 여섯 번 정정했습니다★

```
차트팀   ① PM 이 읽은 체크박스는 격자선이 아니라 「봉」 탭 테두리였고 사실과 맞음
         ② "저장하면 꺼진다" — PM 실측은 맞았고 피해 경로가 ★색 변경★ 이었음
         ③ "768 은 화면 안" — 틀림. 라이브 재측정 431px 넘침
         ④ "지표를 켜면 더 나빠진다" — 반대. 바깥 높이 고정, 캔들 칸이 쪼그라듦
기록팀   ⑤ "225·280행을 git rm 으로 고치라" — 그 두 곳은 ★이미 옳았고★
            봉인 정규식이 오해. ★지시대로 했으면 맞는 글자를 틀리게 바꿀 뻔★
디자인팀 ⑥ "공지판 232px·원화가 항상 더 나쁨" — 폭마다 다르고 390·768 은 0px 차이.
            그리고 ★공지판만으로는 1440 이 절대 안 들어옴★(넘침의 27%뿐)
```
**여섯 번 다 팀이 숫자를 받아쓰지 않고 열어봤기 때문에 잡혔습니다.**

## 대표 승인대기 — 15번 추가 (로고 관련 2가지)

```
(가) 901~1100 헤더 공지가 로고에 밀려 14%만 보임   → 안 A(로고 교체 경계 900→1100) / 안 함
(나) 로고 띠 139 + 메뉴 줄 77 = 216px 를 한 줄로   → 안 B(워드마크 64px) / 안 C(심볼 48px) / 안 함
```
**둘 다 로고가 걸려 대표 자산입니다. 지우거나 새로 만드는 안은 하나도 없습니다.**
**메뉴 글자 26px 은 어느 안에서도 안 건드립니다.**

⚠️ (가)의 잘림은 **저희가 만든 것이 아닙니다** — 1101~1279 에 원래 있던 것이
아래로 같이 드러난 것이고, 지금 상태도 그 구간과 같은 정도입니다.

## 확인 못 한 것

  · **로그인 화면 전부** — 오늘 배포 9건 모두에 해당합니다. **승인대기 5번 15일째**
  · **DPR 2(레티나·실제 폰)** — 오늘 측정은 전부 DPR 1
  · **701~767 구간** — 승인 범위 밖이라 공지판이 아직 차트 위입니다(넘침 421px)
  · **광고띠 `order:3`** — 지금 `display:none`(소재 없음)이라 눈으로 확인 못 함
