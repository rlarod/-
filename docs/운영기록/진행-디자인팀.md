# 진행 — 디자인팀

이 파일은 **디자인팀 진척만** 적습니다.
`docs/운영기록/진행상태.md` 와 `docs/수리준비.md` 에는 쓰지 않습니다.

---

## 2026-08-21 — 바이낸스 모바일 대조 (1회차)

### 지시
- 이전: "UI 구조는 그대로. 글자 체계만."
- 확장: "바이낸스 모바일 버전이랑 우리 모바일 버전 비슷하게 가자. 순서라던가."
- 제안만. 코드 무수정.

### 한 것

| 단계 | 내용 | 상태 |
|---|---|---|
| 테스트 제약 조사 | `tests/*.test.js` 에서 글자·모서리·위계를 강제하는 규칙 전수 | ✅ 완료 (18항목, 제안서 7장) |
| 바이낸스 모바일 390 | 순서·좌표·글자값 실측 + 캡처 | ✅ 완료 |
| 바이낸스 모바일 375 / 360 | 실측 + 캡처. **360 에서 쿠키 배너 → 비필수 거부** | ✅ 완료 |
| 바이낸스 Order Book 탭 | 좌우 2열, 12px/400, 행 피치 20px, 21+21행 | ✅ 완료 |
| 바이낸스 데스크톱 1440 | 글자값 실측 | ✅ 완료 |
| 우리 모바일 390/375/360 | 순서·좌표·글자값·가려짐·가로넘침 실측 | ✅ 완료 |
| 우리 768/1440/1920 | 가려짐·가로넘침 실측 | ✅ 완료 |
| 임시 적용 눈확인 | 시세 바 2열 격자 3번 시도(probe1/2/3), 새로고침으로 원복 | ✅ 완료 |
| 우리 랭킹 / 마이페이지 360 | 캡처 + 가로넘침 | ✅ 완료 (내용은 로그인 필요) |
| `docs/디자인제안.md` 작성 | 순서 비교 / 글자 비교표 / 작은변경 6 + 큰변경 4 / 위험 / 한글 보정 | ✅ 완료 |

### 핵심 실측값 (지어낸 값 없음)

```
문서 총 높이 (390px)   바이낸스 1165px   우리 5295px      (4.5배)
시세가 나오는 위치      바이낸스 116px    우리 525px
시세 바 가로 숨김      360/375/390 에서 +1007px  (6개 지표 중 4개가 화면 밖)
                       768 +743px / 1440 +71px / 1920 +43px
마크가격 겹침          360 -39.8 / 375 -54.8 / 390 -69.8 / 768 -393.7
                       1440 -1065.7 / 1920 -1093.7   → 여섯 폭 전부 안 가려짐 ✅
주문 패널 높이          1148px (스크롤 1856px 지점)
차트+호가 세로 합계      1150px
메뉴바 가로 넘침        360 +119px / 375 +104px / 390 +89px
공지 목록 제목          보임 141px / 내용 408~465px  (3배 잘림)
랭킹 표                 보임 324px / 내용 348px  (+24px 잘림)
포지션 표 글자          th 13px / td 13.5px  (같은 화면 주문창은 18.5~25px)
```

### 임시 적용 결과 (probe — 파일 무수정)

| 시도 | 시세바 가로 넘침 | 높이 | 판정 |
|---|---|---|---|
| 원본 | +1007px | 68px | — |
| probe1 (3열 격자, 대표가격 28px) | +33px | 134px | ❌ 대표가격이 옆 라벨과 충돌 |
| probe2 (3열, 대표가격 24px, 좌열 142px) | +46px | 154px | ❌ 펀딩비 값이 잘림 |
| **probe3 (바이낸스식 배치, 좌열 146px, 라벨 줄바꿈 허용)** | **0px** | 156px | ✅ 360/375/390 전부 넘침 0 |

### 다음에 할 것

1. **데스크톱 코인선물** 상세 대조 — 지금은 1440 글자값만 쟀습니다.
   순서·묶음 비교는 아직 안 했습니다.
2. **메인 화면** 대조 — 코인선물 화면의 상단부만 봤습니다.
3. **랭킹 / 마이페이지** 는 로그인이 필요해 표 내용을 못 봤습니다.
   대표가 로그인한 캡처를 주면 이어서 볼 수 있습니다.
4. 바이낸스 **Trades / Coin Info / Data** 탭 안쪽 (열지 못함).
5. 바이낸스 **768px** (모바일 3폭을 먼저 했습니다).

### 확인 못 한 것

- 바이낸스 모바일 **주문 시트 안쪽** — 로그인 필요. 로그인하지 않음
- 바이낸스 **로그인 후 하단 고정 바** 내용 — 비로그인이라 `Log In / Register Now` 만 봄
- 바이낸스 모바일 **포지션·주문내역 표** 글자·행 높이 — 내용이 비어 있어 표를 못 봄
- 우리 **랭킹 행 / 마이페이지 내역 / 포지션 표 값** — 로그인 필요
- 우리 **% 버튼** 정확한 글자값 — 전용 클래스가 없어 선택자로 못 잡음. 캡처로만 확인
- **제안 적용 후 `npm test` 결과** — 코드를 안 고쳤으므로 안 돌림

### 걸렸던 것 (다음 회차 참고)

`agent-browser` 를 세션 이름 없이 쓰면 **명령마다 다른 탭에 붙습니다.**
바이낸스를 열고 잰 줄 알았는데 localhost 값이 나온 일이 세 번 있었습니다.
→ **`--session <이름>` 을 항상 붙입니다.** (`--session bn`, `--session tl`)
`batch` 는 경로를 `/tmp/...` 로 못 받습니다. 윈도우 경로를 씁니다.
긴 스크립트는 `eval -b <base64>` 로 넘깁니다 (batch 안에서는 따옴표가 깨집니다).

### 코드 무변경 확인

```
git status --short          → ?? docs/수리준비.md  (디자인팀 것 아님)
git diff --stat -- style.css index.html js/ tests/ package.json  → 변경 0
md5sum -c /tmp/baseline.md5 → 12/12 일치
```

커밋·푸시 안 함. 로그인·비밀번호 입력 안 함.

---

## 2026-08-21 — 바이낸스 구조 대조 (2회차)

### 지시
- 이전 "UI 구조는 그대로. 글자만." → **취소됨**
- 지금 **"필요하면 구조도 바꿔라."** 단 **이번엔 1단계 조사만.**
- 목표: **"바이낸스처럼 거래하고, TL답게 경쟁한다"**
- 산출물: `docs/디자인-구조조사.md` (새 문서. `디자인제안.md` 는 그대로 둠)

### 한 것

| 단계 | 내용 | 상태 |
|---|---|---|
| 바이낸스 1440 골격 | `.react-grid-item` 8개 좌표 실측 (헤더/티커/심볼바/차트/호가/체결/주문/포지션/계좌) | ✅ |
| 바이낸스 1440 글자 | 34개 자리 `getComputedStyle` 실측 | ✅ |
| 바이낸스 1440 호가창 | 3열(Price/Size/Sum) 66px씩, 12px/400/lh20, 피치 20px, 7+7행 | ✅ |
| 바이낸스 1440 심볼바 | 지표 8종(Mark·Index·Funding·고가·저가·Vol BTC·Vol USDT·Open Interest) 전부 12px, h60 2줄 | ✅ |
| 바이낸스 390 / 360 | 골격·글자·밀도·하단 고정바(h80) 실측 | ✅ |
| 바이낸스 360 호가창 | 좌우 2판 병렬, Sum 열 없음, 20+20행, 피치 20px | ✅ |
| 바이낸스 1920 | 캡처만 (좌표·글자 미측정) | ⚠ 부분 |
| 바이낸스 표 열 | `Order History` 눌러도 비로그인이면 `<th>` **0개** | ❌ 확인 못 함 |
| 우리 1440 / 1920 / 390 / 360 | 골격·글자·밀도·컨테이너 넘침 실측 | ✅ |
| 우리 주문 패널 순서 | 14단계 전부 좌표·글자 실측 | ✅ |
| TL 개성 8가지 위치 조사 | 좌표로 찍음. **전쟁터가 메뉴에서 숨겨져 있는 것 발견** | ✅ |
| 수정 금지 파일 걸림 지점 | A~I 9건. ui.js / orderbook.js / chart.js 소스 확인 | ✅ |
| 테스트 제약 갱신 | 1회차 18건 유효 + **구조 강제 21건 신규 발견** | ✅ |
| 캡처 12장 | `/tmp/design3/`, 바이낸스·우리 같은 폭으로 짝지음 | ✅ |

### 핵심 실측값 (지어낸 값 없음)

```
문서 총 높이 1440    바이낸스 1222px    우리 3729px    (3.05배)
거래 화면 시작 y     바이낸스   66px    우리  736px    (670px 아래)
첫 화면 숫자 개수 1440  바이낸스 111개   우리    9개    (12.3배)
첫 화면 숫자 개수 390   바이낸스  22개   우리    5개    (4.4배)
칼럼 비율            바이낸스 62.7/16.7/20.9   우리 56.0/22.2/21.2
호가 행 피치         바이낸스 20px      우리 35px
호가 행 수(데스크톱)  바이낸스 7+7       우리 5+5
호가 행 수(모바일)    바이낸스 20+20     우리 5+5
주문 패널 높이        바이낸스 630px     우리 1111px
거래 3열 행 높이      (드래그 가능)      우리 height:max(1150px, 100vh-380px) 고정
시세 바 숨은 양       1920 +53 / 1440 +81 / 390 +977 / 360 +1007px
메뉴바 넘침 360       +119px
```

### TL 개성 — 1440 거래 화면에서 보이는 것 (실측 좌표)

```
보임:  메뉴 🏆랭킹/🔥핫딜/🛒마켓 (y139) · 배너 "실시간 랭킹" (y416)
       매수50%/매도50% 압력바 (y1090) · 무료충전 (y1710) · 로고·골드
안보임: 계급·보유 TL (y2184 = 문서의 58.6%)
       실시간 익절/손절 알림 피드 (y2827~3429 = 76%)
       랭킹 순위·점수 (거래 화면에 0개)
       ⚔ 전쟁터 → style.css:1823 .nav-coming-soon{display:none} 로 메뉴에서 숨김
                  (js/market-war.js 는 계속 동작 중, 압력바에만 흔적)
1920 에서만 내 정보(계급+보유 TL)가 첫 화면(y222)에 있음
 → style.css:1871 @media(max-width:1799px) 가 우측 칼럼을 풀어 아래로 스택
```

### 1회차에서 한 말 정정

- 1회차 L1 위험 "차트를 탭 뒤로 숨기면 resize 를 다시 쏴야 한다"
  → **정정.** `js/chart.js:186` 이 `createChart(..., {autoSize:true})` 라 내부 ResizeObserver 가
  자동 처리합니다. chart.js 에 수동 resize 리스너가 아예 없습니다.
  단 `display:none`(0×0) 복구 여부는 실제로 눌러 확인해야 합니다.
- 1회차 S3 "공지·배너를 시세 아래로"
  → **지금 그대로는 못 합니다.** `tests/top-panel.test.js:1422~1427` 이 index.html 의
  블록 등장 순서(menu < notice < ad < product-tabs)를 강제합니다.

### 승인 필요 (`docs/디자인-구조조사.md` 13장)

```
A1 테스트의 "레퍼런스 실측" 숫자를 바꿔도 되는가 (칼럼비율·행높이·블록순서)  ← 이거 하나가 7건을 품
A2 js/orderbook.js 를 열 것인가 (DEPTH_LEVELS=5, 호가 밀도)
A3 js/ui.js 를 열 것인가 (탭·표 <th> 가 문자열로 박혀 있음)
A4 전쟁터를 다시 켤 것인가
A5 TL 포인트 표기 통일 ("보유 TL" vs "TL 잔액")
A6 거래 화면에 경쟁 요소를 올릴 것인가 (A1 이 먼저 풀려야 함)
```

### 다음에 할 것

1. **랭킹 화면** 구조 조사 (경쟁이 핵심인데 이번에 못 함)
2. **마이페이지 / TL 핫딜 / TL 마켓** 구조
3. **전쟁터** 를 브라우저에서만 임시로 켜서 화면 확인 (파일 무수정)
4. 바이낸스 **768 / 1920 상세**, Coin Info·Data 탭
5. 우리 **375 / 768**
6. **2단계(안 제시)** — 대표가 A1~A6 에 답한 뒤에

### 확인 못 한 것

- 바이낸스 **포지션·주문내역·거래내역 표** 열·행높이·정렬 — 비로그인이면 `<th>` 0개. 로그인 안 함
- 바이낸스 **로그인 후 주문 패널**, **모바일 주문 시트**, **주문 입력값 글자**
- 바이낸스 **1920 상세**, **768**, **Coin Info / Data 탭**
- 우리 **로그인 후** 계급장·보유 TL·포지션 표 실제 행 — 로그인 안 함
- 우리 **차트 숨김→복구** 실제 동작 (파일을 못 고쳐 탭을 만들 수 없음)
- 우리 **375 / 768**
- **`npm test`** — 코드 무수정이라 안 돌림. 지금 수리팀이 style.css·index.html 작업 중

### 걸렸던 것 (다음 회차 참고)

- `--session bn` / `--session tl` 분리는 이번에도 유효했습니다. 섞이지 않았습니다.
- `eval -b <base64>` 로 긴 스크립트를 넘기는 방식이 안정적이었습니다.
- 바이낸스 쿠키 배너가 **이번엔 어느 폭에서도 뜨지 않았습니다.** 아무것도 누르지 않았습니다.
- 캡처 경로는 윈도우 절대경로(`C:\Users\pc\AppData\Local\Temp\design3\...`)로 줘야 합니다.

### 코드 무변경 확인

```
git status --short (끝)
 M docs/운영기록/진행-디자인팀.md     ← 이 파일
?? docs/디자인-구조조사.md           ← 산출물

git diff --stat -- style.css index.html js/ tests/ package.json  → 출력 없음 (변경 0)
md5sum -c /tmp/baseline.md5 → 12/12 일치
```

> 시작 시점에 있던 `M index.html` `M style.css` 등이 목록에서 사라진 것은
> **디자인팀이 되돌려서가 아니라 조사 중(15:12:35) 수리팀이 커밋했기 때문**입니다
> (`f942bce fix: [P1] … 1초·5초·15초 차트 버튼을 임시로 가림.`).
> 디자인팀 변경은 위 두 문서뿐입니다.

디자인팀 산출물: `docs/디자인-구조조사.md` 1개 + `/tmp/design3/` 캡처 12장.
새 CSS 파일 만들지 않음. 임시 스타일 얹지 않음(이번 회차는 측정만).
커밋·푸시 안 함. 로그인·비밀번호 입력 안 함.

---

## 2026-08-21 — A1 무관 개선안 + 다섯 화면 구조 조사 (3회차)

### 지시

- **A1(테스트 레퍼런스 숫자 변경) 대표 답변 대기 중** → 칼럼 비율·행 높이·블록 순서를 건드리는 안은 **보류**
- 두 갈래:
  1. **A1 과 무관하게 지금 낼 수 있는 코인선물 개선안** (글자 / 여백·행높이 / 주문 패널 안쪽 순서 /
     호가 열 순서 / 용어 / 포지션 표 글자)
  2. **아직 안 본 화면 구조 조사** — 랭킹 → 마이페이지 → TL 핫딜 → TL 마켓 → 커뮤니티, 여섯 폭 전부
- 조사·제안만. 코드 무수정. 새 CSS 파일 금지. 커밋·푸시 금지.

### 한 것

| 단계 | 내용 | 상태 |
|---|---|---|
| CSS 원문 재확인 | 호가창·주문 패널·포지션 표·간격 버튼의 실제 값과 **줄 번호** 전수 | ✅ |
| 테스트 원문 재확인 | 여백·행 높이를 막는 규칙이 실제로 몇 개인지 재조사 | ✅ **대부분 글자만 막고 여백은 안 막음** |
| A1 무관 개선안 | 작은 변경 **N1~N8**, 큰 변경 **L5·L6·L7** | ✅ `docs/디자인제안.md` 뒤에 추가 |
| 위계 검사 손계산 | N7 의 12개 값이 4개 테스트를 전부 통과하는지 직접 계산 | ✅ 통과 |
| 랭킹 화면 | 6폭 실측 + 360/1440 캡처 | ✅ |
| 마이페이지 | 6폭 실측 + 캡처 | ✅ |
| TL 핫딜 | 6폭 실측 + 캡처 (로딩 8초 대기 후) | ✅ |
| TL 마켓 | 6폭 실측 + 캡처 | ✅ |
| 커뮤니티 | 6폭 실측 + 캡처 | ✅ |
| 측정 원본 | 5화면 × 6폭 = **30건 전부 성공** (`/tmp/design4/v2-*.json`) | ✅ |
| 캡처 10장 | `/tmp/design4/us-{360,1440}-{5화면}.png` | ✅ |
| 전쟁터 화면 | **3회차 연속 못 함** | ❌ |

### 핵심 실측값 (지어낸 값 없음)

```
문서 총 높이 (360px)   랭킹 3153 / 마이페이지 3155 / 핫딜 5887 / 마켓 8019 / 커뮤니티 3153
문서 총 높이 (1440)    랭킹 3007 / 마이페이지 3007 / 핫딜 3687 / 마켓 4436 / 커뮤니티 3007

첫 화면 숫자 개수      랭킹    360~1440 전부 0개, 1920 만 4개(= 내 정보 카드 값)
                      마이페이지 360~1440 전부 0개
                      마켓    360~390 0개
                      핫딜    5~6개 / 커뮤니티 7개
                      (참고) 바이낸스 거래 1440 = 111개

랭킹 패널 높이         여섯 폭 전부 177px  (문서의 5.6%)
랭킹 빈 공간           360~768 약 618px / 1440 약 723px / 1920 약 1160px
  원인                style.css:122  .app{min-height:100vh}
                      1920 은 style.css:2611 로 패널까지 늘어남(카드 안이 빔)

TL 마켓 아이템 12개 중 판매 준비중 = 10개  (360 기준 y2021~y5805)
TL 핫딜 상품 6개 전부 정상 판매

메뉴바 가로 잘림       360 +119 / 375 +104 / 390 +89 / **768 +84** / 1440 0 / 1920 0
                      -> 768 은 1·2회차에 안 쟀던 폭. 태블릿에서도 "TL 마켓"이 화면 밖
공지 목록 제목 잘림     360~390  보임 141 / 내용 408~465 = +267~324
마이페이지 총자산 값     360 보임 31 / 내용 113 = +82 잘림 (비로그인 "-" 상태에서도)
커뮤니티 표             360 +9 (헤더 5칸이 6~14px 씩 잘림)
랭킹 표                 360 +24 / 375 +9 / 390 이상 0

"준비중" 배지 마크업 9개 중 화면에 보이는 것 2개
  (공지 박스 탭 "자유게시판" / "분석게시판")
```

### A1 무관 개선안 요약 (`docs/디자인제안.md` "코인선물 — A1 무관 개선안")

```
작은 변경
  N1 호가 행 여백 (피치 35->29px)        -60px  위험 낮음  테스트 안 막음
  N2 호가 현재가 블록 2줄->1줄            -50px  위험 중    테스트 안 막음
  N3 주문 패널 안쪽 순서(가용잔고 위로)     0    위험 낮음  테스트 안 막음  ★ CSS order 만
  N4 포지션 표 글자 13->15 / 13.5->16px    0    위험 중    테스트 안 막음 (문자열 3개 주의)
  N5 간격 버튼 20.5->16px                -10/-26 위험 낮음  정규식 주의
  N6 호가 제목·포지션 탭 글자             -14px  위험 낮음  테스트 안 막음
  N7 주문 패널 글자·여백 압축            -160px(추정) 위험 중  위계 검사 통과 확인함
  N8 용어 정리                             0    위험 낮음  T4(마감손익)만 막힘 -> 승인 필요
큰 변경
  L5 호가 좌우 2판(모바일)               -190px  ** 지금은 권하지 않음 ** (A2 먼저)
  L6 주문 패널 접기 (1회차 L3 재확인)    -590px  위험 높음
  L7 계급·보유 TL 을 주문 패널 안으로      +44px  ★ A6 의 일부를 A1 없이 할 수 있음
```

**칼럼 비율·행 높이·블록 순서는 한 개도 안 건드립니다.**

### TL 개성 지도 (여섯 화면)

```
계급(19단계)   여섯 화면 어디에도 본문에 없음. 공통 카드(.user-panel-box)에만.
               1440 기준 y1373~1462 = 첫 화면 밖. 1920 만 우측 칼럼 y222 에 있어 보임
               마이페이지에도 없음 / 게시판 글쓴이 옆에도 없음
랭킹 순위      랭킹 화면 말고 어디에도 없음. 마이페이지에도 없음
TL 포인트      핫딜·마켓에서 30px/700 로 가장 잘 보임
               거래 화면·랭킹 화면에서는 안 보임
               -> "쓰는 곳"에는 크게 있고 "버는 곳"에는 없음
알림 피드      여섯 화면 전부 채팅 패널 안(문서 아래쪽)
전쟁터         메뉴에서 여전히 숨김 (style.css:1823) — A4 그대로
```

### 1·2회차에서 한 말 정정

- **없음.** 다만 **이번 회차 안에서 스스로 한 번 잘못 볼 뻔한 것**을 적어 둡니다:
  마이페이지의 `TL 내역 / 핫딜 구매 내역 / 마켓 아이템` 이 **"불러오는 중…"으로 영영 남는**
  것처럼 보였습니다. **조용한 고장인 줄 알았는데 아닙니다.**
  `js/mypage-history.js:132` 가 읽기를 **`#page-nav-mypage` 의 click 이벤트**에 붙여 놨는데,
  제가 `App.PageNav.showPage()` 로만 넘겨서 로딩이 안 돈 것이었습니다.
  **실제로 버튼을 눌러 다시 재니 8초 안에 "…없습니다." 로 전부 채워졌습니다. 판정: 정상.**
  → **다음 회차부터 페이지 전환은 반드시 실제 클릭으로 합니다.**
- 핫딜·마켓도 처음 5초 시점에는 "상품을 불러오는 중…" 이 남습니다.
  8초 뒤 재니 6장·12장이 전부 그려져 있었습니다. **정상.**
  이 때문에 첫 측정(`w*.json`)을 버리고 **7초 대기 방식(`v2-*.json`)으로 30건을 다시 쟀습니다.**

### 승인 필요 (이번 회차에 새로 생긴 것)

| # | 항목 | 왜 | 막히는 것 |
|---|---|---|---|
| **A7** | **"마감손익" 탭 이름을 "거래내역"으로 바꿔도 되는가** | `order-panel.test.js:518` 이 `"포지션(1),미체결주문(0),주문내역,마감손익"` 문자열을 **그대로** 검사합니다. 바꾸려면 테스트를 같이 고쳐야 합니다 | N8-T4 하나 |
| **A8** | **TL 마켓 아이템 12개 중 10개가 "판매 준비중"인데, 화면에 계속 둘 것인가** | 360 에서 아이템 영역 5030px 중 약 4800px 이 살 수 없는 것입니다. 숨길지 / 아래로 내릴지 / 그대로 둘지는 **사업 판단**입니다 | 마켓 화면 개선 전체 |

> **A7·A8 은 이 보고서에만 적었습니다. `승인대기.md` 는 본부장이 관리하는 파일이라
> 디자인팀이 직접 쓰지 않았습니다.** 본부장이 옮겨 주시면 됩니다.

### 다음에 할 것

1. **전쟁터 화면** — 3회차 연속 못 했습니다.
2. **로그인 후 화면** — 랭킹 실제 행 / 마이페이지 실제 값 / 포지션 표 실제 행.
   **대표가 로그인한 캡처를 주시면 바로 이어서 봅니다.**
3. N2(호가 현재가 한 줄)를 **360 에서 임시 적용(probe)해 눈으로 확인**.
4. 커뮤니티 **글 상세·댓글** 화면 (실서버 조회수가 올라가므로 먼저 정해야 함).
5. 다섯 화면 **색상 실측** — 확정 팔레트가 실제로 쓰이는지.
6. 바이낸스 **768 / 1920 상세**, Coin Info · Data 탭 (2회차부터 밀려 있음).
7. **2단계(코인선물 골격 안)** — A1 답변 후.

### 확인 못 한 것

- 랭킹 표 **실제 행**, 계급장이 실제로 붙는지 — 비로그인이라 행 0개. **판단 불가**
- 마이페이지 **값 10칸의 실제 숫자**, 360 에서 실제 금액이 잘리는지 — 로그인 필요
- 핫딜·마켓 **구매 흐름**, 마켓 **보관함·거래내역 탭 안** — 로그인 필요. 누르지 않음
- 커뮤니티 **글 상세·댓글** — 조회수가 올라가므로 **누르지 않았습니다**
- **전쟁터 화면**
- 다섯 화면 **색상**
- **375 / 768 / 1920 캡처** — 캡처는 360·1440 두 폭만. 실측은 여섯 폭 전부 했습니다
- N2 를 360 에서 적용했을 때 한 줄에 들어가는지 / N7 의 −160px(추정) / L7 비로그인 표시
- **`npm test`** — 코드 무수정이라 안 돌림

### 걸렸던 것 (다음 회차 참고)

- **`App.PageNav.showPage()` 로 페이지를 넘기면 클릭에 걸린 로딩이 안 돕니다.**
  (`js/mypage-history.js:132`) → **실제 클릭으로 넘겨야 합니다.**
- **`.nav-coming-soon` 버튼은 누르면 `alert()` 가 떠서 브라우저를 붙잡습니다.**
  (`js/page-nav.js:90`) 대회일정·참가신청·공지사항·고객센터·전쟁터 — **누르지 마십시오.**
- `agent-browser eval` 안에서 **Promise + setTimeout 35초짜리**를 쓰면 eval 이 먼저 끊깁니다.
  → 페이지 전환·대기·측정을 **명령 3개로 쪼개는** 방식(`run.sh`)이 안정적이었습니다.
- **다른 팀과 브라우저를 같이 씁니다.** 조사 중 두 작업이 겹쳐 `v2-768-*.json` 5건과
  `v2-1440-mypage.json` 이 **다른 폭의 값으로 덮였습니다.**
  → 측정 JSON 에 `w`(뷰포트)와 `page`(어느 화면인지)를 같이 저장해 두었더니 **바로 잡혔고**,
  그 6건만 다시 쟀습니다. **다음에도 결과 안에 조건을 같이 적는 것을 권합니다.**
- 처음 `agent-browser` 명령이 한 번 **2분 넘게 안 끝나** 중단했습니다(브라우저 경합).
  잠시 뒤 한 번 재시도하니 정상이었습니다. 그 뒤로는 계속 정상.

### 코드 무변경 확인

```
md5sum -c /tmp/baseline.md5                                      -> 12개 전부 OK (12/12)
git diff --stat -- style.css index.html js/ tests/ package.json  -> 출력 없음 (변경 0)
```

> ⚠ **디자인팀이 만든 문서가 다른 팀 커밋에 휩쓸렸습니다.**
> 조사 중(**16:02**) 커밋 `95247ec fix: [P1] 체결 스트림을 살려…` 가
> `docs/디자인제안.md`(+564) 와 `docs/디자인-구조조사.md`(+582) 를 **같이 담아 갔습니다.**
> **디자인팀은 커밋·푸시를 하지 않았습니다.** 2회차에도 같은 일이 있었습니다.
> 그 커밋이 건드린 코드(`index.html` · `js/order-pressure-bar.js` · `js/trade-stream-fix.js` ·
> `main.js` · `package.json` · `tests/`)는 **전부 디자인팀 것이 아닙니다.**
> **수정 금지 12개는 하나도 안 들어 있습니다** (위 해시 12/12 로 확인).

디자인팀 산출물: 문서 2개 갱신 + `/tmp/design4/` 캡처 10장 + 측정 JSON 30건.
새 CSS 파일 만들지 않음. 임시 스타일 얹지 않음(이번 회차는 측정만).
로그인·비밀번호 입력 안 함. 쿠키·동의 배너 없음(로컬 서버).
`준비중` 메뉴는 `alert()` 때문에 **누르지 않았습니다.**
