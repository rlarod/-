# 운영기록 2026-08-31 (월)

> **금요일(08-28) 13:49 이후 첫 근무입니다.** 주말(08-29 토 · 08-30 일)은 작업 없음.
> 금요일 마지막에 **사용량 한도로 다섯 팀이 작업 도중 동시에 죽었습니다.**
> 오늘 첫 일은 **그 잔해 확인**이었습니다.

## 발견
- 총 건수: 2
- 등급별: P1 0 / P2 1 / P3 1

## 게이트 1 (발견 → 배정)
- 통과: 2
- 반려: 0
- 반려 사유: 없음

## 게이트 2 (완료 → 승인)
- 통과: 1
- 반려: 0
- 반려 사유: 없음
- 3회 반려로 대표 보고한 건: 없음

## 팀별 처리
- 점검팀: 1 (금요일 완료분 접수)
- 조사팀: 1 (금요일 완료분 접수)
- 수리팀: 1
- 디자인팀: 1 (금요일 완료분 접수)
- 기록팀: 2
- 차트팀: 0
- 감사팀: 0

## 대표 승인 요청
- 없음 (승인대기 11건은 그대로 대기 중)

## 확인 못 한 것
- **로그인 화면 전부** — 8일째. 승인대기 5번
- **채팅 32~40분 구간** — 금요일에 다른 팀 브라우저에 밀려 못 채웠습니다
- **`tw-probe-tmp.js` 말고 또 딸려간 임시 파일이 있는지** — 감사팀 확인 중

## 수정 금지 파일 해시
- 결과: 12/12 일치
- 불일치 시 파일명: 없음

---

## 🔴 죽은 팀들의 잔해 — 먼저 확인한 것

**팀이 죽는 순간 쓰던 파일은 반쯤 쓰다 만 상태일 수 있습니다.**

```
수리팀    "Now the AFTER measurement" 에서 끊김
차트팀    "Now wire the button in chart-drawings.js" 에서 끊김
기록팀    "Let me write the seal." 에서 끊김
조사팀    시작하자마자 끊김
점검팀    ✅ 정상 완료
디자인팀   ✅ 정상 완료
```

**작업트리에 남아 있던 것**

```
?? js/symbol-switch-price-clear.js                    수리팀 — ★완성돼 있었음★
?? tests/market-war-power-bar-burst-seal.test.js      기록팀 — _order 에 등록돼 있었음
?? tests/sync-guard-baseline-blindspot.test.js        기록팀 — ★_order 에 없었음★
 M index.html · main.js · tests/_order.txt
 D tests/tw-probe-tmp.js
차트팀이 만들던 파일 — ★흔적 없음. 날아갔습니다★
```

**수정 금지 12개는 12/12 일치했습니다.**

---

## ✅ [P2] 종목을 바꿔도 옛 종목 가격이 1초 남던 것 — 고쳤습니다 (`47176d1`)

**가장 나쁜 장면 — 호가창은 이미 삼성전자 188.67 인데 바로 옆 매수가격은 비트코인 79,824.70.**
**한 화면에 두 종목이 동시에 있었습니다.**

### 원인 — 1초 타이머 하나뿐이었습니다

```
js/order-info-panel.js:25    REFRESH_INTERVAL_MS = 1000
js/order-info-panel.js:129   setInterval(updatePreview, 1000)
symbol:change 를 구독하지 않습니다
```

**`stale-price-guard` 가 dataset 을 즉시 지워도 화면 글자는 다음 1초 눈금까지 옛 숫자였습니다.**
**남는 시간이 누른 순간에 따라 0~1.0초로 무작위였습니다.**

### ⭐ 수리팀이 순서를 안 타는 쪽을 골랐습니다

```
A) order-info-panel 이 symbol:change 때 다시 돌게 한다
   → updatePreview() 는 dataset.price 를 ★읽습니다★
     가드가 지우기 전에 우리가 돌면 옛 값이 다시 찍힙니다
     ★누가 먼저 도느냐에 결과가 달라집니다★

B) 두 칸을 곧장 "-" 로 쓴다  ← 채택
   → 아무것도 안 읽으므로 ★누가 먼저 돌든 결과가 같습니다★
```

**폭마다 결과가 같게 나온 것이 이 판단이 맞았다는 증거입니다.**

### PM 실측 (localhost, 60ms 간격)

⚠️ **`App.Config.getActiveSymbol()` 로 종목이 진짜 바뀐 것만 셌습니다.**

```
SAMSUNG→BTC      옛값 182.75      옛값샘플 0/45   새값복귀 1549ms
BTC→QQQ          옛값 77,991.20   옛값샘플 0/45   새값복귀  536ms
QQQ→SAMSUNG      옛값 714.33      옛값샘플 0/45   새값복귀  535ms
SAMSUNG→SKHYNIX  옛값 182.90      옛값샘플 0/45   새값복귀 1554ms

360 / 768 / 1920   전부 옛값 0 · 이벤트 1 · 지움 2 · 오류 0
360 은 칸 자체가 숨김인데도 오류 없이 돕니다
```

### 라이브 확인 (배포 뒤)

```
BTC→QQQ    옛값 77,948.20   옛값샘플 0/40   새값복귀 1448ms → 713.21
QQQ→BTC    옛값 713.21      옛값샘플 0/40   새값복귀 1240ms → 77,966.10
BTC→QQQ    옛값 77,966.10   옛값샘플 0/40   새값복귀  778ms → 713.06

최근 체결 30줄 · 콘솔 오류 0건
```

### ⚠️ PM 이 처음 잰 것은 틀렸습니다

**같은 탭을 다시 눌러놓고 "옛값 9~20샘플 남음" 으로 읽었습니다.**
**`getCounters().events` 가 4번 클릭에 1밖에 안 오른 것을 보고 알았습니다.**
**종목이 진짜 바뀌었는지 확인하는 절차를 넣고 다시 쟀습니다.**

---

## 🔴 봉인을 써놓고 등록을 안 해 **영원히 안 돌던 것**

```
tests/sync-guard-baseline-blindspot.test.js 가 _order.txt 에 없었습니다
파일은 멀쩡하고 오류도 없는데 ★검사만 안 됐습니다★
```

**전형적인 조용한 고장입니다. 우리 테스트 안에서 났습니다.**

**단독 실행 14/14 통과를 확인하고 등록했습니다. 같은 일을 막는 봉인을 기록팀에 배정했습니다.**

### 그 봉인이 박아둔 결함 (수리팀 배정)

```
js/sync-guard.js:54-57 이 기준값을 만들 때 서버 응답의 .error 를 안 봅니다
조회가 실패하면 기준값이 조용히 {0,0} 이 됩니다
그러면 looksLikeDataLoss 가 "잃은 게 없다" 고 판정합니다
손실 판정이 이 기준값에 기대는 곳이 5곳입니다
```

---

## 🔴 PM 이 지난 커밋에 임시 파일을 딸려 올렸습니다

```
tests/tw-probe-tmp.js 가 e3cfc66 에 들어가 있었습니다
기록팀 임시 조사 파일인데 제가 쓸어담았습니다
```

**아무도 안 부르는 것을 확인하고 지웠습니다. 같은 일을 막는 봉인을 기록팀에 배정했습니다.**

---

## ⭐ 봉인이 제 일을 했습니다 — 라이브 404 를 막았습니다

```
첫 npm test 에서 html-assets-tracked 가 터졌습니다

  ✗ 부르는 js 가 전부 git 에 있다 — 미추적 1개:
    js/symbol-switch-price-clear.js
    이대로 index.html 만 푸시하면 라이브에서 404 입니다
```

**`fs.existsSync` 로는 못 잡는 것입니다. 제 디스크에는 있었으니까요.**
**`git ls-files` 로 보는 봉인이라 잡혔습니다.**

⚠️ **그리고 `npm test` 는 실패해도 종료코드가 0 이 아닙니다 — `EXIT=1` 로 확인했습니다.**
**`| tail` 로 파이프하면 tail 의 0 이 덮어씁니다. 반드시 `> /dev/null; echo $?` 로 봅니다.**

---

## 검증

```
npm test          147개 파일 / 147개 통과 / 검사 7,242건 / 실패 0
수정 금지 12개     12/12 — 커밋 전·후 둘 다
스테이징           제가 이름으로 적은 7개만. 그 외 0개
```

---

## 컴퓨터 렉 (대표 최우선)

```
크롬 프로세스        26개   (08-27 최고 153개 → 08-28 34개 → 오늘 26개)
agent-browser 프로필  2개   (08-28 최고 91개 → 오늘 2개)
```

**주말 동안 안 쌓였습니다. 오늘 일곱 팀이 붙었으니 다시 오를 겁니다. 주기적으로 청소합니다.**

---

## 오늘 배정 (일곱 팀 전부 가동)

```
감사팀   ① 반쯤 쓰다 만 파일 찾기  ② 또 딸려간 임시 파일  ③ 곧 일 없어질 팀
차트팀   ① MA7 점선 (라인 모드만)  ② 360 축 눈금 지워짐   ③ 툴바가 시간축 덮음
수리팀   ① 폰 하단 버튼 안 잠김    ② sync-guard 기준값 {0,0}
기록팀   ① _order 미등록 찾기+봉인 ② 옛 가격 봉인        ③ 임시 파일 봉인
조사팀   ① 왜 이전 세션만 회복 못 했나 ② 왜 하필 691초    ③ 32~40분 공백
점검팀   ① 옛 가격 독립 실측       ② 배포 뒤 라이브       ③ 375 원화 9.69px
디자인팀 ① 시트 열면 종목 못 바꿈   ② 봉 종류 폰에서 읽히나 ③ 대표 캡처 요청표
```

---

## ⚠️ 감사팀 지적 — 게이트 2 구멍. **절반만 맞습니다**

**감사팀 지적**

> PM 님이 재신 폭은 `360 / 768 / 1920` 인데, 디자인팀 결론은
> "실제로 보이는 폭은 768 / 1440 / 1920" 이고 최악 장면이 **1440에서 794ms** 였습니다.
> **증상이 가장 크게 나는 1440 이 사후 실측 목록에 없습니다.**

**확인 결과 — 1440 은 쟀습니다. 다만 제 보고가 그렇게 안 읽혔습니다.**

```
폭 스윕(360/768/1920)        ← 감사팀이 본 것. 1440 이 빠져 있습니다
4회 전환 본측정 (폭 1440)     ← ★여기가 1440 입니다★
   SAMSUNG→BTC / BTC→QQQ / QQQ→SAMSUNG / SAMSUNG→SKHYNIX
   전부 옛값샘플 0/45
라이브 확인 (폭 1440)         ← 여기도 1440
   3회 전환 전부 옛값샘플 0/40
```

**즉 1440 은 로컬 4회 + 라이브 3회, 총 7회 쟀습니다. 구멍은 없습니다.**

⚠️ **다만 지적이 정당합니다.** 제가 "폭별" 표에 1440 을 안 넣어서,
**표만 보면 1440 을 건너뛴 것으로 읽힙니다.** 앞으로 여섯 폭 표에 전부 넣겠습니다.

**⭐ 그리고 375 · 390 은 정말 안 쟀습니다.** 두 폭은 칸이 숨김이라 증상이 안 보이지만,
**"안 보인다" 를 제가 확인한 게 아니라 디자인팀 실측을 빌린 것입니다.** 점검팀에 맡겼습니다.

---

## ⚠️ 감사팀 지적 — **세 팀 산출물이 한 커밋에 묶였습니다**

> CLAUDE.md 가 경고한 `-A` 패턴과 같은 모양입니다.

**맞습니다. 모양이 같습니다. 다만 경위는 다릅니다.**

```
git add <파일7개>            이름으로 하나씩 적었습니다. -A 안 썼습니다
git diff --cached --name-status   커밋 ★전에★ 읽었습니다
git status --porcelain | grep -v  스테이징 안 된 것 0개 확인
```

**세 팀 것이 한 덩어리인 것은 사실이고, 그건 제 판단이었습니다** —
수리팀 모듈이 `_order.txt` 를 건드리고, 기록팀 봉인 두 개도 같은 파일을 건드려서
나누면 중간 커밋이 테스트 실패 상태가 됩니다.

**셋 다 게이트 2 를 거쳤습니다.** 안 거친 것이 섞이지는 않았습니다.

---

## ✅ 감사팀이 확인해 준 것

```
문법 깨진 파일          js 125개 · tests 149개 · main.js — ★0개★
디스크 js 276개  ==  git ls-files 276개    어느 방향으로도 어긋남 0
main.js 부팅 목록 48개  전부 실제 App.<이름> + init() 으로 존재
_order.txt 147  ==  디스크 tests/*.test.js 147    미등록 0 · 유령 0
임시 파일               tw-probe-tmp.js ★하나뿐★. 최근 10커밋 전수
tw-probe 부르는 곳       0곳 — 지워도 안전
차트팀 유실물            디스크·git·shots 어디에도 흔적 0 — ★통째로 유실★
```

**⭐ 기록팀은 손실이 없었습니다.** 끊긴 줄 알았던 봉인 두 개가 `main()` 까지 온전했고
직접 돌려 12/12 · 14/14 통과했습니다.

**⭐ `e3cfc66` 의 shots 2장은 사고가 아니라 `git add -f` 로 일부러 넣은 것입니다.**

---

## 🔴 감사팀 발견 — `js/chart-style.js` 가 33KB인데 테스트 0건

```
App.ChartStyle · 33KB
getStateForTest() 를 /* 확인용 */ 주석과 함께 ★이미 내보내고 있습니다★
봉인을 쓰라고 만든 출구인데 아무도 안 씁니다
```

**기록팀에 넘깁니다.**

---

## ⚠️ 낡은 문서 (감사팀 지적)

```
docs/백로그.md:653   TL-018 이 "미착수 · 미배정" 인데
                     e3cfc66 이 표시 절반을 이미 배포했습니다
docs/운영기록/진행상태.md · 진행-수리팀.md · 진행-디자인팀.md   08-21
docs/운영기록/진행상태-야간.md                                08-24
```
