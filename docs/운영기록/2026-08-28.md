# 운영기록 2026-08-28

> **밤사이 09:30 이전 분은 `2026-08-27.md` 에 있습니다. 이 파일은 09:30 이후분입니다.**
>
> ⚠️ **발견은 생길 때마다 아래 "발견 상세" 에 한 줄씩 넣습니다.**
> 어제 하루 끝에 몰아서 세다가 **두 번 틀렸습니다**(22 → 31 → 40).
> 상단 집계는 하루 끝에 그 줄 수를 세서 채웁니다.
>
> ⚠️ **2026-08-28 대표 지시로 명칭이 바뀌었습니다 — 사업본부장 → 프로젝트 매니저(PM).**
> `docs/지배구조.md` 참조.

## 발견
- 총 건수: 4
- 등급별: P1 0 / P2 2 / P3 2

## 발견 상세 (생길 때마다 한 줄씩 추가)

- 11:52 **[P2] `shots/` 가 `.gitignore` 밖** — 미추적 **687장 172MB**.
  `git add -A` 한 번이면 저장소 역사에 영구 편입(.git 현재 59MB).
  PM 이 `fde463a`·`799e3f6` 에서 이미 두 번 `-A` 사고. **발견: 감사팀 · 조치 완료(PM)**
- 11:52 **[P2] 채팅 P1 을 DOM 후처리로 고치면 반드시 무한 되돌이가 납니다** —
  `js/chat.js:307` 이 `{ init }` 만 내보내 `seenIds`·`channel`·`loadRecent` 가
  전부 클로저 안에 갇혀 있습니다. 우회 모듈이 다시 그려도 `chat.js:143` 의
  `renderEmpty()` 가 `innerHTML=""` 로 통째로 지우고, 관찰자가 다시 그리는 되돌이.
  **가능성이 아니라 확정입니다. 발견: 감사팀 · 수리팀에 즉시 전달**
- 11:52 [P3] 채팅 P1 이 `docs/백로그.md` 에 TL 번호로 미등록.
  `2026-08-27.md` 에만 있어 추적 누락 위험. **발견: 감사팀**
- 11:52 [P3] `docs/백로그.md` TL-001·002·003 제목에 ✅ 가 없는데 상태줄은 "해결".
  제목/상태 불일치 3건. **발견: 감사팀**

## 게이트 1 (발견 → 배정)
- 통과: 0
- 반려: 0
- 반려 사유:

## 게이트 2 (완료 → 승인)
- 통과: 3
  - 10:08 `3eadd66` 종목 탭 14px → 19px · 줄 높이 48px (디자인팀) — **대표 직접 지시**
  - 10:22 `00f46d1` 지배구조 제정 + 사업본부장 → PM 명칭 변경 — **대표 직접 지시**
  - 11:18 `eb9a2c2` 여러선(평행 채널) + 봉인 3개 (차트팀 · 기록팀) — **대표 직접 지시**
- 반려: 0
- 반려 사유:
- 3회 반려로 대표 보고한 건: 없음
- **커밋 전 차단: 0**

## 팀별 처리
- 점검팀: 1 (qa18 종목 탭 여섯 폭 실측 — 진행 중, 11:46 착수)
- 조사팀: 0 (11:5x 배정 — 채팅 시작 조건 · 측정 차이 · 종목 전환)
- 수리팀: 0 (09:30 이후 산출물 흔적 없음 — 설계 지점 확정 대기)
- 디자인팀: 2 (dz4 종목 탭 배포 완료 · dz5 11:51 착수)
- 기록팀: 3 (chart-addons-seal 491줄 · chart-channel-seal 652줄 ·
  chart-double-tap-swallow 172줄 — 전부 `eb9a2c2` 로 배포)
- 차트팀: 2 (채널 배포 완료 · ct5 봉 종류 바이낸스 실물 조사 진행 중)
- 감사팀: 1 (순회 1회차 · 채팅 REST 측정 충돌 판정 · 채팅 우회 설계 제약 확인)

## 대표 승인 요청
- 신규 없음. 기존 8건은 `docs/운영기록/승인대기.md` 에 그대로 있습니다.
  1·2·3·4·8번 🔴 높음 · **5번(로그인 캡처)은 08-24 부터 5일째 대기**
- **구두로 여쭌 것 2건** (승인대기에는 아직 안 올림)
  - 차트 "표정" 도구 — 이모지 스티커인데 `js/no-emoji.js` 정책과 충돌. 차트에만 열지
  - 추세선 굵기 1px → 2px — **PM 재량으로 진행**(CLAUDE.md "차트 도구는 바이낸스를 따라간다")

## ⭐ PM 이 오늘 늦게 배정한 건 (대표 지적)

> 대표: **"차트팀 노는데?"**

차트팀이 11:18 에 여러선을 끝내고 보고했는데, PM 이 배포만 하고 다음 일을 안 줬습니다.
**11:5x 에야 일곱 팀을 다시 붙였습니다.**

**고친 방법**
```
① 배정할 때 1건·2건·3건을 미리 붙이고 "제 답을 기다리지 마세요" 를 명시
② 감사팀 순회의 최우선 항목을 "곧 일이 없어질 팀을 미리 알려달라" 로 바꿈
   지금까지는 다 끝나고 나서 노는 걸 알았습니다
```

## 확인 못 한 것
- **수리팀이 지금 무엇을 쓰고 있는지** — 09:30 이후 산출물 0건이라 디스크로는 판정 불가.
  파일명 한 줄을 요청했습니다
- **디자인팀 dz5 업무 내용** — 캡처 1장뿐이라 감사팀이 특정 못 함
  (PM 이 붙인 것이 맞습니다 — 360 여유 8.9px 대안 · 채널 색 · 압력바 "모름")
- **채팅 REST 조회의 탭 백그라운드 영향** — 브라우저 타이머 조임이라 코드로는 못 가림
- **차트팀이 앞서 보고한 "45초 주기" 의 근거** — 코드에도 기록에도 없음
- **로그인해야 보이는 화면 전부** — 5일째

## 수정 금지 파일 해시
- 결과: 12/12 일치 (11:50 확인)
- 불일치 시 파일명: 없음

---

## 📌 감사팀이 두 팀 측정 차이를 **코드로 가렸습니다** — 둘 다 맞았습니다

**REST 조회(`loadRecent`)가 나가는 경로는 정확히 두 곳뿐입니다. 주기 조회는 없습니다.**

```
js/chat.js:303   init() 안에서 — 페이지당 정확히 1회
js/chat.js:175   .subscribe() 가 SUBSCRIBED 를 줄 때,
                 단 reconnectAttempts > 0 인 경우에만
```

| 상태 | REST | 누가 잰 것 |
|---|---|---|
| **한 번도 안 끊긴 탭** | **추가 0건** — `reconnectAttempts` 가 0 이라 175줄이 절대 안 탐 | 점검팀 30초에 0건 |
| **한 번이라도 끊긴 탭** | **약 1초에 1건** | 조사팀 48초에 47건 |

**계산 — 1 사이클 = 1,000ms = REST 1건 → 48초에 약 48건.**
조사팀 실측 **47건 · 간격 평균 1,020ms** 와 코드가 정확히 일치합니다.

```
chat.js:193  delay = min(1000 × 1.6^0, 15000) = 1000ms   ← 항상 1초
chat.js:176  성공 직후 reconnectAttempts = 0 으로 리셋
             → 다음 사이클도 지수가 1단계로 되돌아가 delay 가 절대 안 늘어남
```

### ⭐ 그리고 이 3,600회는 **아무것도 안 그립니다**

`renderEmpty` 가 한 번 돌면 DOM 은 비는데 `seenIds` 는 안 비웁니다.
그 뒤 조회가 성공해도 `chat.js:84` 가 `seenIds.has(msg.id)` 로 **50건 전부 되돌려 보냅니다.**

**시간당 3,600회를 DB 에서 긁어와 한 줄도 안 그리고 버립니다.**
회원 화면은 계속 "채팅을 불러오지 못했습니다" 입니다.
**조용한 고장 + 순수 낭비가 겹쳐 있습니다.**

### 표현 정정

**"탭 하나당 시간당 3,600회" 는 "끊김을 한 번 겪은 탭 하나당" 으로 적어야 합니다.**
지금 표현이면 모든 탭이 그러는 것처럼 읽힙니다.

---

## 🔴 채팅 루프는 **자작극이었습니다** (조사팀, 확신도 확실)

**첫 끊김이 무엇이든 상관없습니다. 재연결 코드가 스스로 끊김을 만들어 냅니다.**

```js
// js/chat.js:162
if (channel) { client.removeChannel(channel); }
```

**`removeChannel` 이 그 채널의 상태 콜백을 같은 밀리초에 `CLOSED` 로 부릅니다.**

### 실증 — 같은 client 로 직접 확인

```
[54336, "SUBSCRIBED"]
[58282, "--calling removeChannel--"]
[58283, "CLOSED"]          ← 1ms
```

### 고리

```
재연결 타이머 발화 → reconnectTimer=null(196) → subscribeRealtime
  → removeChannel(162) → 옛 채널 CLOSED(180) → scheduleReconnect(181) → 1000ms 예약
  → 새 채널 SUBSCRIBED → attempts>0 이라 loadRecent(175) → attempts=0
  → 1000ms 뒤 처음으로
```

**`reconnectTimer` 가 바로 앞 196행에서 비워진 뒤라 191행 중복 방지가 안 걸립니다.**
**`SUBSCRIBED` 가 매번 성공해도 `attempts` 가 0 으로 리셋돼 대기시간이 안 늡니다.**

### 끊김을 한 번만 주입한 뒤 방치 (localhost, 1440, 비로그인)

```
622299ms  pagehide 한 번 발생시킴
622394ms  웹소켓 close  code=1005  wasClean=true
622393ms  CHANNEL_ERROR          ← 진짜 끊김. 딱 1회
623399ms  CLOSED                 ← 여기부터 전부 자작
625009 / 626035 / 627059 / 628073 / 629109 …   간격 1024·1026·1024·1014·1036ms
869892ms  여전히 CLOSED — 247초 동안 242회, 한 번도 회복 안 됨
```

### 정상일 때는 완전히 조용합니다

```
소켓 하트비트 25초 간격, 왕복 70~98ms, 전부 ok
10분 34초 동안 close 0회 · error 0회 · REST 1건(최초 1회)
```

**⭐ 21분 방치 실측 — 자연 끊김 0회. "시간이 지나면 반드시 터진다" 는 아닙니다.**

### 첫 방아쇠 — 유력 후보는 `pagehide` 입니다

**supabase-js 2.112.4 번들을 직접 읽어 창에 거는 것을 전수로 셌습니다.**

```
pagehide            socket.disconnect()  →  CHANNEL_ERROR   ← 실증
pageshow            connect()
visibilitychange    pageHidden=true 만
```

**폰에서 앱을 전환하거나 화면을 끄면 iOS·안드로이드가 `pagehide` 를 쏩니다.**
**인위로 한 번 쏘자 즉시 무한루프가 시작됐습니다.**

⚠️ **점검팀이 실제로 무엇을 맞았는지는 증명 못 했습니다 → 유력.**

### ✅ 창 크기는 무관합니다 (확실)

```
resize 를 듣는 파일 19개 전부 차트·레이아웃. Supabase 를 건드리는 것 0개
페이지 이동도 무관 — js/page-nav.js:30-36 은 display 만 토글합니다
실측 — 여섯 폭 전부 바꿔도 끊김 0회 · REST 추가 0건 · 소켓 이벤트 0건
```

---

## ⚠️ 정정 — 두 팀 측정 차이에 **함정이 있었습니다**

**앞서 `f6fd6ef` 커밋에 "둘 다 맞았습니다" 라고 적었습니다. 절반만 맞았습니다.**

**감사팀 코드 분석은 유효합니다** — 한 번도 안 끊긴 탭은 REST 추가가 정말 0건입니다.

**그런데 조사팀이 0 이 나오는 두 번째 경로를 찾았습니다.**

```
performance.getEntriesByType('resource') 는 기본 250개에서 멈춥니다

페이지 로드 직후 resource 항목  201개 (한도 250)
→ 49개만 더 오면 버퍼가 꽉 차고, 그 뒤로는 진짜 요청이 와도 목록이 안 늘어납니다

같은 160초 구간
  getEntriesByType 방식     3건   (버퍼가 250에서 멈춤. resTotal=250)
  PerformanceObserver 방식  157건  (진짜 값)
```

**조사팀도 같은 방식으로 세다가 똑같이 당했고, 방법을 바꿔 재현했습니다.**

```
PerformanceObserver 로 다시 셈 (끊김 주입 후)
  30.8초 × 5회 = 160.9초에 157건 → 1.025초 간격 → 시간당 3,512회 / 탭
  같은 시각 경고도 30초에 30건으로 정확히 일치
```

⚠️ **점검팀이 어떤 방법으로 셌는지 모릅니다.**
**둘 중 어느 것이었는지 확인이 필요합니다 —**
**(가) 애초에 안 끊긴 탭이었다 (정상)  (나) 250 버퍼에 걸렸다 (측정 함정)**

---

## ✅ 새 종목 전환이 느린 건 **바이낸스가 안 보내주는 것**입니다 (조사팀, 확신도 확실)

**우리 코드가 한 줄도 안 낀 맨 웹소켓으로 종목당 10회씩 쟀습니다.**

```
종목          소켓 열림    kline 첫 도착        aggTrade 첫 도착
BTCUSDT      118~250ms   259~722ms           213~859
QQQUSDT      123~170ms   457 ~ 15,000 초과    130 ~ 15,000 초과
SAMSUNGUSDT  116~143ms   474~3,986ms         273~4,124
SKHYNIXUSDT  119~138ms   170~1,113ms         225~506
```

```
소켓 열림은 전 종목 동일 (116~250ms) — 차트팀 실측 81ms 와 같은 자리
→ 우리 전환 절차는 범인이 아닙니다

kline 도착 시각이 aggTrade 도착 시각과 거의 같습니다
  QQQ 12,768 / 12,894 · 삼성 3,374 / 3,362 · 삼성 1,655 / 1,652
→ kline 은 체결이 있을 때만 옵니다
```

**QQQUSDT 는 한 회차에서 15초 동안 kline·체결이 한 건도 안 왔습니다**
(markPrice 와 호가는 정상 도착).

**⭐ 점검팀이 본 10,324ms 는 앞서 차트팀이 본 6.1초와 같은 것입니다. 정상 범위입니다.**
**첫 방문/두 번째 방문 차이도 없습니다 — 매 회차가 새 소켓인데 편차가 똑같습니다.**

### 고칠 방법은 있는데 **가격의 출처를 바꾸는 것**입니다

```
같은 소켓의 @markPrice@1s 는 전 종목에서 242~1,222ms 안에 반드시 옵니다
js/websocket.js:117-124 가 이걸 받아 funding:update 로만 내보내고
setCurrentPrice 는 안 부릅니다
→ 첫 체결 전까지만 마크가격을 임시 표시하면 체감 대기가 최대 1.2초
```

⚠️ **마크가격은 체결가가 아닙니다. 회원이 보는 가격의 원본을 바꾸는 것입니다.**
**조사팀이 "실행 제안이 아니라 판단 요청" 으로 올렸습니다. 맞는 판단입니다.**
**바이낸스가 이 종목들에서 무엇을 표시하는지 먼저 확인해야 합니다.**

**대안(가격 안 건드림)** — 첫 값이 오기 전 안내를 새 종목에 한해 더 일찍 띄워
"거래가 뜸한 종목입니다" 로 말해주기.

---

## ⚠️ [P3] supabase-js 판본이 고정돼 있지 않습니다 (조사팀)

```
index.html:75   @supabase/supabase-js@2      ← 버전 미고정
오늘 실제로 받아온 것            2.112.4
```

**라이브러리가 올라가면 하트비트 간격·`pagehide` 처리 같은 게 예고 없이 바뀝니다.**
**팀 간 측정이 갈리는 다른 이유가 될 수 있습니다.**

---

## ✅ 그린 선 굵기를 바이낸스에 맞췄습니다 (차트팀, `cf95854`)

**바이낸스에서 도구를 하나씩 직접 그어 픽셀로 쟀습니다.**

```
도구           바이낸스   근거
추세선          2px       216열 중 209열이 정확히 2행 · shots/ct6-bnf-trend.png
수평선          2px       바이낸스가 설정줄에 "— 2px" 라고 직접 표시 + 픽셀 2행
피보나치 눈금 7개 2px      y 81·112·131·146·161·183·211 전부 2행
피보나치 안내선  2px       점선
평행 채널        2px       6차 때 이미 잰 값
자 (Measure)    테두리 없음  상자 위끝에서 테두리 없이 바로 채움
```

**⭐ 도구마다 다르지 않습니다. 바이낸스는 그리기 도구를 전부 2px 로 긋습니다.**

### 우리 값과 대조 — 다른 것만 맞췄습니다

```
                바이낸스   전     후
추세선           2px       1px    2px  ✅
수평선           2px       1px    2px  ✅
피보나치 (7+안내) 2px       1px    2px  ✅
채널 위·아래      2px       2px    2px  ❌ 같아서 안 건드림
브러시           2px       2px    2px  ❌ 같아서 안 건드림
자 테두리         없음      1px    1px  ⚠️ RULER_WIDTH 로 분리
```

**자만 1px 로 남긴 이유** — 바이낸스는 자에 테두리를 아예 안 긋습니다(파란 20% 채움 + 말풍선만).
**우리 채움은 10% 라 테두리를 없애면 상자가 안 보입니다.**
**그래서 "없는 쪽에 가장 가까운 1px" 로 뒀습니다. 2px 로 올리면 바이낸스에서 더 멀어집니다.**

### 여섯 폭 — 겹침·가려짐 0

```
피보나치 라벨   여섯 폭 전부 7/7 표시. 하나도 안 밀려났습니다
페이지·패널·차트칸 넘침   전부 0

금색이 캔들칸을 덮는 비율 (다섯 도구를 동시에 그린 최악 조건)
  360    4.075% → 4.620%   (+0.545%p)
  1440   1.262% → 1.483%   (+0.221%p)
```

**두 배가 안 되는 이유 — 금색 픽셀 상당수가 글자·딱지·채움이라 굵기와 무관합니다.**

⚠️ **회원이 이미 그어 둔 선도 다음에 열 때 같이 굵어집니다.** 의도한 동작입니다.

---

## ⚠️ [P3 관찰] 라인 모드에서 금색이 **세 겹**입니다 (차트팀)

```
시세선   #F0B429 2px   봉 종류 라인 (오늘 넣은 것)
MA 7     #F0B429 1px   js/chart-indicators.js:74 (원래 있던 것)
그린 선  #F0B429 2px
```

**시세선과 MA 7 은 실제로 구분이 어렵습니다.** 둘이 거의 붙어 다니고 굵기 차이(2 vs 1)뿐입니다.

### ✅ 그런데 바이낸스도 똑같습니다 — 그래서 안 고칩니다

```
shots/ct5-bnf-line.png — 바이낸스 라인 모드도 시세선·MA7 이 둘 다 금색(#F0B90B)
```

**`CLAUDE.md` 는 "바이낸스와 다른 곳을 발견하면 보고" 라고 합니다. 여기는 같습니다.**
**따라서 지금 상태가 지침에 맞습니다. 관찰로만 남깁니다.**

⚠️ **그린 선 vs 시세선은 안 헷갈립니다** — 시세선은 잘게 떨리는 곡선이고
그린 것은 곧은 직선·점선입니다. 디자인팀 판정(MA7 vs 채널)과 같은 근거입니다.

---

## ⚠️ [P2 · 과정] 팀이 파일을 오가는 중에 PM 이 커밋했습니다 — 아슬아슬했습니다

**차트팀이 스스로 보고했습니다.**

```
12:41~12:46   팀이 전·후 캡처를 찍느라 LINE_WIDTH 를 2 → 1 → 2 로 오감
12:48         PM 커밋
```

**결과는 운이 좋아 `2` 가 잡혔습니다. 몇 초만 어긋났으면 `1px` 이 배포됐습니다.**
**그 순간엔 테스트도 통과했을 겁니다 — 값이 맞았으니까요.**

### ⭐ 게이트 2 의 구멍입니다

**PM 이 "지금 값" 을 보는 것만으로는 부족합니다.**
**그 값이 팀이 확정한 값인지, 잠깐 되돌려 둔 값인지 구분이 안 됩니다.**

**`CLAUDE.md` 에 규칙으로 넣었습니다.**

```
커밋 직전에 "지금 이 파일을 만지는 팀이 있나" 를 확인한다
완료 보고를 받은 파일만 커밋한다. 보고 없이 mtime 만 보고 커밋하지 않는다
팀은 전·후 비교를 할 때 원본을 오가지 말고 별도 사본을 띄운다
```

---

## ⭐ 기록팀이 봉인 하나가 **엉뚱한 곳을 보고 있던 것**을 잡았습니다

```
tests/chart-chip-viewport-seal.test.js
  "칩 버튼 글자에 그림문자가 없다" 를 검사한다면서
  주석을 안 걷어내고 파일 전체에서 따옴표를 찾고 있었습니다
  → 주석에도 따옴표가 있어 설명문 덩어리가 통째로 잡혔습니다
```

**실제로 차트팀이 주석에 경고 표시를 넣자 칩과 아무 상관 없는 이 검사가 터졌습니다.**

**양방향으로 확인해 고쳤습니다** — 진짜 칩 라벨에 넣으면 터지고, 주석에만 넣으면 안 터집니다.

---

## ⚠️ [P3] 봉인 두 개가 사본이 아니라 진짜 저장소를 읽습니다 (기록팀)

```
tests/supabase-client-seal.test.js:52       const REPO = path.resolve(__dirname, "..");
tests/chart-chip-viewport-seal.test.js:61   const REPO = path.resolve(__dirname, "..");
```

**돌연변이가 조용히 통과합니다** — 사본을 고쳐도 테스트는 진짜 저장소를 읽습니다.
**기록팀이 실제로 두 번 속았습니다.**

**배포가 늦어질까 봐 지금은 안 건드렸다고 정직하게 보고했습니다. 다음에 정리합니다.**
