# 백로그

본부장이 관리합니다. 등록만 하고 지시 없이 착수하지 않습니다.

**닫힌 건은 지우지 않습니다.** 언제·무엇으로 닫혔는지와 근거(커밋 해시·파일·줄)를
그 자리에 남깁니다. 나중에 같은 증상이 다시 나왔을 때 "전에 어떻게 닫았는지" 를
찾을 수 있어야 하기 때문입니다.

제목 맨 앞의 🔴 는 **아직 안 닫힌 P1** 에만 붙입니다. 닫히면 ✅ 로 바꿉니다.

*(2026-08-28 감사팀 전수 정리 — 제목과 상태가 어긋난 3건, 4일 낡은 상태 7건,
종결 누락 1건을 근거와 함께 맞췄습니다. 지운 항목은 없습니다.)*

---

## TL-001 · [P2] 폰에서 펀딩비가 안 보임

**등록** 2026-08-21 · **상태** ✅ 해결 (2026-08-24, 커밋 aa33fab — 시세 바를 700px 이하에서 두 줄로 접음)

### 현상

390px 이하 화면에서 시세 바의 **펀딩비 항목에 손가락으로 밀어야만 닿습니다.**
가만히 두면 화면에 안 나옵니다.

실측 (2026-08-21, 비로그인, localhost:3000):

| 폭 | 펀딩비 | 마크가격 |
|---|---|---|
| 1920 | 보임 | 보임 |
| 1440 | 보임 | 보임 |
| 390 | **가려짐 (336px 밖)** | 보임 |

### 원인 (확인됨)

**펀딩비 항목 하나의 폭이 351px** 입니다. 라벨이 `펀딩비 (다음 정산까지)` 로 길어서입니다.
390px 화면에서 시세 바가 쓸 수 있는 폭은 **372px** 이라, 펀딩비를 넣으면
현재가나 마크가격 중 하나를 밀어내야 합니다. 셋 다는 물리적으로 안 들어갑니다.

### 왜 P2 인가

- **돈이 실제로 나가는 항목입니다.** 펀딩비는 8시간마다 잔고에서 빠집니다.
  회원이 얼마가 빠지는지 폰에서 못 보는 상태입니다.
- 다만 **마크가격(강제청산 기준가)은 보입니다.** 즉시 손해로 직결되는 정보는
  확보돼 있어 P1 은 아닙니다.
- 밀면 볼 수 있고, 스크롤 힌트가 "옆에 더 있다"를 알려줍니다. 접근 자체가
  막힌 것은 아닙니다.

### 고치려면

라벨을 좁은 화면에서만 `펀딩비` 로 줄이면 들어갑니다.
다만 **라벨 문구는 `js/chart.js`(수정 금지 파일)가 만듭니다.**
CSS 로 글자를 잘라 숨기는 건 원칙 1-2(데이터 감추기 금지)에 걸릴 소지가 있으니,
별도 모듈로 라벨만 바꿔 끼우는 방식이 필요합니다.

**대표 지시 없이 착수하지 않습니다.**

### 관련

시세 바 잘림 수정(2026-08-21)의 **범위 밖**으로 분리한 건입니다.
그 작업은 마크가격·펀딩비를 앞으로 당겨 넓은 화면에서는 둘 다 보이게 했지만,
390 이하에서 펀딩비까지 넣는 것은 폭이 부족해 해결하지 못했습니다.

---

## TL-002 · [P2] 호가창 헤더가 통화를 따라가지 않음

**등록** 2026-08-21 · **상태** ✅ 해결 (2026-08-24 — js/ob-header-currency.js 로 헤더가 통화를 따라감)

KRW 모드에서 값은 `112,261,650원` 인데 헤더는 `가격(USDT)` 그대로입니다.
헤더가 [index.html:327](../index.html) 에 글자로 박혀 있어 통화를 바꿔도 안 바뀝니다.

**다만 통화 전환 버튼(USDT/KRW)이 현재 화면에서 숨겨져 있습니다**
([style.css:2112](../style.css), 2026-08-17 커밋 `8fcb903` 에서 의도적으로 숨김).
그래서 지금 새로 KRW 로 바꿀 방법은 없습니다.

**그런데 선택한 통화는 브라우저에 저장됩니다**(`js/config.js`).
8/17 이전에 KRW 로 바꿔둔 회원은 **KRW 에 갇혀 있고, 돌아갈 버튼도 없습니다.**
몇 명이 해당되는지는 각자 브라우저를 봐야 알 수 있어 확인 못 했습니다.

**대표가 "뒤로 미룬다"고 판단한 건입니다.**

---

## TL-003 · [P3] 3D 병사 모델이 안 보임

**등록** 2026-08-21 · **상태** ✅ 해결 (2026-08-24, 커밋 ba5bc9f — importmap 추가 + three 두 벌 로드 회피)

[index.html:28-34](../index.html) 에서 three.js 를 옛 방식(`<script src>`)으로 불러온 뒤
최신 방식(`import`)으로 부품을 꺼내려다 충돌합니다.

**콘솔에 오류가 아니라 경고로 뜹니다.** 코드가 이미 대비해 두었습니다:

```
[market-war.js] GLTFLoader를 찾을 수 없어 병사 모델을 표시할 수 없습니다(나머지 기능은 정상 동작).
```

전쟁터 화면의 3D 캐릭터만 안 나오고 나머지 기능은 정상입니다.

---

## ✅ TL-004 · [P1 · 닫힘] 차트를 1초·5초·15초로 두면 손익·강제청산이 통째로 멈춤

**등록** 2026-08-21 14:55 · **상태** ✅ 닫힘 (2026-08-21 16:02) · **담당** 수리팀

**무엇으로 닫혔나** — 커밋 `95247ec` "[P1] 체결 스트림을 살려 매수/매도 비율·최근 체결
목록을 되살림". 새 파일 `js/trade-stream-fix.js` 가 `/public` 경로로 체결 전용 소켓을
따로 열어 `trade:tick` 을 되살립니다. `js/config.js` 와 수정 금지 12개는 안 건드렸습니다.

⚠️ **감사팀 정정 (2026-08-28)** — 여기 적혀 있던 닫힌 날짜 `2026-08-24` 는 틀렸습니다.
실제 커밋은 **08-21 16:02** 입니다. 아래 본문은 닫히기 전 기록 그대로 둡니다.

### 현상

차트 간격을 **1초 / 5초 / 15초** 로 바꾸면 가격 신호가 **완전히 끊깁니다.**
오류 메시지는 하나도 안 뜹니다. 화면도 멀쩡합니다. **전형적인 조용한 고장입니다.**

### 실측 (본부장 직접, 10~12초간 `price:update` 수신 횟수)

| 간격 | 로컬 | 라이브 | 판정 |
|---|---|---|---|
| 1초 | **0회** | — | 죽음 |
| **5초** | **0회** | **0회** | 죽음 |
| 15초 | **0회** | — | 죽음 |
| 1분 | 30회 | 28회 | 정상 |
| 5분 | 17회 | — | 정상 |

같은 시간 호가창(`orderbook:update`)은 24회로 **정상**입니다. 부분 고장이 아니라
가격 신호만 딱 죽습니다.

### 무엇이 멈추나 (코드로 확인)

`js/trading.js:671` 이 `price:update` 하나에 전부 걸려 있습니다.

```
price:update → onPriceUpdate (88행)
                 ├→ checkPendingOrder()  지정가 주문 체결
                 └→ checkTriggers (488행)
                      ├→ 강제청산 판정   (493~494행)
                      ├→ 익절(TP) 판정   (496~499행)
                      └→ 손절(SL) 판정   (500~503행)
                 └→ trading:update       화면 손익 갱신
```

**따라서 1초·5초·15초에서는 이것들이 전부 안 돕니다:**

- ❌ **강제청산이 안 됨** — 청산가를 지나쳐도 포지션이 살아 있음
- ❌ **손절(SL)이 안 걸림** — 회원이 걸어둔 안전장치가 조용히 무력화
- ❌ **익절(TP)이 안 됨**
- ❌ **지정가 주문이 안 체결됨**
- ❌ **미실현 손익이 멈춘 숫자로 보임**

### 왜 P1 인가

- **돈에 직결**: 손절을 걸어놨는데 안 걸립니다. 회원은 걸린 줄 압니다
- **조용함**: 오류가 없어 회원이 고장인 줄 모릅니다
- **라이브에서 재현됨**: 지금 쓰는 회원이 겪을 수 있습니다
- 완화 요소: 기본값이 `1분` 이라, **1초·5초·15초를 직접 누른 회원만** 해당됩니다

### 원인 (수리팀 확정)

`js/config.js:131-138` 의 `buildCombinedStreamUrl()` 이 `/market` 경로로 접속하는데,
체결 스트림 `@trade` 는 바이낸스의 `/public` 경로에만 있습니다. 바이낸스는 없는
스트림 이름을 받아도 **오류를 내지 않고 연결을 유지**해서, 같은 연결의 kline·ticker 는
정상인데 `@trade` 만 한 건도 안 옵니다.

1분 이상 간격은 kline 스트림으로 가격을 받지만, **1초·5초·15초는 바이낸스에 없는
간격이라 체결로 캔들을 직접 만들게 되어 있습니다**(`js/websocket.js:148-163`).
그 체결이 안 오니 가격도 안 옵니다.

수리팀 실측:

| 접속 주소 | 결과 |
|---|---|
| `/market/stream?…btcusdt@trade` (현재 사용 중) | 15초간 **trade 0건** (kline 42 / markPrice 15) |
| `/public/ws/btcusdt@trade` | 8초간 **trade 737건** |
| `/market/ws/btcusdt@aggTrade` | 8초간 **aggTrade 234건** |

### 같은 원인의 다른 증상

- **최근 체결 목록이 영구히 빔** (TL 최초 발견 건) — 같이 해결됐습니다
- **전쟁터 매수/매도 강도가 전부 0** (`js/market-war.js:1216` 도 `trade:tick` 구독)
  → ⚠️ **이쪽은 안 나았습니다.** 체결이 와도 막대가 반반입니다.
  원인이 스트림이 아니라 연출값 포화라 **TL-018 로 분리**했습니다

### 고치는 방법 (당시 계획 — 그대로 적용됐습니다)

새 파일 `js/trade-stream-fix.js` 1개 + `index.html` 1줄 + `main.js` 1단어.
**수정 금지 12개는 물론 `js/config.js` 도 안 건드립니다.**
호가창(`js/orderbook.js:70`)이 이미 쓰는 "자기 연결 따로 열기" 패턴과 같습니다.

상세는 `docs/수리준비.md` 참조.

---

## ✅ TL-005 · [P1 · 닫힘] 매수/매도 압력 바가 항상 정확히 50:50

**등록** 2026-08-21 15:10 · **상태** ✅ 닫힘 (2026-08-28, 감사팀 정리) · **담당** 없음(고칠 것 없음)

### 어떻게 닫혔나 — 세 방향이 따로 확인했고 결론이 같습니다

| 누가 | 무엇을 | 결과 |
|---|---|---|
| 수리팀 | 뿌리 원인(`trade:tick` 0건)을 08-21 에 이미 고침 | 커밋 `95247ec` · `js/trade-stream-fix.js` (TL-004) |
| 조사팀 | 바이낸스 스트림 3분 직접 셈 | btc 3174 / qqq 123 / samsung 151 / skhynix 863건.<br>**60초 창이 빈 횟수 0** (0/120 · 0/120 · 0/118 · 0/119) |
| 조사팀 | 실제 페이지 15초 | `trade:tick` **237건** vs `orderbook:update` 45회.<br>`getRatio() {48,52}` → 화면 글자 "매수 48%" · 막대 48% 일치 |
| 점검팀 | 라이브 60초 7회 | 72 / 76 / 48 / 40 / 46 / 48 / 49 — 계속 움직임 |

**예전 "체결 0 / 호가 20" 과 정반대입니다. 지금은 값이 실제로 갱신됩니다.**

⚠️ **본부장 배정 착오 기록** — 2026-08-27 이 건을 수리팀에 배정하면서 근거로
`js/market-war.js:1258` 을 댔는데, 그 줄은 **압력바가 아니라 전쟁터 막대**를 그립니다.
감사팀이 짚어 중단했고, 멀쩡한 계산식을 뜯는 일을 막았습니다.
근거 — `docs/운영기록/2026-08-27.md` 1375행 · 1431행.

### 남은 것 — 고장이 아니라 표시 규칙 한 줄 (본부장 재량, 새 번호 안 땀)

`js/order-pressure-bar.js:93-101` 과 `index.html:495-496` 이 **데이터가 없을 때도**
`50% / 50%` 을 그립니다. 지금은 데이터가 오니까 문제가 안 보이지만,
스트림이 또 끊기면 "모름" 이 다시 "반반" 으로 보입니다.
글자만 `—` 로 바꾸고 막대는 그대로 두면 됩니다. **표시 보정이라 재량 범위입니다.**

⚠️ 종목 전환 직후 재접속 구간(실측 6.0초)에는 `—` 가 정상적으로 나옵니다. 상시가 아닙니다.

---

⚠️ **아래는 닫히기 전(2026-08-21) 기록입니다. 원인 설명은 그때 기준이라 지금과 다릅니다.**

### 현상

주문창의 **매수/매도 비율 바가 언제나 `매수 50% / 매도 50%`** 를 표시합니다.
전쟁터의 매수·매도 강도도 마찬가지로 전부 0 입니다.

**균형 잡힌 시장처럼 보이지만, 사실은 "데이터가 하나도 없음"입니다.**

### 원인 (코드로 확정, 추측 아님)

`js/market-war.js:1254`

```js
function getBuySellRatio() {
  const total = buyIntensity + sellIntensity;
  const buyPct = total > 0 ? (buyIntensity / total) * 100 : 50;   // ← total 0 이면 50
  return { buyPct: Math.round(buyPct), sellPct: Math.round(100 - buyPct) };
}
```

`buyIntensity` / `sellIntensity` 는 **오직 체결 신호에서만** 늘어납니다
(`js/market-war.js:498-499`, 갱신 경로는 1216행의 `App.Bus.on("trade:tick", ...)` 하나뿐).

**TL-004 와 같은 이유로 `trade:tick` 이 0회**이므로 두 값은 영원히 0 이고,
`total > 0` 이 거짓이 되어 **항상 50 이 반환**됩니다.

`js/order-pressure-bar.js` 가 이 값을 그대로 주문창 압력 바에 씁니다.

### 왜 P1 인가

- **"모름"을 "반반"으로 보여줍니다.** 빈 화면이면 회원이 이상하다고 느끼지만,
  50:50 은 **그럴듯한 정상값처럼 보여서 회원이 그대로 믿습니다**
- 매수/매도 압력은 **진입 방향을 정하는 근거**입니다. 잘못된 근거로 돈을 겁니다
- 오류도 경고도 없습니다 — 조용한 고장의 전형

### 확인된 근거

- 코드 경로 3곳 대조 (갱신처 / 대체값 / 사용처)
- 2026-08-21 11:05 화면 실측에서 `매수 50%  매도 50%` 확인
- TL-004 실측: `trade:tick` 10초간 **0회**

### 고치는 방법

**TL-004 를 고치면 같이 해결됩니다.** 체결 신호가 살아나면 두 값이 실제로 갱신됩니다.

**추가로 권장** — 데이터가 없을 때 50 을 반환하지 말고 "데이터 없음"을 표시해야 합니다.
지금 구조는 스트림이 또 끊겨도 아무도 모릅니다. `js/market-war.js` 는 수정 금지가
아니지만, 표시 쪽(`js/order-pressure-bar.js`)에서 처리하는 편이 안전합니다.

### 확인 못 한 것 (당시)

- 라이브 사이트 화면 확인 (브라우저 세션 경합으로 보류)
  → **2026-08-27 점검팀이 라이브 60초 7회로 확인했습니다** (위 표).

---

## ✅ TL-006 · [P2 · 닫힘] 비회원에게 랭킹이 안 보입니다

**등록** 2026-08-21 16:40 · **상태** ✅ 닫힘 (2026-08-21 16:33) · **담당** 수리팀

**무엇으로 닫혔나** — 커밋 `3517506` "[P2] 비회원에게도 랭킹을 보여줍니다 (TL-006)".
새 파일 `js/guest-leaderboard.js` 로 우회했습니다. 수정 금지 파일인
`js/leaderboard.js:140` 은 한 글자도 안 건드렸습니다.

⚠️ **감사팀 정정 (2026-08-28)** — 닫힌 날짜가 `2026-08-24` 로 적혀 있었으나
실제 커밋은 **08-21 16:33** 입니다.

### 현상

로그인하지 않고 **🏆 랭킹** 을 누르면 이렇게만 나옵니다.

```
🏆 TL 랭킹 (실현 손익 기준)
순위 닉네임 총자산 수익금 수익률
로그인 후 확인할 수 있습니다.
```

**본부장 실측 (1440px, 비로그인)** — 랭킹 페이지 높이 **177px**(화면 900px),
**첫 화면에 숫자 0개**, 아래로 **723px 이 빈 공간**.

### 왜 문제인가

대표가 **"랭킹 리그 — 경쟁이 이 사이트의 핵심"** 이라고 했습니다.
그런데 **링크를 받고 처음 들어온 사람에게 그 핵심이 안 보입니다.**

경쟁을 보고 들어오게 만들어야 하는데, 경쟁을 보려면 먼저 가입해야 하는 구조입니다.

### 원인 (확정)

**서버는 이미 비회원에게 열려 있습니다.**

`supabase/schema-guest-read.sql:54`
```sql
grant execute on function public.get_leaderboard to anon;
```

같은 파일 머리말에 의도가 명확히 적혀 있습니다.

> 증상: 비회원이 홈에 들어오면 공지·게시판·인기글·채팅·**랭킹**이 전부 빈칸.
> 이 파일이 하는 일: 읽기(SELECT)만 누구나 가능하게 바꿉니다.

**그런데 화면 쪽 잠금이 안 풀렸습니다.**

`js/leaderboard.js:140` (**수정 금지 파일**)
```js
if (!(await hasSession(client))) {
  renderMessage("로그인 후 확인할 수 있습니다.");
  return;                              // ← 서버를 부르지도 않고 끝냄
}
```

**서버를 열어주는 작업은 끝났는데, 화면의 잠금을 걷어내는 것을 빠뜨렸습니다.**
같은 증상으로 함께 고쳤어야 할 게 반만 된 상태입니다.

### 조용한 고장인가?

**아닙니다.** "로그인 후 확인할 수 있습니다"라고 **정직하게 말하고 있습니다.**
회원이 잘못된 정보로 판단하지는 않습니다. 그래서 P1 이 아니라 **P2** 로 잡았습니다.

다만 **의도한 동작과 다르고, 사업의 핵심에 닿아 있습니다.**

### 고치는 방법 (당시 계획 — 그대로 적용됐습니다)

`js/leaderboard.js` 는 **수정 금지 파일**이라 별도 모듈로 우회해야 합니다.
`js/guest-access.js` 가 이미 비슷한 일(비회원 부팅)을 하고 있으니 그 패턴을 볼 것.

**확인 필요** — 같은 잠금이 다른 곳에도 있습니다:
- `js/trade-history.js:152` — 거래내역 (개인 정보라 **막는 게 맞음**)
- `js/my-private-info.js:61` — 개인정보 (**막는 게 맞음**)
- `js/tl-hotdeal.js:475` · `js/tl-market.js:404` — TL 잔액 (**막는 게 맞음**)

**랭킹만 공개 정보인데 같이 막혀 있습니다.**

### 확인 못 한 것 (당시 · 일부는 지금도 그대로)

- 로그인하면 실제로 순위가 나오는지 (본부장은 로그인하지 않습니다 — 지금도 확인 못 함)
- 서버가 실제로 `anon` 에게 응답하는지 (SQL 실행이 대표 몫 — 승인대기 2번)

---

# 2026-08-24 점검 1회차 — 신규 등록

점검팀 발견 8건 중 **본부장이 직접 재현한 것만** 등록합니다.

---

## ✅ TL-007 · [P2 · 닫힘] 폰에서 주문 확인 버튼이 화면 밖에 있음

**등록** 2026-08-24 · **상태** ✅ 닫힘 (2026-08-24 17:16) · **담당** 디자인팀

**무엇으로 닫혔나** — 커밋 `a3e01d4` "점검 1회차 P2 3건".
`js/order-sheet-mobile.js` · `style.css` — `.order-buttons` 를 `position:sticky` 로
시트 바닥에 고정. DOM 은 안 옮겼습니다. 시트 머리말에 매수/매도 방향 배지도 붙였습니다.
**실측** 375×667 `0px → 46px 전부 보임` · 360×800 `22px → 46px`.

하단 매수/매도 바로 시트를 열면 확인 버튼 top 이 **화면 높이와 무관하게 778px 고정**입니다.

| 기기 | 46px 중 보이는 양 |
|---|---|
| 375×667 (아이폰 SE·8) | **0px — 전혀 안 보임** |
| 360×800 (갤럭시 표준) | **22px** (절반) |
| 390×844 (아이폰 표준) | 46px (정상) |

시트 안(scrollHeight 1233 / clientHeight 610~743)을 스크롤하면 나오지만 **신호가 없습니다.**
매수를 눌러도 매도를 눌러도 보이는 화면이 같습니다 — 강조(`data-tl-side`)가 붙는 대상이
바로 이 화면 밖 버튼이기 때문입니다.

**원인** — 2026-08-24 모바일 3단계(`js/order-sheet-mobile.js`)의 부작용.

---

## ✅ TL-008 · [P2 · 닫힘] 시세 바가 768~1920 에서 잘림

**등록** 2026-08-24 · **상태** ✅ 닫힘 (2026-08-24 17:16) · **담당** 디자인팀

**무엇으로 닫혔나** — 커밋 `a3e01d4`. 새 파일 `stats-bar-desktop-wrap.css`
(`style.css` 규칙과 분리) — `flex-wrap` + 항목 간격 16→8px. 라벨과 항목은 그대로.
**실측** 768 `753px 숨김 → 0` · 1920 `24H 거래량 53px → 0`.

`.stats-bar` 내용 폭은 항상 1503px 인데 바 폭이 **1450px 로 묶여** 있습니다.

| 폭 | 바 폭 | 잘림 | 화면 밖 |
|---|---|---|---|
| 768 | 750 | **753px** | 24H 변동률·고가·저가·거래량 **4개** |
| 1440 | 1422 | 81px | 24H 거래량 |
| 1920 | 1450 | 53px | 24H 거래량 |

360/375/390 은 TL-001 수정으로 두 줄이 되어 정상입니다.

**주의** — 본부장이 2026-08-21 에 "1920 은 숨겨지는 양 없음" 이라고 잘못 보고한 적이 있습니다.
바 폭을 실측하지 않고 화면 폭으로 추정한 탓입니다. **늘 잘리고 있었습니다.**

---

## ✅ TL-009 · [P2 · 닫힘] 마감손익·주문내역 표에 스크롤 힌트가 없음

**등록** 2026-08-24 · **상태** ✅ 닫힘 (2026-08-24 17:16) · **담당** 디자인팀

**무엇으로 닫혔나** — 커밋 `a3e01d4`. `js/table-scroll-hint.js` 의 대상에
마감손익·주문내역·자산 표를 추가했습니다. 빈 표 안내문도 "보이는 폭" 기준
가운데정렬로 바꿔 화면 밖으로 밀리지 않게 했습니다.

360×800 `마감손익` 탭 — 보이는 폭 **342** / 내용 폭 **775** → **433px 숨음. 힌트 없음.**

열 오른쪽 끝: 진입가 322 / 종료가 395 / 증거금 469 / **손익 524 / 수익률 597 / 수수료 730**
→ **진입가까지만 보이고 정작 손익이 화면 밖입니다.**

`주문내역` 탭도 같습니다(439/342, 97px).
`js/table-scroll-hint.js` 의 대상이 `["#leaderboard-panel .table-scroll"]` 뿐입니다.

빈 표 안내문 `거래 내역이 없습니다.` 도 775px 기준 가운데정렬이라 화면 밖으로 밀립니다.

---

## ✅ TL-010 · [P1 · 닫힘 — 고장이 아니었습니다] 랭킹이 전원 0원

**등록** 2026-08-24 · **상태** ✅ 닫힘 (2026-08-25, 백로그 반영은 2026-08-28) · **담당** 없음

### 어떻게 닫혔나

```
docs/운영기록/2026-08-25.md:22    게이트 1 반려 — "고장이라는 근거가 없어 배정하지 않음"
docs/운영기록/2026-08-25.md:74    본부장 자기 정정 — "화면 숫자만 보고 검산하지 않았다"
docs/운영기록/승인대기.md:370     처리 완료 표 — "랭킹 0원 / 지갑 0 / 확정손익 차이 → 전부 고장 아님" (08-25)
```

⚠️ **08-25 에 이미 닫혔는데 이 파일만 4일 동안 `P1 · 담당 미배정` 으로 남아 있었습니다.**
`docs/백로그.md` 와 `docs/운영기록/승인대기.md` 가 서로 다른 상태를 들고 있었습니다.
(감사팀 2026-08-27 발견 → 2026-08-28 반영)

### ⚠️ 아래 본문의 원인 설명은 낡았습니다

`profit_amount = trading_accounts.realized_pnl` 이라고 적혀 있는데,
그 뒤 `supabase/schema-leaderboard-floor.sql` 이 **`ranking_profit`(바닥 규칙)** 으로
바꿨습니다. 지금 랭킹은 그 파일이 계산합니다.

### 곁가지는 진짜로 남아 있습니다 → **TL-016 으로 분리**

"`profiles` 는 4명인데 랭킹에는 3명" 은 별개의 진짜 문제였습니다.
가입할 때 지갑 줄(`trading_accounts`) 만들기가 실패하면 랭킹에서 통째로 사라집니다.

`get_leaderboard` RPC 를 직접 불러도 3명 전부 이렇게 옵니다.

```
CHRO   총자산 100,000 / 수익금 0
Mang9  총자산 100,000 / 수익금 0
김갱    총자산 100,000 / 수익금 0
```

같은 화면 채팅에는 오늘(한국시간 8/24) 김갱님 거래 **7건**이 있습니다. 합계 **-220,579,147원**.

랭킹 계산은 `profit_amount = trading_accounts.realized_pnl` 입니다
(`supabase/schema-ranking-fix.sql:46`). 즉 **서버의 realized_pnl 이 0** 입니다.

**둘 중 무엇인지 못 가렸습니다.**

| | |
|---|---|
| (가) | 계좌가 초기화됨 — 거래기록은 있는데 손익만 0 |
| (나) | 손익이 안 쌓임 — 거래해도 realized_pnl 이 안 올라감 |

**본부장이 서버를 못 봅니다.** `supabase/조사-랭킹0원-2026-08-24.sql`(읽기 전용)을
대표에게 전달했고 결과를 기다립니다.

**손익·랭킹 계산식 변경은 대표 확인 항목**이라 추측으로 손대지 않습니다.

곁가지 — `profiles` 는 4명인데 랭킹에는 3명만 나옵니다(김갱TV 없음). 같은 조회로 확인됩니다.

---

## ✅ TL-011 · [P3 · 닫힘] 폰에서 공지 제목이 6글자쯤에서 잘림

**등록** 2026-08-24 · **상태** ✅ 닫힘 (2026-08-24 18:34, + 19:08 보강) · **담당** 디자인팀

**무엇으로 닫혔나** — 커밋 `23aaf64` "점검 1회차 P3 4건" · `style.css`.
원인은 `style.css:1889` 가 폰에서도 2칸을 강제해 박스가 167px 밖에 안 됐던 것입니다.
**실측** 360 제목 줄 `141px(296px 숨음) → 316px, 숨는 양 0`. 글자를 `…` 로 없앤 곳 없음.

**보강** — 커밋 `205e1c1` 에서 701~767px 구간도 같이 고쳤습니다
(2칸 그리드라 칸 하나가 338px 로 360 폰 한 칸 342px 보다 좁았습니다).
768 `120px 숨음 → 0`. 360~1920 전 구간 숨는 양 0.

제목 줄 폭 360=141px / 375=149 / 390=156. `[공지]` 태그가 40px 를 먹습니다.
`[공지] 랭킹은 청산된 거래(실현 손익) 기준으로 계산됩니다` → 화면엔 `[공지] 랭킹은 …`

탭 줄(`.notice-board-tabs`)도 250px 필요 / 167px 만 보여 `최신게시물` 이 `초…` 로 잘립니다.
제목을 눌러도 안 열리는 건 정상입니다(원래 클릭 대상이 아님).

---

## ✅ TL-012 · [P3 · 닫힘] 게시판 댓글마다 계급 배지가 2개씩 중복

**등록** 2026-08-24 · **상태** ✅ 닫힘 (2026-08-24 18:34) · **담당** 디자인팀

**무엇으로 닫혔나** — 커밋 `23aaf64` · `style.css`.
원인 확정 — `js/rank-badge-attach.js` 의 `attachChat`/`attachBoard` 가 서로 다른
완료 표시를 써서 상대가 붙인 것을 못 알아봤습니다.
게시판 규격(22px)만 남기고 채팅용을 CSS 로 숨겼습니다. `:has` 로 **뒤에 커뮤니티
배지가 있을 때만** 숨겨서 배지가 0개가 되는 일이 없게 했습니다.
**실측** DOM 8개 보존 · 화면에 보이는 것 `8 → 4`.

댓글 4개 / 배지 8개. 같은 `.board-comment-meta` 안에
`img.rank-badge-chat`(69×20)과 `img.rank-badge-community`(76×22)가 나란히 있고
**둘 다 같은 그림 파일**입니다. 채팅·랭킹은 정상(1개씩)입니다.

---

## ✅ TL-013 · [P3 · 닫힘] 360 에서 게시판 목록 머리글이 붙고 작성일이 잘림

**등록** 2026-08-24 · **상태** ✅ 닫힘 (2026-08-24 18:34) · **담당** 디자인팀

**무엇으로 닫혔나** — 커밋 `23aaf64` · `style.css`.
520px 이하에서만 조회·추천 칸을 접습니다. 마크업은 보존했고 되살리는 법을 주석에
남겼습니다. **실측** 표 `333 필요 / 324 (9px 넘침) → 324/324, 넘침 0`.
작성일 `8/14` 가 온전히 보입니다.

표 333px 필요 / 324px. 칸별로도 좁습니다 — `bg-no` 36/25, `bg-date` 49/35,
`bg-view` 37/28, `bg-like` 37/29. 머리글이 `번호제목 … 글쓴이 작성일조회추천` 로 붙고
작성일 `8/14` 가 `8…` 로 나옵니다.

---

## ✅ TL-014 · [P3 · 닫힘] 비회원인데 마이페이지에 로그아웃·회원탈퇴 버튼이 보임

**등록** 2026-08-24 · **상태** ✅ 닫힘 (2026-08-24 18:34) · **담당** 디자인팀

**무엇으로 닫혔나** — 커밋 `23aaf64`. 기존 `login-required` 방식을 그대로 썼습니다.
회원에게는 변화가 없습니다.
**지금 코드에서 확인** (감사팀 2026-08-28 직접 확인)
```
js/login-required.js:43-44   { id: "mh-logout" } · { id: "mh-delete-account" }
style.css:4575               .mh-box:has(> .mh-actions > #mh-logout.login-required){display:none;}
style.css:5039               #mypage-panel:has(#mh-logout.login-required) .mypage-grid{display:none;}
```
⚠️ **화면으로는 확인 안 했습니다** — 감사팀은 코드만 봤습니다.

`#mh-logout`, `#mh-delete-account` 에 `login-required` 클래스가 없습니다.
`js/mypage-history.js:147` 을 보면 닉네임이 없을 때 안내만 뜨고 끝나므로 **위험은 없어 보입니다.**
다만 **실제로 눌러 확인하지는 않았습니다.**

---

# 2026-08-28 감사팀 등록 — 열려 있는데 백로그에 없던 건

**오늘 밤 운영기록·승인대기에는 있는데 백로그에만 빠져 있던 것들입니다.**
번호가 없으면 다음 날 아침에 통째로 잊힙니다. 그래서 번호를 답니다.

---

## TL-015 · [P2] `auth:changed` 를 13곳이 구독하는데 **발신이 0건**

**등록** 2026-08-28 · **상태** 미착수 · **담당** 미배정 · **발견** 조사팀 (2026-08-27)

```
git log -S'emit("auth:changed")' --all   →  문서 1건뿐. 코드 0건
→ 사라진 게 아니라 애초에 안 만들었습니다
```

로그인은 `js/auth.js:132 App.bootApp()`, 로그아웃은 `js/auth.js:349 location.reload()` 로
덮입니다. **딱 하나, "비회원으로 둘러보다 화면 안에서 로그인" 하는 길에만 새로고침이
없습니다.** 그 길에서만 아래가 깨집니다.

### 실제로 깨지는 곳 — 4곳

| 파일·줄 | 무슨 일이 나나 | 무게 |
|---|---|---|
| `js/my-private-info.js:105` | 비회원으로 마이페이지를 한 번 열면 `loaded=true` 가 박힙니다. 로그인해도 되돌릴 길이 `auth:changed` 뿐이라 **로그인했는데 계속 "로그인 후 확인할 수 있습니다"** 가 남습니다 | 눈에 보이는 고장 |
| `js/account-isolation.js:105` | `check()` 가 0·1.2·3초 세 번만 돕니다. 3초 넘게 구경 → 화면 안에서 다른 계정으로 로그인 → **앞 계정 거래 데이터가 로컬에 남습니다.** 이 모듈이 정확히 그 상황을 막으려고 만든 것인데 그 상황에서 안 됩니다 | 조건부 P1 성격 |
| `js/sync-guard.js:161` | `loadBaseline()` 이 2.5초에 한 번만 돌아 그때 비회원이면 `serverBaseline` 이 영원히 `null` → `looksLikeDataLoss()` 가 항상 `false`. 다만 지금 sync-guard 는 저장을 막지 않고 경고 띠만 띄웁니다 | 실피해 작음 |
| `js/mypage-history.js:216` | `loaded` 래치를 되돌리는 구독자 | **감사팀 보충 (2026-08-28)** |

> ⚠️ **감사팀 보충** — 운영기록(`2026-08-27.md:1158`)은 "고장 3곳" 으로 적었고
> `mypage-history` 를 "안 불려도 되는 것" 으로 분류했습니다. 코드를 직접 보니
> **`js/mypage-history.js:26·133·213·216` 에 같은 `loaded` 래치가 있습니다.**
> 다만 **`:218` 의 `trading:persisted` 라는 두 번째 되돌림 길이 있어서**
> 앞의 셋보다 가볍습니다. 그래서 **4곳으로 세되 무게는 다르게** 적습니다.

### 왜 아직 안 고쳤나

발신을 새로 만들면 13곳이 한꺼번에 다시 도는 것이라 **어디에서 발신할지부터 정해야 합니다.**
`js/auth.js` 는 수정 금지 파일이라 우회 모듈이 필요합니다.
**조사팀 확신도 "확실". 수리 방식은 미확정 — 착수 전에 방식을 먼저 정해야 합니다.**

---

## 🔴 TL-016 · [P1] 가입할 때 지갑 줄이 안 만들어지면 랭킹에서 통째로 사라집니다

**등록** 2026-08-28 · **상태** ⏸ 대표 SQL 결과 대기 · **담당** 미배정 · **발견** 조사팀 (확신도 확실)

TL-010 의 곁가지("`profiles` 는 4명인데 랭킹에는 3명, 김갱TV 없음")를 분리한 건입니다.

```
js/auth.js:317   trading_accounts INSERT
js/auth.js:323   if (accErr && accErr.code !== "23505") console.warn(...)   ← 실패해도 그냥 넘어갑니다
supabase/schema-leaderboard-floor.sql:80   join public.profiles p ...       ← inner join
```

**가입은 "완료" 라고 뜨는데 랭킹에서는 그 회원이 아예 없습니다. 오류도 안 나고 본인도 모릅니다.**
전형적인 조용한 고장이고, CLAUDE.md 의 P1 정의(회원이 잘못된 정보로 판단)에 그대로 들어맞습니다.

**저절로 낫는 조건** — 거래를 한 번이라도 하면 `js/supabase-sync.js` 의 `upsert` 가
줄을 만들어 복구됩니다. 그래서 **거래를 한 번도 안 한 회원만** 사라진 채로 남습니다.

**막힌 것** — 실제로 몇 명인지 세려면 SQL 을 돌려야 하는데 **SQL 실행은 대표 몫**입니다.
조회 파일의 `[7]` 블록이 이걸 셉니다. **기존 블록은 전부 `inner join` 이라
지갑 줄이 없는 회원은 그 조회에도 안 나옵니다. `[7]` 만 `left join` 입니다.**

⚠️ `js/auth.js` 는 **수정 금지 파일**입니다. 고치려면 서버 쪽(트리거)이거나 우회 모듈입니다.

---

## 🔴 TL-017 · [P1] 가입할 때 랭킹 점수를 만들어낼 수 있습니다

**등록** 2026-08-28 · **상태** ⏸ 대표 결재 대기 (승인대기 8번) · **담당** 미배정

`docs/운영기록/승인대기.md:28`

```
| 8 | 🔴 높음 | 가입할 때 랭킹 점수를 만들어낼 수 있습니다 | 예/아니오 한 글자 | 계급 0점 → 3,459점을 공짜로 | 08-28 |
```

**대표가 "예/아니오" 한 글자만 답하면 풀립니다.** 그전에는 착수하지 않습니다.
계급·랭킹 점수에 닿아 있어 **계산식 영역**이고, 계산식 변경은 대표 확인 항목입니다.

---

## TL-018 · [P2] 전쟁터 매수·매도 막대가 영구히 50:50

**등록** 2026-08-28 · **상태** 미착수 · **담당** 미배정 · **발견** 조사팀 (2026-08-27)

**TL-005(주문창 압력바)와 다른 파일, 다른 화면입니다.** 압력바는 지금 정상이고 이건 아닙니다.

```
js/market-war.js:626-638  updatePowerBarDom()
                          #mw-power-buy / #mw-power-sell  (index.html:753-754)

실측 (SK하이닉스 보는 중)
  mw-buy-pct 98%   mw-sell-pct 100%     ← 강도가 둘 다 상한에 붙어 있음
  막대 폭 49.4989% / 50.5011%           ← 사실상 영구 50:50
```

**체결이 아무리 와도 반반입니다.** 원인은 스트림이 아니라 **연출용 강도값이 포화된 것**입니다.
`js/market-war.js:628` · `:1259` 에 `total 0 → 50` 지어내기도 남아 있습니다.

⚠️ **손대면 전쟁터 연출이 같이 바뀝니다.** 어디까지 바꿀지 먼저 정해야 합니다.

---

## TL-019 · [P3] 채팅 경고가 435건 쌓입니다

**등록** 2026-08-28 · **상태** 미착수 · **담당** 미배정 · **발견** 차트팀

⚠️ **감사팀은 이 숫자의 원본 기록을 찾지 못했습니다.**
`docs/운영기록/` 어디에도 435 라는 수치가 없습니다(본부장 구두 전달분).
**착수 전에 몇 건인지·어떤 경고인지 다시 세야 합니다.** 지금 상태로는 재현 조건이 없습니다.

콘솔 경고는 회원 화면을 망가뜨리지 않지만, 쌓이면 **진짜 오류가 그 안에 묻힙니다.**
그래서 지우는 게 아니라 "왜 나는지" 를 봐야 하는 건입니다.

---

# 2026-08-28 감사팀 등록 — 이미 끝난 것 (기록용)

**백로그에 번호가 없던 채로 끝난 일입니다. 나중에 "그거 했었나" 를 찾을 수 있게 남깁니다.**

---

## ✅ TL-020 · [P1 · 닫힘] 화면 레버리지 상한이 엔진 상한보다 높아질 수 있었습니다

**등록** 2026-08-28 · **상태** ✅ 닫힘 (2026-08-28 02:04) · **담당** 수리팀

**무엇으로 닫혔나** — 커밋 `f491fa5`.

```
js/trading.js:96      const MAX_LEVERAGE = 125;        ← 엔진 상한. 수정 금지 파일
js/leverage-modal.js  프리셋 1~100, 슬라이더 max 100    ← 회원이 고르는 값
```

**실측** — `setLeverage(150)` 을 불러도 엔진 실제값은 **125** 입니다.
화면을 엔진보다 높게 올리면 회원은 그 배율로 잡은 줄 아는데 엔진이 조용히 깎습니다.
오류도 안 나는 조용한 고장입니다.

**봉인** — `tests/leverage-engine-cap.test.js` 가 **화면 상한 ≤ 엔진 상한** 을
1~1000 까지 실제로 눌러서 검사합니다. 누가 화면만 올리면 그 자리에서 터집니다.

⚠️ **엔진 상한을 올리는 것은 `js/trading.js:96` 을 여는 일이라 대표 결재 사항입니다.**

---

## ✅ TL-021 · [P2 · 닫힘] 종목을 바꾸면 화면이 오래 멈춰 있었습니다

**등록** 2026-08-28 · **상태** ✅ 닫힘 (2026-08-28 02:28) · **담당** 수리팀

**무엇으로 닫혔나** — 커밋 `960d747` "종목 전환이 5.8초에서 2.1초로 — 원인은 둘이었습니다".

**실측 5.8초 → 2.1초.**
⚠️ 본부장 메모에는 `7초 → 3.7초` 로 적혀 있었는데 **커밋에 남은 실측은 5.8 → 2.1** 입니다.
감사팀은 커밋 쪽 숫자를 채택했습니다.
