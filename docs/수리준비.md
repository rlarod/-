# 수리준비 — 수리팀 조사 결과

주말 모드입니다. **아무 코드도 고치지 않았습니다.** 원인 확인과 고치는 방법 준비만 했습니다.

조사 일시: 2026-08-21 · 조사자: 수리팀 · 환경: localhost:3000, 비로그인
수정 금지 파일 12개 해시: **12/12 일치** (조사 시작 시점·종료 시점 모두)

---

## [P1] 최근 체결 목록이 영구히 비어 있음

### 원인

**확인됨 (추측 아님).**

`js/config.js` 131~138행의 `buildCombinedStreamUrl()` 이 만드는 접속 주소가 이것입니다.

```
wss://fstream.binance.com/market/stream?streams=btcusdt@kline_1m/btcusdt@ticker/btcusdt@trade/btcusdt@markPrice@1s
```

바이낸스 선물은 2026-04-23 주소 개편에서 스트림을 경로별로 나눴는데,
**체결(`@trade`) 스트림은 `/public` 경로에만 있고 `/market` 경로에는 없습니다.**
그런데 바이낸스는 없는 스트림 이름을 받아도 **오류를 내지 않고 연결을 유지**합니다.
그래서 연결은 멀쩡하고, 같은 연결의 kline·ticker·markPrice 는 정상으로 오는데,
`@trade` 만 **한 건도 안 옵니다.**

`js/websocket.js` 125행의 `data.e === "trade"` 분기가 영원히 실행되지 않고,
131행의 `App.Bus.emit("trade:tick", ...)` 도 영원히 실행되지 않습니다.
`js/trades.js` 는 정상입니다 — 올 것이 안 올 뿐입니다.

**끊기는 지점은 2번입니다** (본부장이 제시한 4가지 중):
구독 URL 에 이름은 넣지만, 그 경로에 그 스트림이 없어서 메시지가 안 옵니다.

### 확인한 근거

**(가) 사이트 밖에서 같은 주소로 직접 붙어 봤습니다** (읽기 전용, 사이트 무수정)

| 접속 주소 | 관측 | 결과 |
|---|---|---|
| `/market/stream?...btcusdt@trade` (사이트가 쓰는 그 주소) | 15초 | kline 42 · markPrice 15 · ticker 7 · **trade 0** |
| `/market/stream?...@aggTrade/@trade/@ticker` | 15초 | **aggTrade 473** · ticker 8 · **trade 0** |
| `/market/ws/btcusdt@aggTrade` | 8초 | **aggTrade 234** |
| `/public/ws/btcusdt@trade` | 8초 | **trade 737** |
| `/public/ws/btcusdt@aggTrade` | 8초 | **0** |
| `/public/ws/btcusdt@depth5@500ms` (호가창이 쓰는 주소) | 8초 | depthUpdate 15 |

같은 연결·같은 시각에 다른 스트림은 오는데 `@trade` 만 0입니다.
경로를 `/public` 으로 바꾸면 같은 이름이 **초당 92건** 옵니다.
→ 바이낸스에 체결이 없는 것이 아니라, **경로가 틀린 것**입니다.

**(나) 사이트에서 실측** (1920px, 비로그인, 10초)

```
trade:tick        0회      ← 체결
orderbook:update 21회
ticker:update     6회
price:update     32회
kline:update     32회
funding:update   11회

#recent-trades-list 자식 수: 0개
#recent-trades-panel 높이: 575.75px (화면에 멀쩡히 보임)
콘솔 오류: 0건 (경고 2건 — three.js deprecated, GLTFLoader 없음. 무관)
```

본부장 실측(13:45)과 같은 결과입니다. 재현됩니다.

### ⚠️ 같은 원인으로 같이 고장난 것 — 이쪽이 더 큽니다

**`5초`·`15초` 캔들을 고르면 손익과 강제청산이 통째로 멈춥니다.**

`5s`·`15s` 는 바이낸스에 없는 간격이라 `buildCombinedStreamUrl()` 이 kline 스트림을
**아예 빼고**(config.js 134행), 대신 websocket.js 148~163행이 **체결 데이터로 캔들을
직접 만들도록** 되어 있습니다. 그 체결이 안 오므로 —

실측 (5초 간격으로 바꾼 뒤 12초, 1920px):

```
trade:tick     0회
kline:update   0회     ← 차트가 완전히 멈춤
price:update   0회     ← 현재가가 멈춤
ticker:update  3회     ← 상단 24H 바만 계속 움직여서 "살아있는 것처럼" 보임
```

`js/trading.js` 671행은 **`price:update` 하나만** 구독해서 손익·청산가·포지션을
전부 계산합니다. 즉 5초/15초 차트를 보고 있는 회원은

- 미실현 손익이 **멈춘 숫자**로 보이고
- **강제청산 판정이 아예 돌지 않습니다**

기본값이 `1m` 이라 회원이 5초/15초 버튼을 누른 경우에만 해당되지만,
**돈에 직결**되고 **오류가 하나도 안 뜹니다.** 조용한 고장 중에서도 제일 나쁜 쪽입니다.

**추가로** `js/market-war.js` 1216행도 `trade:tick` 을 구독합니다 —
전쟁터의 매수/매도 강도(BUY/SELL INTENSITY)가 체결 기반이라 현재 전부 0 입니다.
(호가 잔량 기반 병력은 살아 있어서 화면은 돌아가는 것처럼 보입니다.)

### 어디를 어떻게 고칠지

**수정 금지 파일 12개를 하나도 안 건드립니다. `js/config.js` 도 안 건드립니다.**
새 파일 1개 + `index.html` 한 줄 + `main.js` 한 단어.

#### ① 새 파일 `js/trade-stream-fix.js` (신규)

호가창(`js/orderbook.js` 70행)이 이미 쓰고 있는 것과 **똑같은 방식**입니다 —
기존 연결을 건드리지 않고 자기 연결을 따로 엽니다.

```js
/* js/trade-stream-fix.js — App.TradeStreamFix
 * 체결(@trade)은 /public 경로에만 있는데 websocket.js 의 연결은 /market 이라
 * 한 건도 오지 않습니다. websocket.js(수정 금지)를 건드리지 않고,
 * 호가창과 같은 방식으로 체결 스트림만 따로 받아 'trade:tick' 을 대신 방송합니다.
 * 바이낸스 화면의 "최근 체결"과 같은 집계 체결(@aggTrade)을 씁니다.
 */
window.App = window.App || {};

App.TradeStreamFix = (function () {
  "use strict";

  var sock = null;
  var delay = 0;
  var synthetic = null; // 5s/15s 간격용 — websocket.js 148~163행과 같은 규칙

  function url() {
    return "wss://fstream.binance.com/market/ws/" +
      App.Config.getActiveSymbol().toLowerCase() + "@aggTrade";
  }

  function onTrade(d) {
    var price = parseFloat(d.p);
    if (isNaN(price)) return;
    var qty = parseFloat(d.q) || 0;

    App.Bus.emit("trade:tick", {
      symbol: d.s,
      price: price,
      qty: qty,
      isBuyerMaker: !!d.m,
      time: d.T || Date.now()
    });

    // 비-native 간격(5s/15s)일 때만 캔들·현재가를 대신 만들어 줍니다.
    // native 간격일 때는 websocket.js 의 kline 스트림이 이미 하고 있으므로
    // 절대 내보내지 않습니다(두 번 나가면 차트가 어긋납니다).
    var interval = App.Config.getActiveInterval();
    if (App.Config.isNativeInterval(interval)) return;

    var bucketSec = App.Config.intervalToSeconds(interval);
    var tSec = d.T ? Math.floor(d.T / 1000) : Math.floor(Date.now() / 1000);
    var bucket = Math.floor(tSec / bucketSec) * bucketSec;
    if (!synthetic || synthetic.time !== bucket) {
      synthetic = { time: bucket, open: price, high: price, low: price, close: price, volume: qty };
    } else {
      synthetic.high = Math.max(synthetic.high, price);
      synthetic.low = Math.min(synthetic.low, price);
      synthetic.close = price;
      synthetic.volume += qty;
    }
    App.Bus.emit("kline:update", { symbol: d.s, candle: synthetic });
    App.Bus.emit("price:update", { symbol: d.s, price: price, time: Date.now() });
  }

  function connect() {
    var s;
    try { s = new WebSocket(url()); } catch (e) { retry(); return; }
    sock = s;
    s.onopen = function () { delay = App.Config.RECONNECT_MIN_MS; };
    s.onmessage = function (evt) {
      var d;
      try { d = JSON.parse(evt.data); } catch (e) { return; }
      if (!d || d.e !== "aggTrade") return;
      onTrade(d);
    };
    s.onerror = function () { try { s.close(); } catch (e) {} };
    s.onclose = function () { retry(); };
  }

  function retry() {
    setTimeout(connect, delay || App.Config.RECONNECT_MIN_MS);
    delay = Math.min((delay || App.Config.RECONNECT_MIN_MS) * 1.6, App.Config.RECONNECT_MAX_MS);
  }

  function init() {
    connect();
    App.Bus.on("interval:change", function () { synthetic = null; });
  }

  return { init: init };
})();
```

#### ② `index.html` — `js/trades.js` 바로 위(1078행 근처)에 한 줄

```html
<script src="js/trade-stream-fix.js"></script>
```

#### ③ `main.js` 66행 `modules` 배열 — `"RecentTrades"` 바로 앞에 이름 추가

```js
..., "OrderbookMarkPrice", "TradeStreamFix", "RecentTrades", "OrderbookTabs", ...
```

(순서는 상관없지만 `"WS"` 보다 앞이면 됩니다. `main.js` 는 각 모듈 init 을
try/catch 로 감싸므로 이 모듈이 실패해도 거래 기능은 안 멈춥니다.)

#### 왜 `@aggTrade` 인가 (선택 근거)

| | `/public/ws/…@trade` | `/market/ws/…@aggTrade` ← 권장 |
|---|---|---|
| 실측 유량 | 초당 **92건** | 초당 **29건** |
| 내용 | 체결 1건씩 전부 | 같은 가격·같은 주문 체결을 묶음 |
| 바이낸스 화면 | 다름 | **바이낸스 "최근 체결"과 같은 것** |
| 필드 | `p q m T s` | `p q m T s` (동일) |

`market-war.js` 는 체결 1건마다 최근 80개 배열을 정렬합니다.
초당 92건이면 초당 92회 정렬이라 저사양 폰에서 부담이 됩니다.
바이낸스 화면과 맞추는 목적으로도 `@aggTrade` 가 맞습니다.
**체결 1건 단위가 꼭 필요하다고 판단되면** 주소만 `/public/ws/...@trade` 로 바꾸고
`d.e !== "aggTrade"` 를 `d.e !== "trade"` 로 바꾸면 됩니다 (둘 다 실측으로 동작 확인).

### 수정 금지 파일 12개를 안 건드리고 되는가

**됩니다.** 12개 중 어느 것도 열지 않습니다.

- `js/websocket.js` — 안 건드립니다. `@trade` 분기가 계속 죽어 있지만,
  `handleMessage()` 최상단에서 `lastMessageAt` 을 갱신하는 것은 kline/ticker/markPrice 가
  하고 있어서 좀비 감시(215~238행)가 오작동하지 않습니다.
- `js/orderbook.js` — 안 건드립니다. 이미 `/public/ws` 로 자기 연결을 갖고 있어
  **연결을 하나 더 여는 것 자체가 이 프로젝트의 기존 패턴**입니다.
- `js/chart.js`, `js/trading.js` — 안 건드립니다. `kline:update` / `price:update` 를
  받는 쪽 코드는 그대로이고, 지금 아무것도 안 오던 5s/15s 에서만 새로 오게 됩니다.
- `js/config.js` 도 안 건드립니다 (12개는 아니지만 굳이 안 건드리는 쪽을 골랐습니다).

### 다른 데가 깨질 위험

| 위험 | 정도 | 근거 / 대응 |
|---|---|---|
| `price:update` 가 두 곳에서 나감 | **없음** | native 간격에서는 이 모듈이 `price:update` 를 안 내보냅니다. 비-native 간격에서는 websocket.js 가 kline 스트림 자체를 구독하지 않으므로 겹칠 수가 없습니다 (실측: 5s 에서 price:update 0회) |
| 연결이 하나 늘어남 | 낮음 | 호가창이 이미 같은 일을 합니다(총 3개). 데이터 양은 원래 combined URL 에 들어 있어야 했던 양과 같습니다 |
| `price-guard.js` 가 값을 막음 | 낮음 | `price-guard.js` 107행은 `price:update` 값의 이상치만 검사합니다. 바이낸스 실제 체결가라 통과합니다. **다만 5s/15s 에서 첫 가격이 들어올 때 가드가 어떻게 반응하는지는 확인 못 했습니다** |
| `market-war.js` CPU | 낮음 | `@aggTrade`(초당 29건) 선택으로 완화. 그래도 폰에서 프레임 저하가 없는지 봐야 합니다 |
| 화면이 갑자기 바빠 보임 | — | 지금 0줄이던 패널에 초당 수십 줄이 흐릅니다. 이것이 정상입니다(바이낸스와 동일) |

### 되돌리는 방법

```bash
cd C:/Users/pc/Documents/projects/trading-league
git checkout -- index.html main.js
rm js/trade-stream-fix.js
```

(커밋 이후라면 `git revert <커밋해시>` 한 번으로 끝납니다. 새 파일 1개 + 두 파일 1줄씩이라
되돌림이 깨끗합니다.)

### 확인 방법 (고친 뒤 이걸로 재면 됩니다)

**① 신호가 오는지 — 10초 측정, 1920px, 비로그인**

```bash
timeout 120 bash -c '
agent-browser set viewport 1920 1080
agent-browser open http://localhost:3000
agent-browser wait 5000
agent-browser eval "(()=>{const c={};[\"trade:tick\",\"orderbook:update\"].forEach(n=>{c[n]=0;App.Bus.on(n,()=>c[n]++)});window.__c=c;return 1;})()"
agent-browser wait 10000
agent-browser eval "JSON.stringify({counts:window.__c,rows:document.getElementById(\"recent-trades-list\").childElementCount})"
'
```

- 합격 기준: `trade:tick` **10초에 100회 이상** (지금 0회), `rows` **30개**
- 불합격: 0회거나, 행은 생겼는데 시간이 안 바뀌면 실패

**② 5초 간격에서 손익이 다시 움직이는지**

간격을 `5초` 로 바꾸고 12초 측정 → `kline:update`, `price:update` 가 **각각 0이 아니어야** 합니다
(지금은 둘 다 0). 차트 봉이 움직이고 포지션 손익 숫자가 바뀌는지 눈으로도 확인합니다.

**③ 바이낸스와 나란히 대조**

바이낸스 선물 BTCUSDT 화면의 "최근 체결" 맨 윗줄 가격과 우리 화면 맨 윗줄 가격이
같은 시각에 몇 달러 차이인지 적습니다. 수십 달러씩 벌어지면 지연이 있는 것입니다.

**④ 6개 폭 전부** (360/375/390/768/1440/1920) 에서 체결이 흐르는지.
1920 미만에서는 탭이라 `최근거래` 탭을 눌러야 보입니다 (`js/orderbook-tabs.js` 의 `canStack()`).

**⑤ 콘솔 오류 0건**, 수정 금지 12개 해시 12/12.

---

## TL-001 · [P2] 폰에서 펀딩비가 안 보임

### 원인

**확인됨. 다만 백로그에 적힌 해법은 실측 결과 틀렸습니다.**

`docs/백로그.md` 41행은 *"라벨을 좁은 화면에서만 `펀딩비` 로 줄이면 들어갑니다"* 라고
적혀 있는데, **줄여도 안 들어갑니다.** 실제로 줄여서 재봤습니다.

390px 실측 (`.stats-bar` 는 `overflow-x:auto` 로 가로 스크롤되는 한 줄):

| 항목 | 폭 | 오른쪽 끝 위치 |
|---|---|---|
| 화면에서 쓸 수 있는 폭 | **372px** | — |
| BTCUSDT | 136 | 145 ✅ |
| 마크가격 | 153 | 304 ✅ |
| 펀딩비 (다음 정산까지) | 337 | **648 ❌** |
| 24H 변동률 | 152 | 805 ❌ |
| 24H 고가 / 저가 / 거래량 | 169 / 169 / 197 | 980 / 1156 / 1358 ❌ |
| `.stats-bar` 전체 | clientWidth 372 / scrollWidth **1349** | |

라벨만 `펀딩비` 로 줄이면 → 폭 337 → **236**, 오른쪽 끝 648 → **546**.
**여전히 372 밖입니다.** 펀딩비 블록이 시작하는 지점이 이미 310px 이라
남은 자리가 62px 밖에 없습니다. 라벨을 아무리 줄여도 못 들어갑니다.

### 어디를 어떻게 고칠지

한 줄을 고집하는 한 물리적으로 불가능하므로 **두 줄로 접는 것**이 답입니다.
360px 에서 실제로 접어 보고 쟀습니다.

`style.css` — `js/stats-bar-priority.js` 관련 블록(3574행 이후) 끝에 추가:

```css
/* 좁은 화면에서는 시세 바를 한 줄로 고집하지 않고 접습니다.
   한 줄을 유지하면 펀딩비가 화면 밖 546px 지점에 남습니다(390 실측).
   이 블록만 지우면 원래대로 돌아옵니다. */
@media (max-width:430px){
  .stats-bar{flex-wrap:wrap; overflow-x:visible; row-gap:6px;}
  .tl-stats-hint-layer::after{display:none;}   /* 스크롤 힌트 그림자 — 접으면 필요 없음 */
}
```

360px 실측 결과 (`flex-wrap:wrap; overflow-x:visible; row-gap:6px` 를 실제로 적용해서 잼):

| | 적용 전 | 적용 후 |
|---|---|---|
| `.stats-bar` scrollWidth | 1349 | **342** (= clientWidth, 숨는 것 없음) |
| 화면 밖에 있는 항목 | 5개 (변동률·고가·저가·거래량·펀딩비) | **0개** |
| `.stats-bar` 높이 | 68px | **192px** (+124px) |
| 문서 가로 스크롤 | 없음 | 없음 (360 = 360) |

**대가는 시세 바가 124px 높아지는 것**입니다. 차트가 그만큼 아래로 밀립니다.
여기서부터는 디자인 판단이라 **디자인팀·본부장 결정이 필요합니다.**
높이가 부담이면 좁은 화면에서 라벨을 줄이는 것(`펀딩비 (다음 정산까지)` → `펀딩비`,
`24H 변동률` → `변동률`)을 같이 적용해 줄 수를 줄일 수 있습니다 — 다만 그것만으로는
안 되고 접기가 반드시 같이 필요합니다.

라벨을 줄이려면 문구를 만드는 곳이 **`js/chart.js` 71행(수정 금지)** 이므로
별도 모듈이 필요합니다.

```js
/* js/stat-label-narrow.js (신규) — 좁은 화면에서 라벨만 줄입니다.
   chart.js 는 값(#stat-funding)만 다시 쓰고 라벨은 한 번 만든 뒤 안 건드리므로
   (chart.js 62~85행, el("stat-funding") 가 있으면 다시 안 만듦) 덮어쓰기 싸움이 없습니다. */
```

**데이터를 숨기는 것이 아니라 자리를 바꾸는 것**이라 원칙 1-2 에 걸리지 않습니다.

### 수정 금지 파일 12개를 안 건드리고 되는가

**됩니다.** CSS 만으로 접기가 되고, 라벨까지 줄인다면 별도 모듈로 합니다.
`js/chart.js` 는 열지 않습니다.

### 다른 데가 깨질 위험

- 시세 바가 124px 높아져 **차트·호가창·주문창이 전부 아래로 밀립니다.** 360~430 전 구간 재확인 필요
- `js/stats-bar-priority.js` 의 스크롤 힌트 그림자가 접힌 상태에서도 떠서 마지막 줄을 덮을 수 있음 → 위 CSS 에서 같이 껐습니다
- 431px 이상은 아무 변화 없음 (미디어쿼리 안에만 있음)

### 되돌리는 방법

```bash
git checkout -- style.css        # (라벨 모듈까지 넣었다면) rm js/stat-label-narrow.js && git checkout -- index.html main.js
```

### 확인 방법

360 / 375 / 390 / 430 네 폭에서:

```js
const bar = document.querySelector('.stats-bar');
JSON.stringify({
  숨는양: bar.scrollWidth - bar.clientWidth,          // 0 이어야 함
  바깥항목: [...bar.querySelectorAll('.stat-block')]
    .filter(b => b.getBoundingClientRect().right > bar.getBoundingClientRect().right).length,  // 0
  바높이: Math.round(bar.getBoundingClientRect().height),
  문서가로: document.documentElement.scrollWidth - document.documentElement.clientWidth   // 0
});
```

그리고 **펀딩비 값이 실제 숫자로 채워져 있는지** (`-` 가 아닌지) 눈으로 확인.
768/1440/1920 은 **바 높이가 그대로인지**(변하면 안 됨) 확인.

---

## TL-002 · [P2] 호가창 헤더가 통화를 따라가지 않음

### 원인

**확인됨.** `index.html` 333행에 글자로 박혀 있습니다.

```html
<span class="ob-cols"><span>가격(USDT)</span><span>수량(BTC)</span><span>총수량(BTC)</span></span>
```

`js/config.js` 82~88행 `setDisplayCurrency()` 는 `currency:change` 를 방송하고
값(`js/utils.js` 의 `formatCurrency`)은 원화로 바뀌지만, **이 헤더 글자를 바꾸는 코드가
어디에도 없습니다.** 그래서 값은 `112,261,650원` 인데 머리글은 `가격(USDT)` 로 남습니다.

**그런데 더 큰 문제가 따로 있습니다.**
통화 전환 버튼이 들어 있는 `.top-banner-right` 가 `style.css` 2112행에서
`display:none` 으로 숨겨져 있습니다(2026-08-17 커밋 `8fcb903`, 의도된 숨김).
선택한 통화는 `App.Storage` 의 `settings` 키에 **저장**됩니다(`js/config.js` 64~77행).

→ **8/17 이전에 KRW 로 바꿔 둔 회원은 KRW 에 갇혀 있고, 되돌릴 버튼이 없습니다.**
몇 명인지는 각 회원 브라우저를 봐야 알 수 있어 **확인 못 했습니다.**

### 어디를 어떻게 고칠지

두 가지를 나눠서 봐야 합니다.

**(가) 헤더 글자 — 쉬움.** `index.html` 은 수정 금지 12개가 아닙니다.
다만 값이 KRW 로 안 바뀌는 회원에게는 무의미하므로 (나)와 같이 가야 합니다.

새 모듈 `js/ob-header-currency.js`:

```js
/* 호가창/최근체결 머리글을 현재 표시 통화에 맞춥니다.
   index.html 의 글자를 지우지 않고 다시 쓰기만 합니다(원칙 1-2). */
function apply() {
  var cur = App.Config.getDisplayCurrency();          // "USDT" | "KRW"
  var label = cur === "KRW" ? "가격(원)" : "가격(USDT)";
  var head = document.querySelector("#orderbook-panel .ob-cols span");
  if (head) head.textContent = label;
}
function init() { apply(); App.Bus.on("currency:change", apply); }
```

`js/orderbook.js`(수정 금지)는 `.ob-cols` 를 다시 그리지 않으므로 덮어쓰기 싸움이 없습니다.
(`js/trades.js` 가 만드는 최근 체결 머리글은 `가격 / 수량 / 시간` 이라 통화 표기가 없어
손댈 것이 없습니다.)

**(나) KRW 에 갇힌 회원 — 대표 판단이 필요합니다.** 셋 중 하나를 골라야 합니다.

1. 통화 전환 버튼을 다시 보이게 한다 (`style.css` 2112행 한 줄 삭제) — 8/17 에 **일부러**
   숨긴 것이라 되살리는 것은 **대표 결정 사항**입니다
2. 저장된 통화를 USDT 로 한 번만 되돌리는 1회성 모듈을 넣는다 — 회원의 저장값을
   임의로 바꾸는 것이라 역시 대표 확인 필요
3. 그대로 둔다 (대표가 이미 "뒤로 미룬다"고 판단한 건)

**수리팀은 (가)만 준비했습니다. (나)는 착수하지 않습니다.**

### 수정 금지 파일 12개를 안 건드리고 되는가

**됩니다.** `index.html` + 새 모듈만. `js/orderbook.js` 는 열지 않습니다.

### 다른 데가 깨질 위험

낮음. 글자 하나를 다시 쓰는 것뿐입니다.
다만 `가격(원)` 이 `가격(USDT)` 보다 짧아 좁은 화면 정렬이 살짝 달라질 수 있어
360 에서 확인이 필요합니다.

### 되돌리는 방법

```bash
git checkout -- index.html main.js && rm js/ob-header-currency.js
```

### 확인 방법

콘솔에서 `App.Config.setDisplayCurrency("KRW")` → 머리글이 `가격(원)` 으로 바뀌고
값도 원화인지. 다시 `("USDT")` → 원복되는지. 새로고침 후에도 유지되는지.
**단, 전환 버튼이 숨겨져 있어 회원은 이 경로를 쓸 수 없습니다** — 이 점을 보고에 적어야 합니다.

---

## TL-003 · [P3] 3D 병사 모델이 안 보임

### 원인

**확인됨 (실물 파일로 검증).**

`index.html` 32~39행이 GLTFLoader 를 이렇게 불러옵니다.

```html
<script type="module">
  import { GLTFLoader } from "https://cdn.jsdelivr.net/npm/three@0.160.0/examples/jsm/loaders/GLTFLoader.js";
```

그 파일의 실제 내용을 받아서 확인했습니다 — **67행이 `} from 'three';`** 입니다.
`'three'` 는 경로가 아니라 이름(bare specifier)이라, 브라우저가 어디서 찾을지 모릅니다.
페이지에 `<script type="importmap">` 이 **없어서** 이 import 가 실패하고,
`window.GLTFLoader` 가 끝내 안 만들어집니다. `SkeletonUtils.js` 도 똑같습니다.

`js/market-war.js` 1236~1241행이 `three-addons-ready` 이벤트와 500ms 안전장치로
기다리다가 포기하고 경고만 남깁니다 — **콘솔 실측에서 그 경고가 그대로 찍힙니다:**

```
[market-war.js] GLTFLoader를 찾을 수 없어 병사 모델을 표시할 수 없습니다(나머지 기능은 정상 동작).
Scripts "build/three.js" and "build/three.min.js" are deprecated with r150+, ...
```

**오류(error)는 0건**입니다. 경고 2건뿐입니다.

### 어디를 어떻게 고칠지

`index.html` 28행 위에 importmap 을 추가합니다 (수정 금지 파일 아님).

```html
<script type="importmap">
{ "imports": {
    "three": "https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.module.js",
    "three/addons/": "https://cdn.jsdelivr.net/npm/three@0.160.0/examples/jsm/"
} }
</script>
```

importmap 은 **첫 모듈 스크립트보다 반드시 앞**에 있어야 합니다.

### ⚠️ 이 방법의 위험 — 그냥 넣으면 안 됩니다

importmap 을 넣으면 three 가 **두 벌** 로드됩니다.

- 기존 `three.min.js`(UMD) → 전역 `THREE`. `market-war.js` 가 이걸 씁니다
- importmap 이 가리키는 `three.module.js`(ESM) → GLTFLoader 가 이걸 씁니다

두 벌은 서로 다른 클래스라서, GLTFLoader 가 만든 Mesh 를 UMD 쪽 Scene 에 넣으면
동작할 수도 있고(three 는 대부분 `.isMesh` 같은 표시로 판별) 재질·스키닝에서
어긋날 수도 있습니다. **적용해 보고 병사가 실제로 보이는지 눈으로 확인해야 합니다.**

**깔끔한 쪽(권장하지만 더 큼)**: UMD 를 버리고 전부 모듈 빌드 하나로 통일합니다.
그런데 모듈 스크립트는 항상 지연 실행이라, `main.js` 가 `MarketWar.init()` 을 부를 때
전역 `THREE` 가 아직 없어서 `market-war.js` 1197행이 조용히 빠져나갑니다
(→ 전쟁터가 통째로 안 뜨는 **더 큰 회귀**). 이걸 하려면 `market-war.js` 또는 `main.js` 에서
"THREE 준비될 때까지 기다렸다가 init" 하도록 순서를 바꿔야 합니다.
`market-war.js`·`main.js` 둘 다 수정 금지 파일은 아니라 가능은 합니다.

**P3 이므로 서두를 이유가 없습니다.** 대표·본부장이 어느 쪽인지 정한 뒤 착수합니다.

### 수정 금지 파일 12개를 안 건드리고 되는가

**됩니다.** `index.html`(+ 필요시 `market-war.js`, `main.js`) 만.

### 다른 데가 깨질 위험

- three 두 벌 → 재질/애니메이션이 어긋날 수 있음 (**반드시 눈으로 확인**)
- 로드 용량이 늘어남 (three.module.js 약 600KB 추가) → 폰에서 첫 로딩이 느려짐. **실측 필요**
- 깔끔한 쪽을 고르면 전쟁터가 통째로 안 뜰 위험이 있어 순서 처리가 필수

### 되돌리는 방법

```bash
git checkout -- index.html
```

### 확인 방법

- 콘솔에서 `[market-war.js] GLTFLoader를 찾을 수 없어…` 경고가 **사라졌는지**
- `typeof window.GLTFLoader` 가 `"function"` 인지
- 전쟁터 화면에 병사가 실제로 보이는지 (**캡처 필수**)
- 전쟁터 외 나머지 화면(차트·호가창·주문)이 그대로인지
- 폰(360)에서 첫 화면이 뜨는 시간이 눈에 띄게 늘지 않았는지

---

## [P3] 수수료 표기 자릿수가 다름 (`0.02% / 0.050%`)

### 원인

**확인됨.** `js/order-info-panel.js` **96행** 한 줄입니다.

```js
dom.accFeeRate.textContent = (snapshot.feeRate.maker * 100).toFixed(2) + "% / " + (snapshot.feeRate.taker * 100).toFixed(3) + "%";
```

메이커는 소수점 **2자리**(`toFixed(2)` → `0.02%`), 테이커는 **3자리**(`toFixed(3)` → `0.050%`).
값 자체는 맞습니다 (`js/trading.js` 46~49행: 메이커 0.0002 / 테이커 0.0005 —
바이낸스 USDⓈ-M 일반 사용자 기준과 일치). **자릿수만 안 맞습니다.**

### 어디를 어떻게 고칠지

`js/order-info-panel.js` 96행. **수정 금지 파일이 아닙니다. 그 파일에서 바로 고칩니다.**

```js
// 바이낸스와 같이 소수점 4자리로 통일합니다 (0.0200% / 0.0500%).
dom.accFeeRate.textContent =
  (snapshot.feeRate.maker * 100).toFixed(4) + "% / " + (snapshot.feeRate.taker * 100).toFixed(4) + "%";
```

**자릿수를 몇 자리로 할지는 본부장/디자인 판단입니다.** 선택지:

| 안 | 결과 | 비고 |
|---|---|---|
| `toFixed(4)` 양쪽 | `0.0200% / 0.0500%` | **바이낸스 화면과 동일 표기** ← 권장 |
| `toFixed(2)` 양쪽 | `0.02% / 0.05%` | 가장 짧음. 나중에 수수료가 0.045% 같이 되면 못 나타냄 |
| `toFixed(3)` 양쪽 | `0.020% / 0.050%` | 절충 |

계산식은 손대지 않습니다 — **표시만 바꾸는 것이라 본부장 재량**입니다(CLAUDE.md 140~144행).

### 수정 금지 파일 12개를 안 건드리고 되는가

**됩니다.** `js/order-info-panel.js` 는 12개에 없습니다.

### 다른 데가 깨질 위험

거의 없음. 글자가 `0.02% / 0.050%`(11자) → `0.0200% / 0.0500%`(17자)로 **6자 길어집니다.**
좁은 화면에서 그 줄이 넘치거나 줄바꿈되지 않는지 **360 에서 확인 필요**.

`js/order-fee-preview.js` 10행 주석의 `테이커 0.05%` 는 주석이라 화면과 무관합니다.

### 되돌리는 방법

```bash
git checkout -- js/order-info-panel.js
```

### 확인 방법

주문 패널의 수수료 줄을 열어 `0.0200% / 0.0500%` 로 보이는지,
360/375/390 에서 줄이 안 넘치는지 (`scrollWidth` vs `clientWidth`) 확인.

---

## [백로그] TL 이 계좌 초기화 때 사라지는 문제 (72,560 → 60)

### 원인

**구조적 원인은 확인했습니다. 다만 "그 날 실제로 무엇이 실행됐는지"는 확인 못 했습니다.**

`supabase/schema-tl-hotdeal.sql` 125~141행:

```sql
create or replace function public.tl_earned(p_uid uuid) ... as $$
  select
    coalesce((select count(*) from public.trades t where t.user_id = p_uid), 0) * 10
    + greatest(0, coalesce((
        select case when ta.initial_balance > 0
                    then (ta.realized_pnl / ta.initial_balance) * 100 else 0 end
        from public.trading_accounts ta where ta.user_id = p_uid), 0)) * 20
    + coalesce((select pr.rank_points from public.profiles pr where pr.id = p_uid), 0);
$$;
```

**획득 TL 은 저장된 값이 아니라 "지금 남아 있는 거래 기록에서 매번 새로 계산하는 값"입니다.**
세 조각 중 두 조각이 거래에 매달려 있습니다.

| 조각 | 근거 | 초기화하면 |
|---|---|---|
| 청산 거래 건수 × 10 | `public.trades` 행 수 | `delete from trades` → **0** |
| 실현 수익률 × 20 | `trading_accounts.realized_pnl` | `realized_pnl = 0` → **0** |
| 수동 가감점 | `profiles.rank_points` | **남음** |

`reset_season()`(`supabase/schema-reset-season-fix.sql` 50~58행)이 `trades` 를 지우고
`realized_pnl = 0` 으로 되돌립니다.

**72,560 → 60 은 이 구조와 정확히 맞습니다** — 남은 60 이 `profiles.rank_points` 이고,
나머지 72,500 은 전부 거래에서 나온 값이라 거래와 함께 사라진 것입니다.

**추측이라고 명시할 부분:** 그 회원의 `rank_points` 가 실제로 60 인지, 그리고 그 날
실행된 것이 `reset_season()` 인지 다른 초기화 경로인지는 **서버를 봐야 알 수 있어
확인 못 했습니다.** SQL 은 실행하지 않았습니다.

**대표가 실행해서 확인할 것** (읽기만 하는 조회입니다):

```sql
-- 그 회원의 rank_points 가 60 인지, 거래가 정말 0건인지
select p.nickname, p.rank_points, public.tl_earned(p.id) as 획득TL,
       (select count(*) from public.trades t where t.user_id = p.id) as 거래건수,
       ta.realized_pnl, ta.initial_balance
from public.profiles p
left join public.trading_accounts ta on ta.user_id = p.id
where p.nickname = '<닉네임>';
```

### 어디를 어떻게 고칠지

**서버(SQL)만 고치면 됩니다. 사이트 코드는 한 글자도 안 고쳐도 됩니다.**

초기화 직전의 획득 TL 을 `tl_transactions` 에 **양수 한 줄로 박제**해서 이월시킵니다.
`tl_balance()` 는 `획득 + tl_transactions 전체 합계` 이므로(`schema-tl-hotdeal.sql` 145~150행)
거래가 사라져 획득이 0 이 되어도 박제해 둔 줄이 그대로 잔액으로 남습니다.

`reset_season()` 안에서, `delete from public.trades` **보다 앞**에:

```sql
  -- 시즌이 바뀌어도 지금까지 모은 TL 은 그대로 이월합니다.
  -- tl_earned() 는 거래에서 매번 새로 계산하는 값이라, 거래를 지우면
  -- 함께 사라집니다. 지우기 "전"에 그 시점의 값을 한 줄로 박제합니다.
  insert into public.tl_transactions (user_id, amount, kind, memo)
  select p.id, public.tl_earned(p.id), 'grant', '시즌 이월'
  from public.profiles p
  where public.tl_earned(p.id) > 0;
```

(`kind` 에 허용된 값은 `tl_transactions` 정의를 먼저 확인해야 합니다 —
`refund`/`grant` 타입이 이미 정의돼 있다고 `schema-tl-balance-fix.sql` 18행에 적혀 있습니다.)

**그리고 현재 `schema-reset-season-fix.sql` 은 `tl_transactions` 를 비웁니다**
(60~62행 주석: "거래가 사라져 획득 TL 이 0 이 되므로, 사용 기록도 함께 비웁니다").
이월 줄을 넣으려면 **비우는 순서와 함께 다시 설계해야 합니다** —
사용 기록(음수)은 비우고, 이월(양수)은 그 뒤에 넣어야 합니다. 순서가 바뀌면 이월이 날아갑니다.

### ⚠️ 먼저 확인해야 할 것 — 서버에 어느 버전이 올라가 있는지

`docs/인계-보충.md` 207~222행이 경고하는 그대로입니다.
`tl_balance_info` 만 파일 2개, `reset_season` 은 3개입니다.

특히 **`supabase/schema-tl-balance-fix.sql` 이 실행됐는지 여부로 결과가 갈립니다.**

| | 잔액 계산 | 이월(양수) 줄이 |
|---|---|---|
| tl-balance-fix **실행됨** | `balance = tl_balance()` = 획득 + 전체 합계 | **잔액에 반영됨 ✅** |
| tl-balance-fix **미실행** | `balance = 획득 − 음수합계` (양수 무시) | **화면에서 사라짐 ❌** |

미실행 상태에서 이월 줄을 넣으면 **구매 판정은 통과하는데 화면 잔액은 0** 이라는
더 나쁜 상태가 됩니다. **`health-check.sql` 결과부터 확인해야 합니다.**

### 수정 금지 파일 12개를 안 건드리고 되는가

**됩니다.** 서버 함수만 바꿉니다. `js/season.js`·`js/admin.js` 는 열지 않습니다.

### 다른 데가 깨질 위험

- **이미 초기화돼서 TL 이 날아간 회원의 복구는 이 수정으로 안 됩니다.**
  지나간 값은 계산해 낼 근거가 서버에 남아 있지 않을 수 있습니다.
  복구하려면 별도 보정(가상 자산 소급 변경)이라 **누가·얼마에서 얼마로** 를 보고에
  남겨야 합니다 (CLAUDE.md 166~174행)
- 랭킹 점수(`rank_points_all`)도 `tl_earned` 를 쓰므로 순위가 함께 움직일 수 있습니다
- **`tl_earned` 는 손익·랭킹 계산식에 해당합니다 → 대표 확인 대상입니다** (CLAUDE.md 178~184행)

### 되돌리는 방법

```sql
-- ① 이월로 넣은 줄만 지웁니다 (WHERE 있는 DELETE 입니다)
delete from public.tl_transactions where kind = 'grant' and memo = '시즌 이월';
-- ② 함수는 이전 파일을 다시 실행하면 원복됩니다
--    supabase/schema-reset-season-fix.sql 를 그대로 다시 실행
```

지우기 전에 몇 건인지 먼저 셉니다:

```sql
select count(*), sum(amount) from public.tl_transactions
where kind = 'grant' and memo = '시즌 이월';
```

### 확인 방법

**실서버에서 시험하지 않습니다.** 대표가 테스트 계정 하나로:

1. 초기화 전 `tl_balance_info()` 의 `earned` / `balance` 를 적어 둔다
2. `reset_season()` 실행
3. 초기화 후 `balance` 가 **1번과 같은지**, `earned` 는 0 에 가까워졌는지
4. `tl_balance(uid)` 와 `tl_balance_info()->>'balance'` 가 **같은 값인지**
   (`schema-tl-balance-fix.sql` 맨 아래 확인 쿼리가 그대로 있습니다)
5. TL 마켓에서 실제로 구매가 되는지 (잔액이 화면에만 있고 판정에서 막히지 않는지)

---

## [백로그] 채팅창 위아래 흔들림 — 재현 안 됨

### 측정 결과

`?jitter=1` 진단 도구(`js/jitter-probe.js`)를 **실제로 돌렸습니다.**
5초 대기 후 0.1초 간격 150회(15초) 측정, 비로그인.

**1920 × 1080**

```
창 너비: 1920px
채팅 칸: 0px 움직임, 0번 변함
높이가 변한 곳: 없음
(지금은 흔들리지 않는 상태입니다)
```

**1440 × 900**

```
창 너비: 1440px
채팅 칸: 0px 움직임, 0번 변함
높이가 변한 곳: 없음
```

19개 감시 대상(문서 전체·헤더·차트·호가창·주문·채팅 메시지·내 정보 박스 등)
**전부 높이 변화 0** 입니다.

### 다만 — 이 결과로 "문제 없음" 이라고 결론내면 안 됩니다

측정 시점에 `#chat-messages` 는 **메시지 12개, 높이 829px 로 이미 채워져 있었고,
측정한 15초 동안 새 메시지가 한 건도 안 들어왔습니다**(그래서 높이 변화 0).

흔들림은 **새 메시지가 들어와 높이가 늘어날 때** 나는 현상일 가능성이 큰데,
그 조건이 측정 중에 일어나지 않았습니다. **방아쇠가 안 당겨진 상태로 잰 것**입니다.

### 다시 재려면

- 채팅이 실제로 흐르는 시간대(회원이 대화 중일 때)에 `?jitter=1` 로 재기
- 또는 **대표가 로그인해서 직접 메시지를 몇 줄 보내면서** 재기
- 360/390(폰)에서도 재기 — 채팅 칸 위치가 데스크톱과 다릅니다

**원인 확인 못 함.** 재현 조건을 못 만들었습니다.
`js/chat.js` 는 수정 금지 파일이라, 원인이 잡히면 우회 모듈이 필요합니다.

---

## 확인 못 한 것

| 무엇 | 왜 |
|---|---|
| 로그인 후 화면 전부 | 비밀번호 입력 금지 (CLAUDE.md 379~383행) |
| 서버 실제 상태 (`tl_earned` 결과, `rank_points`, 어느 SQL 버전이 올라가 있는지) | SQL 실행 금지. `health-check.sql` 은 대표가 실행해야 함 |
| TL 72,560 → 60 사건의 **실제 실행 경로** | 서버 로그·기록을 못 봄. 구조적 원인만 확인 |
| 채팅 흔들림 | 측정 15초 동안 새 메시지가 안 들어와 방아쇠가 안 당겨짐 |
| 360 / 375 / 768 폭에서의 체결 패널 상태 | P1 원인 규명에 시간을 썼습니다. 1920/390/360(시세 바만) 만 봤습니다 |
| `price-guard.js` 가 5s/15s 첫 가격에 어떻게 반응하는지 | 지금은 `price:update` 가 아예 안 와서 시험할 수가 없음 |
| 폰에서 체결이 초당 29건 흐를 때의 프레임 저하 | 고치기 전이라 측정 불가 |
| 3D 병사 — importmap 을 넣었을 때 실제로 보이는지 | 코드를 못 고치므로 확인 불가 |

---

## 테스트를 추가하지 않은 이유

주말 모드는 `tests/` 추가만 허용합니다. 이번에는 **추가하지 않았습니다.**

이번 P1 은 "코드에 무엇이 적혀 있나" 가 아니라 **"바이낸스 서버에서 그 스트림이 오는가"**
가 원인이라, 파일을 읽어 정규식으로 검사하는 이 저장소의 테스트 방식
(`tests/trades-dedupe.test.js` 같은)으로는 잡히지 않습니다.
지금 상태를 통과로 적으면 고친 뒤에 오히려 실패하고,
고친 뒤 상태를 적으면 지금 실패합니다 — 주말 모드 규칙 6번에 걸립니다.

**고치는 시점에 같이 넣을 테스트**(`tests/trade-stream.test.js`)는 이런 것이어야 합니다.

- `js/trade-stream-fix.js` 가 존재하고 `index.html`·`main.js` 에 등록돼 있다
- 그 안의 접속 주소가 `/market/ws/` + `@aggTrade` (또는 `/public/ws/` + `@trade`) 조합이다
  — `/market` + `@trade` 조합이면 **실패시킨다** (이번 버그가 그 조합이었음)
- native 간격일 때 `price:update` / `kline:update` 를 내보내지 않는다 (이중 방송 방지)
- 수정 금지 12개 해시가 그대로다

---

# 테스트 보강 (2026-08-21 · 과제 6)

> 위쪽 "테스트를 추가하지 않은 이유" 절은 **이 절로 대체됐습니다.**
> 당시엔 "지금 상태로도 통과하고, 고친 뒤에도 통과하는 테스트"를 못 만들겠다고 적었는데,
> **탐지기 + 알려진 결함 예외 목록 + 돌연변이 자체검증** 세 가지를 조합하면 가능합니다.
> (예외 목록은 "허용"이지 "요구"가 아니라서, 결함을 고쳐도 테스트는 계속 통과합니다.)

`tests/` 에 새 파일 4개를 추가했습니다. **기존 테스트 파일은 한 글자도 안 고쳤습니다.**
`index.html` · `style.css` · `js/*` 도 안 고쳤습니다. 바꾼 것은 `package.json` 의 test 목록 한 줄뿐입니다.

## 공통 설계 — 왜 이렇게 만들었나

이 저장소의 테스트는 파일을 읽어 정규식으로 검사합니다. 그래서 두 가지 함정이 있습니다.

1. **아직 안 고친 결함**을 그대로 검사하면 지금 실패합니다.
2. **지금 상태를 그대로 적어** 두면 고친 뒤에 실패합니다.

그래서 네 층으로 나눴습니다.

| 층 | 하는 일 | 지금 / 고친 뒤 |
|---|---|---|
| **탐지기** | "무엇이 잘못인가"를 순수 함수로 정의 | 항상 통과 |
| **자체검증** | 합성 입력을 넣어 탐지기가 진짜 잡는지 증명 | 항상 통과 |
| **예외 목록 + 래칫** | 알려진 결함만 허용, 그 밖의 위반이 새로 생기면 실패 | 둘 다 통과 |
| **돌연변이** | 실제 소스를 **메모리에서** 망가뜨려 다시 검사 | 항상 통과 |

**돌연변이 검사는 파일을 하나도 안 고칩니다.** 소스를 문자열로 읽어 그 문자열만 바꿔
다시 jsdom 에 올리거나 다시 검사합니다. 그래서 "이 테스트가 진짜 잡는지"가
테스트 안에 영구히 박혀 있습니다 — 사람이 나중에 따로 확인할 필요가 없습니다.

---

## 1. tests/stream-signals.test.js — 조용한 고장 (검사 36개)

**무엇을 막나** — "신호가 와야 하는데 안 오는" 상태.

- 접속 주소에 체결 계열 스트림이 있으면 그 스트림이 **실제로 있는 경로**를 쓰는지
  (실측 카탈로그: `@trade`→`/public`, `@aggTrade`→`/market`, `@depth`→`/public`,
  `@kline_`·`@ticker`·`@markPrice`→`/market`)
- `INTERVALS` 의 `native: true` 가 **바이낸스 선물이 실제로 지원하는 간격**인지
  (선물 kline 은 `1m` 이 최소입니다. `1s` 는 현물에만 있습니다)
- **비-native 간격(5s/15s)은 체결 스트림에 매달려 있다**는 관계가 안 끊겼는지
  — 비-native 에서 kline 스트림이 빠지면, 같은 주소에 체결이 **반드시** 있어야 합니다
- `js/websocket.js`(수정 금지)의 체결 분기 안에 `trade:tick` 방송 · 합성 캔들 ·
  `setCurrentPrice` 가 그대로 있는지 + md5 고정
- `js/` 전체에서 주소를 만드는 곳을 찾아 같은 규칙 적용.
  **주소를 조각내어 이어 붙여도 잡습니다** (경로 문자열 × 그 파일의 `"@스트림"` 문자열 전조합)

**실제 코드를 돌려서 검사합니다.** `js/config.js` 를 jsdom 에 올려
`buildCombinedStreamUrl()` 을 native / 비-native 두 상태로 실제로 호출합니다.

**예외 목록 2건** — `market:@trade`(TL-004), `native:1s`.
3건째가 생기면 실패합니다.

## 2. tests/placeholder-values.test.js — 모름을 반반으로 (검사 26개)

**무엇을 막나** — 데이터가 없는데 그럴듯한 숫자를 지어내는 것.

- `총합 > 0 ? 실제값 : 50` 같은 **비중립 대체값**을 `js/` 전체에서 찾습니다
  (`: 0` · `: null` · `|| 0` 은 통과 — "모름"을 모름이라 말하는 것은 정직합니다)
- 같은 비율 계산이 `js/market-war.js` 에 **두 벌** 있는데(626행·1256행),
  두 식이 **글자까지 같은지** — 한쪽만 고치고 잊는 실수를 막습니다
- 주문창 압력 바(`js/order-pressure-bar.js`)가 데이터 없음을 `null` 로 돌려주고
  화면에 `매수 —` 를 쓰는지, 전쟁터 연출값(`getBuySellRatio`)에 다시 기대지 않는지

**예외 목록 1건** — `js/market-war.js` 의 `: 50` 두 줄(전쟁터 연출용).

### 찾아본 다른 대체값 — js/ 전체에서 이 두 줄이 전부입니다

```
js/market-war.js:626   const buyPct = total > 0 ? (buyIntensity / total) * 100 : 50;
js/market-war.js:1256  const buyPct = total > 0 ? (buyIntensity / total) * 100 : 50;
```

비슷한 모양을 더 찾으려고 `> 0 ? … : 상수`, `.length ? … : 상수`, `|| 50`, `|| 100`,
`|| 0.5` 를 전부 훑었지만 다른 곳은 없었습니다. **없으면 없다고 적습니다.**
(`|| 0` 은 여러 곳에 있지만 "없으면 0" 은 지어내는 것이 아니라 사실입니다.)

한 가지 남는 것 — 고쳐진 `order-pressure-bar.js` 도 데이터가 없을 때 **막대 폭**은
`50%` 로 그립니다. 다만 글자는 `매수 —` 라 회원이 "측정값 50%" 로 오해할 여지는 없습니다.
막대까지 비울지는 디자인 판단이라 손대지 않았습니다.

## 3. tests/memory-counters.test.js — 새로고침하면 0 이 되는 카운터 (검사 25개)

**무엇을 막나** — "이미 했다"를 메모리 변수 하나로만 기억하는 것.
인계문서 3번에 **세 번** 나왔다고 적힌 유형입니다.

- 모듈 범위에서 `seen*` · `sent*` · `notified*` · `last*Synced` 처럼 **"이미 했다"를 뜻하는
  이름**으로 선언되고 `0`/`null`/`false`/`new Set()`/`[]`/`{}` 로 시작하는 변수를 찾습니다
- 함수 안(들여쓰기 4칸 이상)의 지역 변수는 세지 않습니다
- 이미 고친 두 곳이 되돌아가지 않았는지:
  - `js/trade-events-chat.js` — 저장소에 남기고, **매번 다시 읽고**(창 두 개가 서로 보게),
    건수가 아니라 **청산 시각**으로 판단하는지
  - `js/cycle-pnl.js` — 메모리 캐시가 아니라 서버 값을 보는지
- 아직 못 고친 `js/supabase-sync.js`(수정 금지)는 원인이 그대로인 것을 기록하고,
  서버(`supabase/schema-trades-dedupe.sql`)가 막고 있는지 확인

**허용 목록 5건** (전부 이유 확인함)

| 곳 | 변수 | 왜 괜찮은가 |
|---|---|---|
| `js/chat.js` | `seenIds` | 서버가 준 **메시지 id** 로 판단. 새로고침하면 화면도 함께 비므로 같이 0 이 되는 것이 맞음 |
| `js/chat.js` | `lastSentAt` | 도배 방지 간격(`Date.now()` 비교) |
| `js/login-required.js` | `notified` | 같은 안내창이 0.8초 안에 두 번 안 뜨게 하는 간격 |
| `js/multi-tab-guard.js` | `notified` | 이 탭에서 경고 한 번 — 탭 단위가 맞음 |
| `js/supabase-sync.js` | `lastSyncedTradesCount` | **진짜 결함.** 수정 금지라 서버가 막음 |

`js/chat.js` 의 `seenIds` 는 이번 스캔에서 **새로 나온 것**입니다(이전 조사에서 놓쳤습니다 —
`const` 로 선언돼 있어 `let|var` 만 보던 검색에 안 걸렸습니다). 확인해 보니 결함은 아닙니다.

## 4. tests/css-duplicate-rules.test.js — 같은 CSS 규칙이 두 벌 (검사 23개)

**무엇을 막나** — `style.css` 에 덧붙이다 뒤엣것이 앞을 덮어 수정이 안 먹히는 것.

- CSS 를 규칙 단위로 훑어 **같은 선택자가 같은 미디어쿼리 안에서 두 번** 나오는 곳을 셉니다
  (폭이 다르면 중복이 아닙니다. 쉼표로 묶인 선택자는 하나씩 따로 셉니다)
- 그중 **실제로 같은 속성을 덮어쓰는 것**을 따로 셉니다
- 지금 상태(중복 81곳 / 덮어쓰기 44곳)를 **기준선으로 통째로 박아** 두고,
  하나라도 늘거나 새 선택자가 중복되면 실패시킵니다(래칫). 없애면 그냥 통과합니다
- 최근 사고 구역(`.stats-bar` · `.stat-*` · `.tl-stats-hint-layer`)은 이름을 불러 따로 검사

### 지금 style.css 에 남아 있는 중복 — 눈여겨볼 것

| 곳 | 덮어쓰는 속성 | 비고 |
|---|---|---|
| `@media (max-width:400px) >> .stats-bar` | `gap` | **최근 사고 구역.** 시세 바 gap 이 400px 이하에서 두 벌 |
| `.top-banner-right` | `display` | 통화 전환 버튼 숨김(TL-002)과 같은 자리 |
| `:root` | `--surface` | **확정 팔레트 변수가 두 번 정의**돼 있음 |
| `.position-table td` | `font-size` | 최상위에서 4벌 |

전부 **지금 문제를 일으키고 있지는 않습니다.** 다만 그 구역을 고칠 때는 두 벌을 다 봐야 합니다.
81곳을 한 번에 정리하는 것은 CSS 대수술이라 주말 모드에서 할 일이 아니라고 판단했습니다.

---

## 5. 미디어쿼리마다 값이 다른데 하나로 가정하는 검사 — 목록만 (기존 테스트 안 고침)

`tests/stats-bar-priority.test.js` 가 이미 겪은 유형입니다.
`css.match(/\.선택자\{[\s\S]*?속성:(\d+)px/)` 는 **CSS 에서 처음 나오는 값 하나**만 읽습니다.
그 선택자가 미디어쿼리 안에서 다시 정의돼 있으면 **틀린 전제로 검사**하게 됩니다.

기존 테스트를 훑어 **그런 선택자를 읽는 곳 19군데**를 찾았습니다.
**아무것도 고치지 않았습니다.** 지금 실패하는 것도 없습니다 — 다만
그 폭에서만 나는 문제는 못 잡습니다.

| 테스트 | 줄 | 읽는 선택자 | 그 선택자가 다시 정의되는 미디어쿼리 |
|---|---|---|---|
| `chart-font.test.js` | 125 | `.ob-row` | `max-width:600px` |
| `top-panel.test.js` | 90 | `.notice-board-wrap` | `1200px` · `760px` · `700px` |
| `top-panel.test.js` | 606 | `.app` | `700px` |
| `top-panel.test.js` | 747, 1438 | `.ad-creative-wide` | `900px` |
| `top-panel.test.js` | 1091, 1398, 1408 | `.exchange-main` | `min 2000px` · `max 1799px` |
| `top-panel.test.js` | 1093, 1109 | `.main-grid` | `1300px` |
| `top-panel.test.js` | 1128 | `.side-column` | `1300px` |
| `top-panel.test.js` | 1168 | `.page-shell` | `1799px` |
| `top-panel.test.js` | 1203 | `.side-chat-panel > .page-chat-col` | `min 1800px` |
| `top-panel.test.js` | 1329 | `.top-banner-inner` | `400px` · `760px` |
| `top-panel.test.js` | 1336 | `.brand-logo` | `900px` |
| `top-panel.test.js` | 1350 | `.up-nav` | `1799px` |
| `top-panel.test.js` | 1403 | `.exchange-shell` | `min 2000px` · `min 2600px` |
| `top-panel.test.js` | 1462 | `.top-banner-nav-btn` | `1366px` · `700px` · `520px` · `400px` |
| `top-panel.test.js` | 1560 | `.mypage-grid` | `700px` · `400px` |

**고치는 방법은 이미 저장소 안에 있습니다** — `tests/stats-bar-priority.test.js` 의
`cssSegments()` / `cssValue(선택자, 속성, 폭)` 를 그대로 쓰면 됩니다
(폭을 주면 그 폭에서 실제로 적용되는 값을 특정도·순서까지 따져 돌려줍니다).
`top-panel.test.js` 는 1,600줄이 넘는 기존 테스트라 **본부장 배정을 받고 고치는 것이 맞습니다.**
가장 위험한 것은 `.top-banner-nav-btn`(구간 4개)과 `.notice-board-wrap`(구간 3개)입니다.

---

## 검증 결과 — "정말 잡는가"

두 가지로 확인했습니다.

**(가) 테스트 안에 박아 둔 돌연변이 검사** — 매번 `npm test` 때 같이 돕니다.
소스를 메모리에서 망가뜨려 검사가 뒤집히는지 확인합니다. 파일은 안 고칩니다.

**(나) 저장소 사본에 실제로 버그를 심어 돌려봤습니다.**
`js/` · `tests/` · `style.css` · `supabase/` 를 임시 폴더로 통째로 복사한 뒤 거기서만 고쳤습니다.
**원본 저장소는 이 과정에서 한 번도 안 건드렸습니다.** 확인 후 사본은 지웠습니다.

| 심은 버그 | 테스트 | 결과 |
|---|---|---|
| (대조군) 사본 그대로 | 4개 전부 | 36 · 26 · 25 · 23 통과 / **실패 0** |
| 체결을 `/public` + `@aggTrade`(실측 0건 조합)로 | stream-signals | 35 통과 / **실패 1** |
| `config.js` 에서 `@trade` 삭제 | stream-signals | 33 통과 / **실패 3** |
| `5s` 를 `native:true` 로 | stream-signals | 34 통과 / **실패 2** |
| 압력 바를 TL-005 이전(50:50)으로 되돌림 | placeholder-values | 21 통과 / **실패 5** |
| `lastSeenClosedCount` 되살림 | memory-counters | 22 통과 / **실패 3** |
| 저장소 저장을 지움(메모리에만) | memory-counters | 24 통과 / **실패 1** |
| `style.css` 뒤에 `.stats-bar{gap:99px}` 덧붙임 | css-duplicate-rules | 16 통과 / **실패 7** |
| 원복 후 | 4개 전부 | 다시 **실패 0** |

## 되돌리는 방법

```bash
cd C:/Users/pc/Documents/projects/trading-league
rm tests/stream-signals.test.js tests/placeholder-values.test.js \
   tests/memory-counters.test.js tests/css-duplicate-rules.test.js
git checkout -- package.json
```

`package.json` 은 test 목록 끝에 네 항목이 붙은 것이 전부이고,
`tests/` 새 파일 4개 외에는 아무것도 안 건드렸습니다.

> 참고 — `tests/stream-signals.test.js` 와 `tests/placeholder-values.test.js` 는
> 작업 도중 커밋 `64df744` 에 이미 들어갔습니다(수리팀이 커밋한 것이 아닙니다).
> 되돌리려면 그 두 파일은 `git rm` 이 필요합니다.

## 확인 못 한 것

| 무엇 | 왜 |
|---|---|
| 스트림 카탈로그가 **오늘 이후에도 맞는지** | 바이낸스가 또 경로를 바꾸면 카탈로그가 틀려집니다. 테스트는 코드만 보므로 실제 서버 응답은 못 봅니다 — 정기적으로 실측해 카탈로그를 갱신해야 합니다 |
| 브라우저에서 실제로 신호가 오는지 | 이 테스트들은 **소스만** 검사합니다. 실측(10초 신호 세기)은 여전히 게이트 2에서 사람이 해야 합니다 |
| `top-panel.test.js` 19군데를 고쳤을 때 무엇이 새로 실패하는지 | 기존 테스트를 안 고치기로 했으므로 돌려보지 않았습니다 |
| CSS 중복 81곳을 정리했을 때 화면이 어떻게 바뀌는지 | 기준선으로 묶어만 뒀습니다. 정리는 별건입니다 |
| 미디어쿼리 값 가정 19건이 **실제로 틀린 값을 보고 있는지** | 목록만 만들었습니다. 각 건이 진짜 오검사인지는 하나씩 봐야 합니다 |
| 이번에 고쳐진 TL-004 / TL-005 가 브라우저에서 실제로 동작하는지 | 다른 팀이 같은 시간에 적용 중이었습니다. 수리팀은 테스트만 맡았습니다 |

---

# 조사 — 통화 전환 버튼은 언제·왜 숨겨졌나 (2026-08-21 · 대표 지시)

> 대표 지시: *"통화 전환 버튼이 왜 숨겨졌는지 나도 몰라. 코드와 커밋 이력을 뒤져서
> 언제 왜 숨겨졌는지 조사해줘. 결론이 나오면 승인대기에 적어두고, **고치지는 마.**"*
>
> **고치지 않았습니다.** 코드는 한 글자도 안 건드렸습니다. 조사 결과만 적습니다.

## 결론 한 줄

**통화 전환 버튼을 숨기려고 숨긴 것이 아닙니다.**
헤더 우측 덩어리를 통째로 감추는 **레이아웃 정리의 巻き添え(딸려 들어간 것)** 이고,
**그 커밋을 만든 사람도 그 사실을 알고 커밋 메시지에 적어 두었습니다.**

## 언제

| | |
|---|---|
| 커밋 | `8fcb9039e994705425a7056242e93b4c4d2a111e` (`8fcb903`) |
| 날짜 | **2026-08-17 13:17:18 +0000** (월) |
| 작성자 | `dev <dev@local>` |
| 제목 | `style: 헤더 우측(연결 상태 · USDT/KRW · 닉네임 · 로그아웃)을 화면에서 숨김.` |
| 바꾼 파일 | `style.css` (+12줄) · `tests/top-panel.test.js` (+20줄) — **둘뿐** |

`git log -S ".top-banner-right"` 로 확인한 그 선택자의 전체 이력은 세 건뿐입니다.

```
8fcb903  헤더 우측을 화면에서 숨김          ← 지금 숨겨져 있는 원인
fbec926  헤더 가로 넘침 회귀 수정 + 죽은 미디어쿼리 정리
0d90656  상단을 거래소 스타일 풀폭 배너로 재구성
```

## 왜 — 근거 세 가지

### 근거 ① 커밋이 스스로 이유를 적어 두었습니다

숨긴 목적은 **헤더 공지 줄에 폭을 내주는 것**입니다. 커밋 메시지 원문:

> 우측이 빠진 만큼 헤더 공지 줄이 남은 폭을 씁니다(1920px에서 1580px,
> 2870px에서 2530px). 헤더 높이는 123px 그대로입니다.

실제로 들어간 CSS도 두 줄이고, 두 번째 줄이 그 목적을 그대로 말합니다.

```css
.top-banner-right{display:none;}
/* 우측이 빠진 만큼 헤더 공지 줄이 남은 폭을 씁니다 */
.top-banner-inner > .header-notice{margin-right:0;}
```

### 근거 ② **작성자가 통화 버튼이 딸려 들어간 것을 알고 남겨 두었습니다**

커밋 메시지 **맨 마지막 줄**이 이것입니다. (원문 그대로)

> 참고: 통화 전환(USDT/KRW) 버튼도 함께 숨겨져 사용자가 KRW 표시로 바꿀 수 없습니다.
> **필요하면 통화 버튼만 다시 노출할 수 있습니다.**

즉 **"통화 전환을 없애자"는 판단이 아니었습니다.** 목표는 연결 상태·닉네임·로그아웃이
있는 우측 덩어리를 치우는 것이었고, 통화 버튼은 같은 상자 안에 있어서 같이 사라졌습니다.
작성자는 그것을 알고 **되살리는 길까지 열어 두었습니다.**

### 근거 ③ 앞 커밋들을 보면 왜 우측을 치웠는지가 보입니다

`8fcb903` 바로 앞 세 커밋이 전부 **"내 정보" 패널을 만드는 작업**입니다.

```
7caa32b  style: 내 정보 값 표 — 자산 통화 기호 제거 + 상승 빨강 / 하락 파랑.
c6f0761  feat: 내 정보 값 표 라벨을 레퍼런스 구성으로 — 선물 / 포인트 / USDT / 수익률.
77e188e  style: 내 정보 값 표를 레퍼런스와 같은 셀 스타일로.
```

헤더 우측에 있던 기능(닉네임·로그아웃)이 **"내 정보" 패널로 옮겨가서 중복**이 됐고,
그래서 헤더 우측을 치운 것입니다. 실제로 커밋 메시지도 로그아웃이 어떻게 이어지는지를
따로 확인해 적어 두었습니다.

> `#auth-logout-btn`은 내 정보 패널의 '로그아웃'이 대신 눌러주는 대상이라 삭제하면
> 로그아웃이 동작하지 않습니다. 숨김 상태에서도 JS의 `.click()`이 전달되는 것을
> 실제로 확인했습니다.

**닉네임·로그아웃·연결상태는 갈 곳이 있었습니다. 통화 전환만 갈 곳이 없었습니다.**
그게 지금 상태의 전부입니다.

## 어떤 경로로 가려지는지 (코드로 확정)

버튼은 `index.html` 에 **없습니다.** `js/ui.js`(**수정 금지 파일**)가 실행할 때 만듭니다.

`js/ui.js` 222~232행:

```js
// 상단 통화 전환 버튼 [USDT] [KRW] — topbar의 ws-status 옆에 추가
const wsStatus = document.querySelector(".ws-status");
if (wsStatus && !el("currency-toggle")) {
  const wrap = document.createElement("div");
  wrap.id = "currency-toggle";
  ...
  wsStatus.parentNode.insertBefore(wrap, wsStatus.nextSibling);   // ← 부모가 .top-banner-right
}
```

`.ws-status` 의 부모가 `.top-banner-right` 라서, 버튼은 **하필 숨겨진 상자 안에** 생깁니다.

### 브라우저 실측으로 확인 (2026-08-21, 1440px, 비로그인)

```
#currency-toggle 존재                  : true
  .top-banner-right 안에 있나           : true
  자기 자신의 display                   : flex      ← 버튼 자체는 안 숨겨짐
  조상 .top-banner-right 의 display     : none      ← 여기서 가려짐
  화면에 보이나(offsetParent)           : false
  차지하는 폭                           : 0px
#btn-cur-usdt / #btn-cur-krw 존재       : 둘 다 true
btn-cur-krw 를 코드로 눌러 봄           : USDT -> KRW  ← 핸들러는 살아 있음
```

**기능은 멀쩡히 살아 있고 픽셀만 없습니다.** 그래서 되살리는 비용이 매우 낮습니다.

## `style.css` 에 `.top-banner-right` 가 세 군데 있습니다 (고칠 때 주의)

```
142행   .top-banner-right{display:flex; ... }                 ← 원래 규칙
1813행  @media (max-width:760px){ .top-banner-right{...} }    ← 좁은 화면 조정
2117행  .top-banner-right{display:none;}                      ← 8fcb903 이 넣은 줄
```

`tests/css-duplicate-rules.test.js` 가 "`.top-banner-right` 의 `display` 가 두 벌"이라고
잡아 둔 그 자리입니다. 되살릴 때 **2117행만 지우면 142행이 다시 살아납니다.**

## 되살린다면 (참고용 · **적용하지 않았습니다**)

우측 덩어리 전체가 아니라 **통화 버튼만** 되살리는 쪽이 8fcb903 의 의도를 유지합니다
(작성자가 직접 "통화 버튼만 다시 노출할 수 있습니다"라고 적어 둔 그 방법입니다).
`.top-banner-right{display:none}` 은 그대로 두고, 그 안의 `#currency-toggle` 만
되돌리는 규칙 한 줄이면 됩니다.

```css
/* 8fcb903 은 헤더 폭을 얻으려고 우측 덩어리를 감췄습니다. 통화 전환은 갈 곳이
   없어서 같이 사라졌을 뿐입니다(그 커밋 메시지에 그렇게 적혀 있습니다).
   덩어리는 계속 감춘 채, 통화 버튼만 다시 보이게 합니다. */
.top-banner-right{display:none;}                    /* 2117행 — 그대로 둠 */
.top-banner-right:has(> #currency-toggle){display:flex;}   /* 상자만 되살리고 */
.top-banner-right > *{display:none;}                       /* 안은 전부 감춘 뒤 */
.top-banner-right > #currency-toggle{display:flex;}        /* 통화 버튼만 다시 */
```

**이 CSS 는 검증하지 않았습니다.** `:has()` 지원과 좁은 화면 배치를 6폭 전부에서
재봐야 합니다. **대표 결정 전에는 손대지 않습니다.**

세 번째 선택지도 있습니다 — **버튼을 헤더가 아닌 다른 곳(예: 내 정보 패널)에 두는 것.**
헤더 폭을 되돌려 받지 않아도 되고, 8fcb903 의 목적과 아예 충돌하지 않습니다.
다만 `js/ui.js` 가 버튼을 `.ws-status` 옆에 만들도록 **박아 두어서**(위 222행),
옮기려면 별도 모듈이 버튼을 만들어진 뒤에 다른 곳으로 **옮겨 담아야** 합니다.

## 확인 못 한 것

| 무엇 | 왜 |
|---|---|
| **몇 명이 KRW 에 갇혀 있는지** | 선택한 통화는 각자 브라우저(`App.Storage` 의 `settings`)에 저장됩니다. 서버에 없어서 셀 방법이 없습니다 |
| 8/17 이전에 실제로 KRW 로 바꾼 회원이 있었는지 | 같은 이유. 있었는지 없었는지도 모릅니다 |
| 대표가 그때 이 커밋을 보고 승인했는지 | 저장소에 그 대화 기록이 없습니다. 커밋 메시지 외의 근거는 못 찾았습니다 |
| 위 "되살린다면" CSS 가 실제로 되는지 | **적용하지 말라는 지시라 실행하지 않았습니다** |

---

# [작업] 회원 계정 삭제 — CHRO · Mang9 (2026-08-24)

대표 지시: **"김갱, 김갱티비 빼고 다 지워"**

**SQL 파일만 만들었습니다. 실행하지 않았습니다.** 실서버에 어떤 쿼리도 보내지 않았습니다.
삭제는 대표가 Supabase SQL Editor 에서 직접 하십니다.

| 닉네임 | 가입일 | 처리 |
|---|---|---|
| 김갱 | 2026-08-14 | 남김 |
| 김갱TV | 2026-08-20 | 남김 |
| CHRO | 2026-08-19 | **지움** |
| Mang9 | 2026-08-20 | **지움** |

## 되돌릴 수 없습니다

한 번 지우면 그 회원의 거래기록·글·댓글·채팅·TL·아이템이 전부 사라집니다.
Supabase 무료 플랜에는 시점 복구(PITR)가 없습니다.
**복구 방법은 "2절에서 받아 둔 CSV 를 보고 사람이 다시 입력하는 것" 뿐입니다.**

그래서 파일을 4절로 쪼갰습니다. **2절(백업 CSV)을 건너뛰면 되돌릴 방법이 0 이 됩니다.**

## 파일

`supabase/회원삭제-2026-08-24.sql` (668줄)

| 절 | 하는 일 | 안전한가 |
|---|---|---|
| 1절 | 무엇이 몇 건 지워지는지 미리 보여줌 | 읽기만 함 |
| 2절 | 지워질 행을 전부 조회 (CSV 로 받아 보관) | 읽기만 함 |
| 3절 | 실제 삭제 | **여기서 지워짐** |
| 4절 | 제대로 지워졌는지 확인 | 읽기만 함 |

**절 단위로 나눠 실행하도록 안내를 넣었습니다. 한 번에 다 돌리면 안 됩니다.**

## 회원 데이터가 들어 있는 표 — 전수 조사 결과

`supabase/*.sql` 40개를 전부 읽고 `references auth.users(id)` 를 찾았습니다.
**19개 표 전부 `on delete cascade` 입니다.**

| # | 표 | 무엇 |
|---|---|---|
| 1 | `profiles` | 닉네임 (PK 가 `auth.users.id`) |
| 2 | `trading_accounts` | 지갑·기준자본·실현손익 |
| 3 | `positions` | 보유 포지션 |
| 4 | `orders` | 주문 |
| 5 | `trades` | 종료된 거래 |
| 6 | `trading_cycles` | 지난 사이클 성적 |
| 7 | `chat_messages` | 채팅 |
| 8 | `posts` | 글 |
| 9 | `post_comments` | 댓글 |
| 10 | `post_votes` | 좋아요·싫어요 |
| 11 | `post_reports` | 글 신고 |
| 12 | `comment_reports` | 댓글 신고 |
| 13 | `post_view_log` | 조회수 중복 방지 기록 |
| 14 | `tl_purchases` | 핫딜 구매 |
| 15 | `tl_transactions` | TL 사용·환불·지급 |
| 16 | `user_items` | 아이템 보관함 |
| 17 | `item_usage_logs` | 아이템 사용 기록 |
| 18 | `customer_private_info` | 개인정보(전화·이메일·provider) |
| 19 | `admin_users` | 관리자 명단 |

`ranks` · `app_meta` · `app_settings` · `tl_products` · `tl_market_products` 는
회원과 무관합니다(설정·상품 표). 건드리지 않습니다.

`leaderboard` · `ranking_profit` · `posts_with_meta` · `user_rank_points` 는
**뷰**라 저장된 데이터가 없습니다. 원본이 사라지면 자동으로 같이 사라집니다.

## 외래키가 없어 자동으로 안 지워지는 자리 2개

| 자리 | 무엇 | 지우면 |
|---|---|---|
| `trading_cycles.ended_by` | 계정 초기화를 실행한 관리자 | 외래키 없음 → 번호만 남음 |
| `app_settings.updated_by` | 설정을 바꾼 관리자 | 외래키 없음 → 번호만 남음 |

오류는 나지 않습니다. "누구인지 알 수 없는 번호"가 남을 뿐입니다.
1절 1-9 에서 몇 건인지 세어 보여주고, 3절 맨 아래에 **주석 처리된 정리 UPDATE** 를
넣어 뒀습니다. 이 UPDATE 는 김갱·김갱TV 의 기록도 건드릴 수 있어서
**기본값은 실행 안 함**입니다. 필요하면 주석을 풀어 돌리십니다.

## auth.users (로그인 계정) 처리

**`profiles` 만 지우면 안 됩니다.** 로그인 계정은 `auth.users` 에 따로 있어서,
프로필만 지우면 **로그인은 되는데 프로필이 없는 상태**가 됩니다.
그 상태로 접속하면 `js/auth.js` 가 닉네임을 새로 만들며 계정이 되살아납니다.

그래서 3-8 에서 `auth.users` 를 **`profiles` 보다 먼저** 지웁니다
(profiles 를 먼저 지우면 닉네임 조건으로 대상을 못 찾습니다).

SQL Editor 에서 `auth.users` 를 지우는 것이 되는지는 **직접 확인하지 못했습니다**
(실서버에 쿼리를 보내지 않았습니다). 다만 이 저장소에 근거가 둘 있습니다.

- `supabase/cleanup-test-data.sql` — `delete from auth.users` 를 그대로 씁니다
- `supabase/schema-mypage.sql:156` — 회원탈퇴 함수 `delete_my_account()` 안에
  `delete from auth.users where id = uid;` 가 있고 `security definer` 입니다

**권한 오류가 나는 경우를 대비해 대안을 파일 안에 적어 뒀습니다** —
대시보드 > Authentication > Users 에서 하나씩 Delete user.
`on delete cascade` 가 전부 걸려 있어서 대시보드에서 지워도 결과는 같습니다.

### 로그인 이메일이 이상하게 보이는 이유

이 사이트는 닉네임으로 로그인하고, 내부적으로 닉네임을 16진수로 바꿔
가짜 이메일을 만듭니다 (`js/auth.js` 의 `nicknameToEmail`, 도메인 `@btcsim.local`).

| 닉네임 | 이메일 | 처리 |
|---|---|---|
| CHRO | `u4348524f@btcsim.local` | 지움 |
| Mang9 | `u4d616e6739@btcsim.local` | 지움 |
| 김갱 | `ueab980eab0b1@btcsim.local` | 남김 |
| 김갱TV | `ueab980eab0b15456@btcsim.local` | 남김 |

대시보드에서 지울 때 이 표로 대조하십시오. 파일 안에도 같은 표를 넣어 뒀습니다.
(카카오·네이버로 가입한 계정이면 이메일이 다릅니다 — 그때는 1-10 결과의 닉네임 열로 판단)

## 안전장치

- **조건 없는 DELETE 가 하나도 없습니다.** 26개 DELETE 전부에
  `nickname in ('CHRO','Mang9') and nickname not in ('김갱','김갱TV')` 가 붙어 있습니다
- **`begin` / `commit` 으로 감쌌습니다.** 중간에 하나라도 실패하면 전부 취소됩니다
- **3-0 안전장치** — 지울 대상이 2명이 아니거나 남길 대상이 2명이 아니면
  `raise exception` 으로 멈춥니다. 닉네임 철자가 실제와 다르면 여기서 걸립니다
- **TRUNCATE · DROP TABLE · ALTER TABLE 이 없습니다**
- **1절·2절은 읽기만 합니다**

`tests/member-delete-sql.test.js` 가 위 항목을 매번 검사합니다(59개).
저장소에 이미 있는 `cleanup-test-data.sql`(전원 삭제)을 이 검사에 넣으면
실제로 걸리는 것까지 확인합니다.

## 알아야 할 부작용

**글 1건이 CHRO/Mang9 것이면 그 글이 통째로 사라지고, 거기 달린 댓글·추천도
같이 사라집니다 — 김갱이 단 댓글이라도 마찬가지입니다.**
외래키가 `posts(id) on delete cascade` 라 막을 수 없습니다.
누구 글인지·누가 댓글을 달았는지는 1절 1-6 / 1-8 에서 먼저 보실 수 있습니다.

랭킹(`leaderboard`)은 `profiles` 와 `join` 이라 지운 회원이 자동으로 빠집니다.
게시판 뷰(`posts_with_meta`)는 `left join` + `coalesce(...,'(알수없음)')` 이라,
**만약 프로필만 지워지고 글이 남으면 작성자가 '(알수없음)' 으로 보입니다.**
4절 4-6 에서 그게 없는지 확인하도록 넣어 뒀습니다.

## 대표가 할 일

1. https://supabase.com/dashboard/project/oxpjpotilcumjqixsdxw/sql/new 열기
2. `supabase/회원삭제-2026-08-24.sql` 의 **1절만** 붙여넣고 Run → 결과 확인
   (특히 1-2 가 `2`, 1-3 이 `2` 인지)
3. **2절만** Run → 쿼리마다 Download CSV 로 받아 보관
4. **3절만** Run ← 여기서 지워집니다
5. **4절만** Run → 남은 계정이 김갱·김갱TV 둘뿐인지 확인

## 확인 못 한 것

| 무엇 | 왜 |
|---|---|
| 실제 계정이 4개가 맞는지 | 실서버에 붙지 않았습니다. 1절 1-1 로 대표가 확인합니다 |
| 닉네임 철자(`김갱TV` vs `김갱티비` 등) | 같은 이유. 다르면 3-0 안전장치가 멈춥니다 |
| CHRO·Mang9 가 관리자인지 | 같은 이유. 1절 1-4 에 확인 쿼리를 넣었습니다 |
| 글 1건·채팅 5건이 누구 것인지 | 같은 이유. 1절 1-6 / 1-7 에 넣었습니다 |
| SQL Editor 에서 `auth.users` DELETE 가 되는지 | 실행하지 않았습니다. 저장소 근거는 2개(위), 안 되면 대시보드 대안 |
| 지운 뒤 화면이 실제로 어떻게 보이는지 | 지우기 전이라 잴 수 없습니다. 4-4~4-6 확인 쿼리로 대신합니다 |
